# Report

Place the final exported PDF here as `HW1_report.pdf`. If the report source (Overleaf
`.tex` project) is mirrored into Git, put it in `report/src/`.

Reminders:
- Title must be exactly: *Predicting Cache Evolution: The Growing Capacity and Latency
  Cost of CPU Caching*
- GitHub URL and Overleaf URL must appear on page 1.
- All main reverse-engineering source-code listings (x86-64 and AArch64 paths) must
  appear in the body, not only as GitHub links.
- Contribution appendix (Tables 6-14) goes at the end — see `../CONTRIBUTION_APPENDIX.md`.
