\documentclass{article}

\usepackage[margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{tabularx}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{amsmath}
\usepackage{hyperref}
\usepackage{float}
\usepackage[section]{placeins}
\usepackage{needspace}
\usepackage{subcaption}
\usepackage{grffile} % allows underscores and multiple dots in \includegraphics paths
\usepackage{pifont}
\newcommand{\cmark}{\ding{51}}
\newcommand{\xmark}{\ding{55}}


\renewcommand{\arraystretch}{1.2}

\definecolor{codegray}{gray}{0.95}
\lstdefinestyle{cstyle}{
    language=C,
    backgroundcolor=\color{codegray},
    basicstyle=\ttfamily\scriptsize,
    keywordstyle=\color{blue!60!black}\bfseries,
    commentstyle=\color{green!35!black}\itshape,
    stringstyle=\color{orange!70!black},
    numbers=left,
    numberstyle=\tiny\color{gray},
    numbersep=6pt,
    breaklines=true,
    breakatwhitespace=false,
    showstringspaces=false,
    frame=single,
    tabsize=4,
    columns=fullflexible,
    captionpos=b,
}
\lstdefinestyle{makestyle}{
    language=make,
    backgroundcolor=\color{codegray},
    basicstyle=\ttfamily\scriptsize,
    breaklines=true,
    showstringspaces=false,
    frame=single,
    tabsize=4,
    columns=fullflexible,
    captionpos=b,
}
\lstset{style=cstyle}

% Helper macros for the repetitive per-machine figure grids in
% Section~\ref{sec:permachine} (associativity L1/L2/LLC triptych; hit-latency
% 4-panel; miss-latency 3-panel). Figures are unlabeled individually (no
% \label inside) since they repeat 8x with identical structure; each
% machine's subsection itself carries the citable \label.
\newcommand{\assocthreepanel}[4]{%
\begin{figure}[H]
\centering
\begin{subfigure}[t]{0.32\textwidth}\centering\includegraphics[width=\linewidth]{#1}\caption{L1}\end{subfigure}\hfill
\begin{subfigure}[t]{0.32\textwidth}\centering\includegraphics[width=\linewidth]{#2}\caption{L2}\end{subfigure}\hfill
\begin{subfigure}[t]{0.32\textwidth}\centering\includegraphics[width=\linewidth]{#3}\caption{LLC}\end{subfigure}
\caption{#4}
\end{figure}}

\newcommand{\hitfourpanel}[5]{%
\begin{figure}[H]
\centering
\includegraphics[width=0.48\linewidth]{#1}\hfill\includegraphics[width=0.48\linewidth]{#2}\par\medskip
\includegraphics[width=0.48\linewidth]{#3}\hfill\includegraphics[width=0.48\linewidth]{#4}
\caption{#5}
\end{figure}}

\newcommand{\missthreepanel}[4]{%
\begin{figure}[H]
\centering
\begin{subfigure}[t]{0.32\textwidth}\centering\includegraphics[width=\linewidth]{#1}\caption{L1$\to$L2}\end{subfigure}\hfill
\begin{subfigure}[t]{0.32\textwidth}\centering\includegraphics[width=\linewidth]{#2}\caption{L2$\to$LLC}\end{subfigure}\hfill
\begin{subfigure}[t]{0.32\textwidth}\centering\includegraphics[width=\linewidth]{#3}\caption{LLC$\to$DRAM}\end{subfigure}
\caption{#4}
\end{figure}}

\title{Predicting Cache Evolution: The Growing Capacity and Latency Cost of CPU Caching}
\author{Nick Ngo, Kevin Chen}
\date{August 2026}

\begin{document}

\maketitle

\begin{center}
\textbf{GitHub repository:} \url{https://github.com/NicholasNgo4/Proj1-Cache-Reverse-Engineering}\\
\textbf{Overleaf project:} \url{https://www.overleaf.com/project/6a933c98d84565980e9e4385}
\end{center}

\section{Introduction}

The goal of this project is to reverse engineer the CPU cache hierarchy using controlled software microbenchmarks and timing measurements. The project compares cache behavior across multiple Intel, AMD, and Arm microarchitectures and CPU generations.

\section{Experimental Platforms and Architecture Research}
\label{sec:platforms}

Table~\ref{tab:machines} summarizes the ECE/Ajorpaz lab machines used during the timing-only phase.

\begin{table}[htbp]
\centering
\small
\caption{ECE/Ajorpaz Lab Machines and Architecture Research}
\label{tab:machines}

\begin{tabularx}{\textwidth}{|l|X|l|l|l|l|l|}
\hline
\textbf{Machine} &
\textbf{ISA} &
\textbf{Vendor} &
\textbf{CPU Model} &
\textbf{Microarchitecture} &
\textbf{Year} &
\textbf{Node} \\
\hline

Sunbird &
x86-64 &
Intel &
2x Xeon E5-2680 v3 &
Haswell &
2014 &
22 nm\\
\hline

Thunderbird &
AArch64 &
Ampere &
Ampere Q80-30 &
ARM Neoverse N1 &
2020 &
7 nm\\
\hline

Skylark &
x86-64 &
AMD &
2x AMD EPYC 7532 &
Zen 2 &
2019 &
7 nm \\
\hline

Artemisia &
x86-64 &
Intel &
2x Xeon Gold 5420+ &
Sapphire Rapids &
2023 &
10 nm, Intel 7\\
\hline

Charnwood &
x86-64 &
Intel &
Core i7-6700 &
Skylake &
2015 &
14 nm\\
\hline

Crux &
x86-64 &
Intel &
Core i7-9700 &
Coffee Lake &
2019 &
14 nm\\
\hline

Ookay &
x86-64 &
Intel &
Core i7-7700 &
Kaby Lake &
2017 &
14 nm\\
\hline

Upgrade &
x86-64 &
Intel &
Core i7-8700 &
Coffee Lake &
2017 &
14 nm\\
\hline

\end{tabularx}

\end{table}

\section{Experimental Methodology}

\subsection{Part 1 -- Cache Benchmarks}

Every timing-only reverse-engineering target in this project -- cache-level/capacity
discovery, line size, associativity, hit latency, miss/next-level latency, and
inclusion/exclusion behavior (Section~\ref{sec:phase1}) -- is built from one shared
measurement primitive implemented once in \texttt{main\_code/common/} and compiled into a
single portable command-line tool, \texttt{cache\_bench}
(\texttt{main\_code/common/main.c}). Every experiment differs only in \emph{which}
parameter it sweeps and how it lays out the pointer-chase graph; the load, timing, and
batching machinery underneath is identical across all six targets and is described once
here so later subsections can point back to a listing number instead of repeating it, per
the reused-common-code convention required for this report.

\subsubsection{Dependent Pointer-Chase Primitive}

Every measurement in this codebase is built around a single dependent load chain: a
singly-linked cycle of fixed-size nodes, each holding only a \texttt{next} pointer, laid
out over a working-set buffer of controlled size and node spacing. Because the address of
load $i+1$ is the \emph{value} produced by load $i$, the CPU cannot issue, prefetch, or
reorder load $i+1$ before load $i$ completes -- this is what isolates true load-to-use
cache/TLB latency instead of memory bandwidth or issue-width effects. Listing~\ref{lst:pchase}
shows the whole primitive; it is architecture-independent and is reused, unmodified, by
every experiment in Section~\ref{sec:phase1}.

\begin{lstlisting}[caption={Dependent pointer-chase primitive (portable, all architectures). File: \texttt{main\_code/common/pointer\_chase.h} / \texttt{pointer\_chase.c}, commit \texttt{e0f2538}.},label={lst:pchase}]
#ifndef POINTER_CHASE_H
#define POINTER_CHASE_H

#include <stdint.h>

struct node {
    struct node *next;
};

void chase(struct node *p, uint64_t steps);

#endif

/* ---- pointer_chase.c ---- */
#include "pointer_chase.h"

static volatile struct node *sink_node;

void chase(struct node *p, uint64_t steps) {
    for (uint64_t i = 0; i < steps; i++) {
        p = p->next; /* next address depends on previous load */
    }
    sink_node = p; /* keep the final result observable */
}
\end{lstlisting}

The result of the final load is stashed in a \texttt{volatile} sink so the compiler cannot
prove the whole loop is dead and eliminate it -- necessary discipline even at the
\texttt{-O0} baseline used throughout this project (Section~\ref{sec:build}).

\subsubsection{Timing: Serialized Cycle Counters (x86-64 and AArch64)}

Every experiment measures elapsed time in raw CPU cycles ("ticks") via a portable
\texttt{timer\_start()}/\texttt{timer\_stop()} pair that dispatches, at compile time, to an
architecture-specific implementation (Listing~\ref{lst:timer}). This is the \emph{only}
architecture-specific code in the entire codebase -- every experiment-specific file
(Section~\ref{sec:phase1}) calls only the portable \texttt{timer.h} interface and requires
zero changes to run on Sunbird/Skylark/Crux/Ookay/Upgrade/Charnwood/ Artemisia (x86-64) or
Thunderbird (AArch64).

\begin{lstlisting}[caption={Portable timer dispatch. File: \texttt{main\_code/common/timer.h}, commit \texttt{e0f2538}.},label={lst:timer}]
#ifndef TIMER_H
#define TIMER_H

#include <stdint.h>

#if defined(__x86_64__) || defined(__i386__)

#include "../x86_64/timer_x86.h"

static inline uint64_t timer_start(void){
    return x86_tsc_start();
}

static inline uint64_t timer_stop(void){
    return x86_tsc_stop();
}

#elif defined(__aarch64__)

#include "../aarch64/timer_arm.h"

static inline uint64_t timer_start(void){
    return arm_counter_start();
}

static inline uint64_t timer_stop(void){
    return arm_counter_stop();
}

#else

#error "Unsupported architecture"

#endif

#endif
\end{lstlisting}

On x86-64 (Listing~\ref{lst:timerx86}), \texttt{timer\_start()} issues \texttt{LFENCE}
before \texttt{RDTSC} to drain in-flight instructions so the cycle read is not reordered
ahead of the code it is meant to time, and \texttt{timer\_stop()} uses the partially
self-serializing \texttt{RDTSCP} (which also drains prior instructions before reading the
counter) followed by a further \texttt{LFENCE} to prevent the \emph{next} region's
instructions from being reordered backward into the timed window.

\begin{lstlisting}[caption={x86-64 serialized TSC timer. File: \texttt{main\_code/x86\_64/timer\_x86.h}, commit \texttt{e0f2538}. Target architecture: x86-64.},label={lst:timerx86}]
#include <stdint.h>
#include <x86intrin.h>

static inline uint64_t x86_tsc_start(void) {
    _mm_lfence();
    uint64_t t = __rdtsc();
    _mm_lfence();
    return t;
}

static inline uint64_t x86_tsc_stop(void) {
    unsigned aux;
    uint64_t t = __rdtscp(&aux);
    _mm_lfence();
    return t;
}

static inline uint64_t time_one_load(volatile const uint64_t *p) {
    uint64_t t0 = x86_tsc_start();
    uint64_t value = *p;
    uint64_t t1 = x86_tsc_stop();
    __asm__ __volatile__("" : "+r"(value) :: "memory");
    return t1 - t0;
}
\end{lstlisting}

On AArch64 (Listing~\ref{lst:timerarm}), the free-running virtual counter
\texttt{CNTVCT\_EL0} is read between \texttt{DSB SY}/\texttt{ISB} barrier pairs, which
serialize the instruction stream in the same role \texttt{LFENCE} plays on x86. A real
resolution limitation surfaced on Thunderbird (Ampere Q80-30, Neoverse N1):
\texttt{CNTFRQ\_EL0} reports only 25\,MHz, so ticks/access for the whole 1\,KiB--1\,GiB
capacity range span roughly 0.08--2.4 -- below the x86-calibrated automatic boundary/knee
detectors' default absolute-tick thresholds (Sections~\ref{sec:capacity} and
\ref{sec:assoc}), which had to be rescaled per-machine on this ISA rather than reused
as-is.

\begin{lstlisting}[caption={AArch64 serialized virtual-counter timer. File: \texttt{main\_code/aarch64/timer\_arm.h}, commit \texttt{e0f2538}. Target architecture: AArch64.},label={lst:timerarm}]
#include <stdint.h>
static inline uint64_t arm_counter_start(void) {
    uint64_t t;
    __asm__ __volatile__(
        "dsb sy\n\t"
        "isb\n\t"
        "mrs %0, cntvct_el0\n\t"
        "isb\n\t"
        : "=r"(t) :: "memory");
    return t;
}

static inline uint64_t arm_counter_stop(void) {
    uint64_t t;
    __asm__ __volatile__(
        "dsb sy\n\t"
        "isb\n\t"
        "mrs %0, cntvct_el0\n\t"
        "isb\n\t"
        : "=r"(t) :: "memory");
return t;
}

static inline uint64_t arm_counter_freq(void) {
    uint64_t f;
    __asm__ __volatile__("mrs %0, cntfrq_el0" : "=r"(f));
    return f;
}
\end{lstlisting}

\subsubsection{Batched Measurement and Statistical Discipline}
\label{sec:batching}

A single \texttt{timer\_start()}/\texttt{timer\_stop()} pair carries tens of ticks of fixed
overhead (quantified directly in Section~\ref{sec:misslatency}), so every experiment except
miss/inclusion latency (which are inherently single-shot, Sections~\ref{sec:misslatency}
and~\ref{sec:inclusion}) times a \emph{batch} of $B$ back-to-back dependent accesses as one
region and reports the batch average, amortizing that fixed cost across $B{=}1000$
accesses by default. Batching this way -- rather than reporting one grand mean per
working-set size -- preserves the per-batch distribution, which downstream repeat-run and
box-plot analysis (Sections~\ref{sec:capacity}--\ref{sec:inclusion}) depends on to tell a
genuine cache-level transition apart from run-to-run system noise. The
\texttt{>= 1{,}000{,}000} timed-access-per-point requirement is enforced structurally, not
by convention: \texttt{num\_batches} is computed by ceiling division of
\texttt{samples}/\texttt{batch\_size}, and untimed warm-up passes never write into the
latency array that is later printed, so a warm-up access can never be miscounted as a timed
sample. Listing~\ref{lst:benchmark} also shows the required independent-load control used
by the hit-latency experiment (Section~\ref{sec:hitlatency}): each load's address is drawn
from a precomputed permutation instead of the previous load's result, which exposes
memory-level parallelism (MLP) and is expected to read \emph{faster} than the dependent
chain at the same footprint -- this is the mechanism the assignment's MLP sanity check
relies on.

\begin{lstlisting}[caption={Batched dependent and independent load timers (portable). File: \texttt{main\_code/common/benchmark.h} / \texttt{benchmark.c}, commit \texttt{e343732}.},label={lst:benchmark}]
void measure_dependency_chain_batched(struct node *start,
                                       uint64_t batch_size,
                                       size_t num_batches,
                                       double *out_latencies);

void measure_independent_loads_batched(struct node *nodes,
                                        const size_t *order,
                                        size_t order_len,
                                        uint64_t batch_size,
                                        size_t num_batches,
                                        double *out_latencies);

/* ---- benchmark.c ---- */
static volatile struct node *bench_sink;

void measure_dependency_chain_batched(struct node *start,
                                       uint64_t batch_size,
                                       size_t num_batches,
                                       double *out_latencies)
{
    struct node *p = start;

    for (size_t b = 0; b < num_batches; b++) {
        uint64_t t0 = timer_start();
        for (uint64_t i = 0; i < batch_size; i++) {
            p = p->next; /* next address depends on previous load */
        }
        uint64_t t1 = timer_stop();
        out_latencies[b] = (double)(t1 - t0) / (double)batch_size;
    }

    bench_sink = p; /* keep the final result observable */
}

void measure_independent_loads_batched(struct node *nodes,
                                        const size_t *order,
                                        size_t order_len,
                                        uint64_t batch_size,
                                        size_t num_batches,
                                        double *out_latencies)
{
    struct node *last = NULL;
    size_t idx = 0;

    for (size_t b = 0; b < num_batches; b++) {
        uint64_t t0 = timer_start();
        for (uint64_t i = 0; i < batch_size; i++) {
            /* Address comes from the caller-supplied permutation, not from
             * the value just loaded -- unlike measure_dependency_chain_batched(),
             * nothing here forces one load to wait on the previous one. */
            last = nodes[order[idx]].next;
            idx++;
            if (idx == order_len) {
                idx = 0;
            }
        }
        uint64_t t1 = timer_stop();
        out_latencies[b] = (double)(t1 - t0) / (double)batch_size;
    }

    bench_sink = last; /* keep the final result observable */
}
\end{lstlisting}

\subsubsection{Access-Pattern Construction: Randomized vs.\ Sequential Control}

Every experiment builds its dependent cycle one of two ways (Listing~\ref{lst:random}):
\texttt{make\_random\_cycle\_strided()} performs a seeded Fisher--Yates shuffle (using a
32-bit xorshift PRNG) over the node indices and links them in that shuffled order, so
consecutive accesses land at pseudo-random offsets a hardware stride prefetcher cannot
predict; \texttt{make\_sequential\_cycle\_strided()} links nodes in plain array order, a
fully stride-predictable control. Every capacity, line-size, and associativity sweep in
this project (Section~\ref{sec:phase1}) is run at \emph{both} patterns over the same swept
range: if a detected transition also appears, unchanged, on the sequential control, it is
a prefetcher or fixed-overhead artifact rather than a genuine cache-capacity/line-size/
associativity effect, and is discounted. The \emph{strided} variants generalize node
spacing beyond \texttt{sizeof(struct node)}, which is what lets the same construction be
reused unmodified for line size (spacing = candidate line size, Section~\ref{sec:linesize})
and associativity (spacing = a cache level's own confirmed capacity,
Section~\ref{sec:assoc}).

\begin{lstlisting}[caption={PRNG, shuffle, and cycle construction (portable). File: \texttt{main\_code/common/random.h} / \texttt{random.c}, commit \texttt{e343732}.},label={lst:random}]
uint32_t xorshift32(uint32_t *state) {
    uint32_t x = *state;
    x ^= x << 13;
    x ^= x >> 17;
    x ^= x << 5;
    return *state = x;
}

static struct node *node_at(void *base, size_t i, size_t stride) {
    return (struct node *)((uint8_t *)base + i * stride);
}

void make_random_cycle_strided(void *base, size_t n, size_t stride, uint32_t seed) {
    size_t *order = malloc(n * sizeof(*order));
    if (!order || n < 2) exit(1);
    for (size_t i = 0; i < n; i++) order[i] = i;

    uint32_t state = seed ? seed : 1u;
    for (size_t i = n - 1; i > 0; i--) {
        size_t j = (size_t)(xorshift32(&state) % (uint32_t)(i + 1));
        size_t tmp = order[i];
        order[i] = order[j];
        order[j] = tmp;
    }
    for (size_t i = 0; i < n; i++)
        node_at(base, order[i], stride)->next = node_at(base, order[(i + 1) % n], stride);
    free(order);
}

void make_sequential_cycle_strided(void *base, size_t n, size_t stride) {
    if (n < 2) exit(1);
    for (size_t i = 0; i < n; i++)
        node_at(base, i, stride)->next = node_at(base, (i + 1) % n, stride);
}

void make_random_cycle(struct node *nodes, size_t n, uint32_t seed) {
    make_random_cycle_strided(nodes, n, sizeof(struct node), seed);
}

void make_sequential_cycle(struct node *nodes, size_t n) {
    make_sequential_cycle_strided(nodes, n, sizeof(struct node));
}

size_t *make_shuffled_indices(size_t n, uint32_t seed) {
    size_t *order = malloc(n * sizeof(*order));
    if (!order || n < 1) exit(1);
    for (size_t i = 0; i < n; i++) order[i] = i;

    uint32_t state = seed ? seed : 1u;
    for (size_t i = n - 1; i > 0; i--) {
        size_t j = (size_t)(xorshift32(&state) % (uint32_t)(i + 1));
        size_t tmp = order[i];
        order[i] = order[j];
        order[j] = tmp;
    }
    return order;
}
\end{lstlisting}

\subsubsection{Build Configuration and the \texttt{cache\_bench} Driver}
\label{sec:build}

All six experiments are compiled with one fixed baseline (Listing~\ref{lst:makefile}):
\texttt{-O0 -g -std=c11 -Wall -Wextra -fno-omit-frame-pointer}. \texttt{-O0} is deliberate,
not just conservative -- the entire method depends on the compiler emitting one genuine
load per loop iteration in \texttt{chase()} / \texttt{measure\_dependency\_chain\_batched()}
(Listings~\ref{lst:pchase},~\ref{lst:benchmark}); any optimization pass that hoisted,
vectorized, or reordered those loads would silently break the intentional dependency the
whole technique relies on. \texttt{-g}/\texttt{-fno-omit-frame-pointer} are kept so the
emitted code can be inspected in a disassembly listing as evidence that the dependency
survives compilation -- a check flagged as still outstanding on Sunbird as of this
writing (see \texttt{data\_raw/sunbird/README.md}).

\begin{lstlisting}[style=makestyle,caption={Build configuration (\texttt{Makefile}, commit \texttt{7be6dac}).},label={lst:makefile}]
CC = gcc

CFLAGS = -O0 -g -std=c11 -Wall -Wextra -fno-omit-frame-pointer
LDLIBS = -lm

TARGET = cache_bench

COMMON_DIR = main_code/common

SOURCES = \
	$(COMMON_DIR)/main.c \
	$(COMMON_DIR)/benchmark.c \
	$(COMMON_DIR)/pointer_chase.c \
	$(COMMON_DIR)/random.c \
	$(COMMON_DIR)/capacity.c \
	$(COMMON_DIR)/line_size.c \
	$(COMMON_DIR)/associativity.c \
	$(COMMON_DIR)/latency.c \
	$(COMMON_DIR)/inclusion_policy.c

OBJECTS = $(SOURCES:.c=.o)

all: $(TARGET)

$(TARGET): $(OBJECTS)
	$(CC) $(CFLAGS) -o $@ $^ $(LDLIBS)

%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

clean:
	rm -f $(TARGET) $(OBJECTS)

.PHONY: all clean
\end{lstlisting}

\texttt{cache\_bench} is a single binary; a \texttt{--experiment} flag selects which of the
six sweep configurations (each documented by its own header in
\texttt{main\_code/common/}, shown per-target in Section~\ref{sec:phase1}) to run, and every
common option (\texttt{--samples}, \texttt{--batch-size}, \texttt{--seed},
\texttt{--warmup-passes}, \texttt{--pattern}) threads through to whichever config struct is
active. Listing~\ref{lst:maindispatch} shows the final dispatch step; the full CLI argument
grammar and per-experiment defaults are in \texttt{main\_code/common/main.c} (commit
\texttt{7be6dac}) and the \texttt{Build \& Run} section of the repository \texttt{README.md}.

\begin{lstlisting}[caption={\texttt{cache\_bench} experiment dispatch, excerpted. File: \texttt{main\_code/common/main.c} (lines 475--503), commit \texttt{7be6dac}.},label={lst:maindispatch}]
    if (strcmp(experiment, "capacity") == 0) {
        return run_capacity_experiment(&cap_cfg);
    }
    if (strcmp(experiment, "line_size") == 0) {
        return run_line_size_experiment(&ls_cfg);
    }
    if (strcmp(experiment, "line_size_family") == 0) {
        return run_line_size_family_experiment(&fam_cfg);
    }
    if (strcmp(experiment, "associativity") == 0) {
        return run_associativity_experiment(&assoc_cfg);
    }
    if (strcmp(experiment, "hit_latency") == 0) {
        return run_hit_latency_experiment(&hl_cfg);
    }
    if (strcmp(experiment, "miss_latency") == 0) {
        return run_miss_latency_experiment(&ml_cfg);
    }
    if (strcmp(experiment, "inclusion_policy") == 0) {
        return run_inclusion_policy_experiment(&incl_cfg);
    }
\end{lstlisting}

\subsection{Machine Access, Core Pinning, and Phase Discipline}
\label{sec:phasediscipline}

Every \texttt{cache\_bench} invocation is pinned to a single physical core with
\texttt{taskset -c <core>} on the ECE lab machines, or \texttt{srun --cpu-bind=cores} on
the Hazel HPC cluster (Section~\ref{sec:hazel}). Before pinning, \texttt{Cpus\_allowed\_list}
and \texttt{lscpu -e=CPU,CORE,SOCKET,NODE} were checked to identify a full physical core
(both SMT siblings) actually available to the session, and \texttt{ps}/other-user activity
was checked to avoid a core another student was actively using. A single point-in-time
\texttt{ps} snapshot was found to be insufficient on at least one shared machine (Artemisia)
-- per-core load moved visibly across three consecutive 6-second sampling windows taken
seconds apart -- so idle-core selection there was instead based on \texttt{/proc/stat}
idle-time deltas sampled over multiple windows.

Long dense sweeps (working sets of $\gtrsim$100\,MiB at the random pattern) cost real wall
time, since random-pattern latency at that scale is dominated by genuine uncached DRAM
access; a full sweep at the default sample count can take on the order of an hour. Any run
in this range was launched inside a detached \texttt{tmux} session so it would survive the
driving terminal session disconnecting, with a second \texttt{tmux} session used purely to
poll for completion when needed -- a background-task watcher tied to an interactive session
does not itself survive that session ending, only a process actually backgrounded on the
machine (via \texttt{tmux}) does. Raw per-batch CSVs are gzip-compressed before being
committed (\texttt{data\_raw/<machine>/<experiment>/*.csv.gz}) to keep the repository size
tractable across eight machines' worth of million-sample sweeps.

One further methodological pitfall, confirmed on Sunbird, is worth stating explicitly: a
dense sweep started exactly at the left edge of the working-set range of interest can show
a spurious plateau caused by CPU frequency/P-state ramp-up (the first several points run
before the core reaches steady-state turbo clock), which decays over the first few dozen
points and is not a cache effect. Every dense sweep in this project accordingly starts
meaningfully below its region of actual interest rather than exactly at its boundary.

Finally, this project follows a strict three-phase discipline, stated in full in the
repository \texttt{README.md} and enforced by a frozen, tagged commit
(\texttt{phase1-timing-only}) before Phase II may begin:
\begin{itemize}
    \item \textbf{Phase I -- timing only} (this section and Section~\ref{sec:phase1}): no
    \texttt{perf}, no PMU, no cache-topology files, no vendor cache tables or published
    specifications used to \emph{derive} an inferred value -- every boundary, line size,
    associativity, and latency number reported in Section~\ref{sec:phase1} is inferred from
    timing measurements alone.
    \item \textbf{Phase II -- counter and literature verification} (Section~\ref{sec:pmu}):
    only entered after Phase I is tagged.
    \item \textbf{Phase III -- Hazel} (Section~\ref{sec:hazel}): a lab-only prediction is
    frozen and committed \emph{before} any cache experiment is run on Hazel.
\end{itemize}
Every experiment's exact build command, run command, compiler flags, core affinity, timer
method, sample count, seed, and raw-output filenames are recorded per machine in
\texttt{data\_raw/<machine>/README.md}, and every reduction step from raw CSV to processed
summary to plot is a checked-in script under \texttt{scripts/} (Section~\ref{sec:phase1}
lists the one used per target) -- together these are sufficient to reproduce every result
in Section~\ref{sec:phase1} from the raw data alone.

\section{Timing-Only Cache Reverse Engineering}
\label{sec:phase1}

Each subsection below follows the same chain: the access pattern and swept parameter
(built from the common code in Section~\ref{sec:build}) $\rightarrow$ the controlled sweep
$\rightarrow$ the measured distribution/plot $\rightarrow$ the inference rule applied to it.
The code, flowchart, and inference rule are identical across every machine (one portable
binary, Section~\ref{sec:build}), so this section states them \emph{once} and illustrates
each with Sunbird's own data as the worked example, in the spirit of PROJECT~1.pdf's
Figures 2--3. \textbf{Full per-machine results for all eight lab machines -- plots, measured
values, and the inference actually drawn on that machine -- are in
Section~\ref{sec:permachine}}, with each machine's Phase-II PMU validation in
Section~\ref{sec:pmu} and the consolidated chronological table in
Section~\ref{sec:evolution}.

\subsection{Cache Hierarchy and Capacity}
\label{sec:capacity}

\textbf{What is varied:} working-set footprint $N$, log-spaced (8 points/octave by
default) from 1\,KiB up to a coarse ceiling (64\,MiB by default, extended manually to
1\,GiB when the topmost region has not yet plateaued). \textbf{What is held constant:}
node size, batch size, seed, warm-up passes, core pinning. \textbf{What is measured:}
median ticks/access per footprint, at both the random and sequential access patterns.
Listing~\ref{lst:capacity} is the complete implementation: for each swept size it builds a
fresh dependent cycle sized to exactly fill that footprint, runs the required warm-up
passes untimed, then times \texttt{num\_batches} batches via
\texttt{measure\_dependency\_chain\_batched()} (Listing~\ref{lst:benchmark}) and prints one
raw CSV row per (size, batch).

\begin{lstlisting}[caption={Cache-level/capacity discovery. Target property: cache-level boundaries (L1/L2/LLC/DRAM). Architecture: portable common code (compiled for both x86-64 and AArch64 via Listing~\ref{lst:timer}). File: \texttt{main\_code/common/capacity.c}, commit \texttt{8fb1467}.},label={lst:capacity}]
int run_capacity_experiment(const struct capacity_config *cfg)
{
    uint64_t min_bytes = cfg->min_bytes;
    if (min_bytes < sizeof(struct node) * 2) {
        min_bytes = sizeof(struct node) * 2;
    }

    uint64_t num_batches = (cfg->samples + cfg->batch_size - 1) / cfg->batch_size;
    if (num_batches < 1) {
        num_batches = 1;
    }
    uint64_t achieved_samples = num_batches * cfg->batch_size;

    double *batch_latencies = malloc(num_batches * sizeof(double));
    if (batch_latencies == NULL) {
        fprintf(stderr, "allocation failed for %" PRIu64 " batch samples\n", num_batches);
        return 1;
    }

    const char *pattern_name =
        (cfg->pattern == ACCESS_PATTERN_SEQUENTIAL) ? "sequential" : "random";

    printf("size_bytes,num_nodes,pattern,batch_index,avg_ticks_per_access\n");

    size_t last_num_nodes = 0;
    double step = 1.0 / (double)cfg->points_per_octave;
    double log2_min = log2((double)min_bytes);
    double log2_max = log2((double)cfg->max_bytes);

    for (double exp = log2_min; exp <= log2_max + 1e-9; exp += step) {
        uint64_t bytes = (uint64_t)(pow(2.0, exp) + 0.5);
        size_t num_nodes = (size_t)(bytes / sizeof(struct node));
        if (num_nodes < 2) num_nodes = 2;
        if (num_nodes == last_num_nodes) continue;
        last_num_nodes = num_nodes;

        struct node *nodes = malloc(num_nodes * sizeof(struct node));
        if (nodes == NULL) { free(batch_latencies); return 1; }

        if (cfg->pattern == ACCESS_PATTERN_SEQUENTIAL) {
            make_sequential_cycle(nodes, num_nodes);
        } else {
            make_random_cycle(nodes, num_nodes, cfg->seed);
        }

        /* Untimed pass(es): fault in pages and settle steady-state
         * residency for this working-set size before timing starts. */
        for (int w = 0; w < cfg->warmup_passes; w++) {
            chase(nodes, (uint64_t)num_nodes);
        }

        measure_dependency_chain_batched(nodes, cfg->batch_size, num_batches,
                                          batch_latencies);

        uint64_t size_bytes = (uint64_t)num_nodes * sizeof(struct node);
        for (uint64_t b = 0; b < num_batches; b++) {
            printf("%" PRIu64 ",%zu,%s,%" PRIu64 ",%.4f\n",
                   size_bytes, num_nodes, pattern_name, b, batch_latencies[b]);
        }
        fflush(stdout);
        free(nodes);
    }
    free(batch_latencies);
    return 0;
}
\end{lstlisting}

Raw per-batch CSVs are reduced by \texttt{scripts/summarize\_raw.py} into one
distribution-stats row per (size, pattern), then \texttt{scripts/detect\_cache\_hierarchy.py}
scans consecutive size points for a sustained tick jump exceeding both an absolute and a
relative threshold to flag a boundary, and \texttt{scripts/plot\_capacity.py} renders the
curve. \texttt{scripts/run\_capacity\_full.sh <machine> <core>} chains coarse sweep
$\rightarrow$ automatic boundary detection $\rightarrow$ per-boundary dense sweep
$\rightarrow$ reproducibility repeats $\rightarrow$ tail-extension sweep $\rightarrow$
plots into one command.

\begin{figure}[htbp]
\centering
\fbox{\parbox[c][2.2in][c]{0.85\textwidth}{\centering
PLACEHOLDER -- methodology-logic flow chart for capacity/cache-level discovery:\\[4pt]
sweep footprint $N$ (log$_2$-spaced) $\rightarrow$ build dependent cycle of $N$ bytes,
random \emph{and} sequential $\rightarrow$ batched avg.\ ticks/access $\rightarrow$
compare consecutive size points $\rightarrow$ sustained jump exceeding threshold?
$\rightarrow$ \textbf{yes}: record a cache-level boundary; \textbf{no}: continue sweep.
}}
\caption{Methodology logic for cache-level/capacity discovery. Measured quantity: median
ticks/access (y) vs.\ working-set footprint in bytes (x, log scale). Expected signature: a
sustained step in the random-pattern curve, absent (or at a different location) in the
sequential-pattern control. Inference rule: the smallest footprint where a step
exceeding both the absolute and relative thresholds is reproduced across repeats marks a
cache-level boundary.}
\label{fig:capacity-flow}
\end{figure}

\begin{figure}[htbp]
\centering
 \includegraphics[width=\textwidth]{sunbird_capacity.png}

\caption{Capacity sweep, Sunbird (2x Intel Xeon E5-2680 v3, Haswell), core pinned via
\texttt{taskset}, 1{,}000{,}000 timed accesses/point, 8 points/octave, 1\,KiB--1\,GiB.
Units: ticks/access (TSC cycles, y-axis) vs.\ bytes (x-axis, log scale). \textbf{Boundary
values shown here are illustrative of the method only; the authoritative, hand-reconciled
per-machine L1/L2/LLC boundary values used in every downstream experiment are in
\texttt{CAPACITY\_RESULTS.md}}, not this plot in isolation. Interpretation: three
flat/ramp transitions are visible across the swept range, each confirmed via an
independent repeat run and cross-checked against the sequential-pattern control. The
same plot for every other lab machine is in Section~\ref{sec:permachine}.}
\label{fig:capacity-plot}
\end{figure}

\textbf{Inference rule:} a reproducible, sustained step in median ticks/access at a
working-set size in the random-pattern curve -- confirmed not to be a frequency-ramp or
session-contention artifact (Section~\ref{sec:phasediscipline}) and not present at the same
location in the sequential-pattern control -- marks the boundary between one cache level's
occupancy and the next.

\textbf{Phase-II validation:} L1D and L2 capacity matched the PMU/system-reported value
exactly on all 8 machines; LLC capacity matched on 5 of 8 (Crux, Skylark, and Artemisia's
Phase-I estimates were low by 1.5x, 2x, and 1.75x respectively -- see
Section~\ref{sec:pmu} and Table~\ref{tab:phase2-summary}). Several machines'
automatically detected boundaries required a dense manual follow-up sweep to confirm or
retract (documented per machine in \texttt{data\_raw/<machine>/README.md}); the
consolidated, hand-verified result for every machine is \texttt{CAPACITY\_RESULTS.md}, and
the full per-machine plots and numbers are in Section~\ref{sec:permachine}.

\subsection{Cache Line Size}
\label{sec:linesize}

\textbf{What is varied:} either the byte stride between nodes at a fixed footprint
(single-curve method), or the working-set footprint at one fixed candidate stride,
repeated once per candidate stride (family-of-curves method, PROJECT 1.pdf Figure 3 /
"Example B"). \textbf{What is held constant:} in each method, the parameter not being
swept, plus buffer base alignment (recorded, not swept, in the single-curve method; also
swept as a sub-line \texttt{offset\_bytes} cross-check in the family method).
\textbf{What is measured:} median ticks/access at each stride or footprint point, random
and sequential. Listing~\ref{lst:linesize} implements both methods in one file.

\begin{lstlisting}[caption={Cache line size, both independent methods. Target property: cache line size. Architecture: portable common code. File: \texttt{main\_code/common/line\_size.c}, commit \texttt{1689b22}.},label={lst:linesize}]
/* ---- single-curve method: fixed footprint, stride swept ---- */
int run_line_size_experiment(const struct line_size_config *cfg)
{
    uint64_t min_stride = cfg->min_stride;
    if (min_stride < sizeof(struct node)) min_stride = sizeof(struct node);

    uint64_t num_batches = (cfg->samples + cfg->batch_size - 1) / cfg->batch_size;
    if (num_batches < 1) num_batches = 1;

    double *batch_latencies = malloc(num_batches * sizeof(double));
    if (batch_latencies == NULL) return 1;

    const char *pattern_name =
        (cfg->pattern == ACCESS_PATTERN_SEQUENTIAL) ? "sequential" : "random";

    printf("stride_bytes,num_nodes,footprint_bytes,align_bytes,pattern,batch_index,avg_ticks_per_access\n");

    for (uint64_t stride = min_stride; stride <= cfg->max_stride; stride += cfg->stride_step) {
        size_t num_nodes = (size_t)(cfg->footprint_bytes / stride);
        if (num_nodes < 2) { continue; } /* stride too large for this footprint */

        uint64_t buffer_bytes = (uint64_t)(num_nodes - 1) * stride + sizeof(struct node);
        void *base = NULL;
        if (posix_memalign(&base, (size_t)cfg->align_bytes, (size_t)buffer_bytes) != 0) {
            free(batch_latencies); return 1;
        }

        if (cfg->pattern == ACCESS_PATTERN_SEQUENTIAL) {
            make_sequential_cycle_strided(base, num_nodes, (size_t)stride);
        } else {
            make_random_cycle_strided(base, num_nodes, (size_t)stride, cfg->seed);
        }

        for (int w = 0; w < cfg->warmup_passes; w++) chase(base, (uint64_t)num_nodes);
        measure_dependency_chain_batched(base, cfg->batch_size, num_batches, batch_latencies);

        for (uint64_t b = 0; b < num_batches; b++) {
            printf("%" PRIu64 ",%zu,%" PRIu64 ",%" PRIu64 ",%s,%" PRIu64 ",%.4f\n",
                   stride, num_nodes, cfg->footprint_bytes, cfg->align_bytes,
                   pattern_name, b, batch_latencies[b]);
        }
        fflush(stdout);
        free(base);
    }
    free(batch_latencies);
    return 0;
}

/* ---- family-of-curves method: fixed stride, footprint swept ----
 * Curves for stride <= true line size coincide (multiple nodes legitimately
 * share a line, so pressure depends only on total footprint); curves for
 * stride > true line size separate from that shared baseline, since each
 * node now occupies its own line. The smallest stride whose curve visibly
 * separates is the line-size estimate. See run_line_size_family_experiment()
 * in main_code/common/line_size.c for the full sweep loop (identical shape
 * to run_capacity_experiment() in Listing 8, at an arbitrary node stride
 * instead of sizeof(struct node), with an added offset_bytes cross-check). */
\end{lstlisting}

\texttt{scripts/run\_line\_size.sh <machine> <core> <boundaries\_csv>} runs the single-curve
sweep and the family-of-curves sweep (once per candidate stride, at least two
\texttt{offset\_bytes} values) per cache-level footprint taken from that machine's own
\texttt{CAPACITY\_RESULTS.md} boundary, then reports whether the two independently-designed
methods agree.

\begin{figure}[htbp]
\centering
\fbox{\parbox[c][2.0in][c]{0.85\textwidth}{\centering
PLACEHOLDER -- methodology-logic flow chart for line-size inference (both methods
side by side): \\[4pt]
\emph{Single-curve:} fix footprint just above a capacity boundary $\rightarrow$ sweep
stride linearly $\rightarrow$ ticks/access ramps then saturates $\rightarrow$ saturation
stride = line size.\\
\emph{Family-of-curves:} fix candidate stride $s$ $\rightarrow$ sweep footprint
$\rightarrow$ overlay curves for several $s$ $\rightarrow$ smallest $s$ whose curve
separates from the shared baseline = line size.
}}
\caption{Methodology logic for cache line-size inference. Expected signature: a
ramp-to-plateau elbow (single-curve) or a baseline-separation point (family-of-curves).
Inference rule: smallest stride at which the tested signature first appears.}
\label{fig:linesize-flow}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.70\textwidth]{sunbird_linesize.png}
\caption{Line-size sweep, Sunbird L1 (32{,}768\,B footprint), family-of-curves method.
Interpretation: both independently-designed methods agree at 64\,B for this level,
confirmed with an offset cross-check. The same plot for every other lab machine's L1,
L2, and LLC footprints is in Section~\ref{sec:permachine}.}
\label{fig:linesize-plot}
\end{figure}

\textbf{Inference rule:} single-curve -- the smallest stride at which ticks/access stops
climbing (saturates), since a larger stride beyond the true line size no longer changes how
many distinct lines are touched; family-of-curves -- the smallest candidate stride whose
footprint-vs-latency curve visibly separates from the shared low-stride baseline.

\textbf{Phase-II validation:} agreement between the two independently-designed methods
(and, for the family method, across independently repeated \texttt{offset\_bytes} values)
is the internal cross-check for this target; the 64\,B line size held on every lab machine
at every level where a citable value was ever produced, and agrees exactly with
system-reported/literature values everywhere PMU verification was possible
(Section~\ref{sec:pmu}), the single most consistent measurement in the whole project. The
one genuine exception is Skylark, whose LLC-region transition measures 128\,B by both
methods -- a real per-level line-size split, not a methodology gap (Section~\ref{sec:permachine}).

\subsection{Cache Associativity}
\label{sec:assoc}

\textbf{What is varied:} the number of same-set nodes chased in one dependent cycle
(\texttt{num\_ways}), from \texttt{min\_ways} to \texttt{max\_ways}. \textbf{What is held
constant:} the node-to-node stride, fixed at \texttt{cache\_bytes} -- the target cache
level's own confirmed capacity from \texttt{CAPACITY\_RESULTS.md} -- so every probed node
lands in the same cache set by construction, but carries a distinct tag.
\textbf{What is measured:} median ticks/access per \texttt{num\_ways}.

\begin{lstlisting}[caption={Cache associativity (way count per set), including the huge-page DTLB-control diagnostic. Target property: associativity. Architecture: portable common code. File: \texttt{main\_code/common/associativity.c}, commit \texttt{22104d6}.},label={lst:assoc}]
int run_associativity_experiment(const struct associativity_config *cfg)
{
    uint64_t min_ways = cfg->min_ways < 2 ? 2 : cfg->min_ways;
    uint64_t num_batches = (cfg->samples + cfg->batch_size - 1) / cfg->batch_size;
    if (num_batches < 1) num_batches = 1;

    double *batch_latencies = malloc(num_batches * sizeof(double));
    if (batch_latencies == NULL) return 1;

    const char *pattern_name =
        (cfg->pattern == ACCESS_PATTERN_SEQUENTIAL) ? "sequential" : "random";

    printf("num_ways_probed,cache_bytes,pattern,batch_index,avg_ticks_per_access\n");

    for (uint64_t num_ways = min_ways; num_ways <= cfg->max_ways; num_ways += cfg->way_step) {
        /* Every node is cfg->cache_bytes apart, so the touched address range
         * spans (num_ways - 1) strides plus one node. */
        uint64_t buffer_bytes = (num_ways - 1) * cfg->cache_bytes + sizeof(struct node);

        void *base = NULL;
        if (cfg->huge_pages) {
            /* 2 MiB-align via mmap(), madvise(MADV_HUGEPAGE), fault in every
             * 4 KiB page -- collapses the whole probe buffer onto a SINGLE
             * TLB entry regardless of num_ways, directly testing whether an
             * observed knee is a TLB/page-boundary artifact rather than a
             * real cache-set limit. Actual backing achieved is confirmed
             * from /proc/self/smaps's AnonHugePages field, not assumed from
             * a successful madvise() call. See report_huge_page_backing()
             * in main_code/common/associativity.c for the full diagnostic. */
            base = alloc_huge_page_aligned(buffer_bytes); /* see full source */
        } else {
            base = malloc((size_t)buffer_bytes);
        }
        if (base == NULL) { free(batch_latencies); return 1; }

        if (cfg->pattern == ACCESS_PATTERN_SEQUENTIAL) {
            make_sequential_cycle_strided(base, (size_t)num_ways, (size_t)cfg->cache_bytes);
        } else {
            make_random_cycle_strided(base, (size_t)num_ways, (size_t)cfg->cache_bytes, cfg->seed);
        }

        for (int w = 0; w < cfg->warmup_passes; w++) chase(base, num_ways);
        measure_dependency_chain_batched(base, cfg->batch_size, num_batches, batch_latencies);

        for (uint64_t b = 0; b < num_batches; b++) {
            printf("%" PRIu64 ",%" PRIu64 ",%s,%" PRIu64 ",%.4f\n",
                   num_ways, cfg->cache_bytes, pattern_name, b, batch_latencies[b]);
        }
        fflush(stdout);
        free_probe_buffer(base, cfg->huge_pages);
    }
    free(batch_latencies);
    return 0;
}
\end{lstlisting}

\begin{figure}[htbp]
\centering
\fbox{\parbox[c][2.0in][c]{0.85\textwidth}{\centering
PLACEHOLDER -- methodology-logic flow chart for associativity:\\[4pt]
fix stride = level's own confirmed capacity (every probed node $\rightarrow$ same set,
distinct tag) $\rightarrow$ sweep num\_ways $\rightarrow$ flat latency while
num\_ways $\le$ true associativity $\rightarrow$ sharp jump once exceeded (LRU/PLRU set
thrashes) $\rightarrow$ knee = associativity.
}}
\caption{Methodology logic for associativity inference. Expected signature: a flat
hit-latency shelf followed by a sharp, sustained jump at \texttt{num\_ways = A+1}.
Inference rule: reported associativity $A$ = the last \texttt{num\_ways} on the flat
shelf before the jump.}
\label{fig:assoc-flow}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{sunbird_assoc_L1.png}
\caption{Associativity sweep, Sunbird L1. Units: ticks/access (y) vs.\ num\_ways probed
(x). Interpretation: flat $\approx$6.0--6.2 ticks/access through \texttt{num\_ways=8},
sharp jump at 9, 0\% run-to-run spread across 3 seeded repeats $\rightarrow$ L1 = 8-way.
The same L1/L2/LLC plots for every other lab machine are in Section~\ref{sec:permachine}.}
\label{fig:assoc-plot}
\end{figure}

\textbf{Inference rule:} $A = $ \texttt{num\_ways} at the last flat point immediately
before the first sustained latency jump.

\textbf{Phase-II validation and an honest negative result:} L1 associativity is confirmed
cleanly this way on every machine attempted -- 8-way on three x86 machines spanning two
vendors/generations (Sunbird, Upgrade, Charnwood), 4-way on Thunderbird (AArch64,
Neoverse~N1), and a less-certain 12-way on Artemisia's larger 48\,KiB L1D. \textbf{L2 and
LLC associativity are not resolved by this method.} On every one of five machines tried,
byte-capacity candidates spanning up to a 32x range at the same nominal cache level break
at the \emph{same} small \texttt{num\_ways} (typically 9--12) regardless of the actual
byte capacity tested, and in two cases (Charnwood, Artemisia) the L2 and LLC curves are
numerically indistinguishable at every \texttt{num\_ways} despite a 32x--15x difference in
candidate capacity -- direct, on-machine evidence that a shared, capacity-independent
structure (leading hypothesis: DTLB aliasing, since every probed node lives on its own
4\,KiB page; a sliced-LLC hash function is a second, not-yet-ruled-out candidate) is what
these attempts actually measure, not the real cache level. The \texttt{--huge-pages}
control shown in Listing~\ref{lst:assoc} exists specifically to test the DTLB hypothesis by
collapsing the whole probe buffer onto one TLB entry, but has not yet been exercised on any
machine -- flagged here as the concrete next step, not attempted this cycle. A
derived-stride-scan technique (\texttt{scripts/run\_associativity\_stride\_scan.sh}) shows
more promise (self-consistent across several stride divisors on Upgrade's known-good L1)
but remains inconclusive at L2/LLC. No L2 or LLC associativity number quoted in this
section (or in any machine's own \texttt{data\_processed/<machine>/FINAL\_CACHE\_TABLE.md}
best guess) should be read as machine-confirmed by timing alone; Section~\ref{sec:pmu}'s
PMU verification directly quantifies how often the confound-blocked best guess turned out
right once ground truth was available (L1: 8/8; L2: 3/8; LLC: 1/8) -- a concrete measure of
the confound's real cost, not just a caveat. See \texttt{CAPACITY\_RESULTS.md} and each
machine's own README for the full negative-result writeup.

\subsection{Cache Hit Latency}
\label{sec:hitlatency}

\textbf{What is varied:} load mode only (\texttt{dependent} vs.\ \texttt{independent}) at
one fixed footprint. \textbf{What is held constant:} the footprint itself, which must
already be confirmed (via Section~\ref{sec:capacity}'s boundaries) to sit entirely inside
one cache level -- this experiment infers nothing about capacity itself.
\textbf{What is measured:} median ticks/access, both load modes, both patterns.
Listing~\ref{lst:latency} implements this together with miss/next-level latency
(Section~\ref{sec:misslatency}), since both live in one file and are shown once here per
the reused-code convention.

\begin{lstlisting}[caption={Cache hit latency (this listing) and miss/next-level latency (Section~\ref{sec:misslatency} reuses this same file). Target properties: hit latency; miss/next-level latency. Architecture: portable common code. File: \texttt{main\_code/common/latency.c}, commit \texttt{e343732}.},label={lst:latency}]
int run_hit_latency_experiment(const struct hit_latency_config *cfg)
{
    size_t num_nodes = (size_t)(cfg->footprint_bytes / sizeof(struct node));
    if (num_nodes < 2) num_nodes = 2;

    uint64_t num_batches = (cfg->samples + cfg->batch_size - 1) / cfg->batch_size;
    if (num_batches < 1) num_batches = 1;

    double *batch_latencies = malloc(num_batches * sizeof(double));
    struct node *nodes = malloc(num_nodes * sizeof(struct node));
    if (batch_latencies == NULL || nodes == NULL) return 1;

    const char *pattern_name =
        (cfg->pattern == ACCESS_PATTERN_SEQUENTIAL) ? "sequential" : "random";
    const char *mode_name =
        (cfg->load_mode == LOAD_MODE_INDEPENDENT) ? "independent" : "dependent";

    if (cfg->pattern == ACCESS_PATTERN_SEQUENTIAL) {
        make_sequential_cycle(nodes, num_nodes);
    } else {
        make_random_cycle(nodes, num_nodes, cfg->seed);
    }
    /* Warm-up always via the dependent chain, regardless of the timed mode,
     * so both modes start from the same warmed-up residency. */
    for (int w = 0; w < cfg->warmup_passes; w++) chase(nodes, (uint64_t)num_nodes);

    printf("footprint_bytes,load_mode,pattern,batch_index,avg_ticks_per_access\n");

    if (cfg->load_mode == LOAD_MODE_INDEPENDENT) {
        size_t *order = (cfg->pattern == ACCESS_PATTERN_SEQUENTIAL)
            ? NULL /* identity order, built inline -- see full source */
            : make_shuffled_indices(num_nodes, cfg->seed);
        measure_independent_loads_batched(nodes, order, num_nodes, cfg->batch_size,
                                           num_batches, batch_latencies);
    } else {
        measure_dependency_chain_batched(nodes, cfg->batch_size, num_batches, batch_latencies);
    }

    for (uint64_t b = 0; b < num_batches; b++) {
        printf("%" PRIu64 ",%s,%s,%" PRIu64 ",%.4f\n",
               cfg->footprint_bytes, mode_name, pattern_name, b, batch_latencies[b]);
    }
    fflush(stdout);
    free(nodes); free(batch_latencies);
    return 0;
}

int run_miss_latency_experiment(const struct miss_latency_config *cfg)
{
    size_t target_nodes = (size_t)(cfg->target_bytes / sizeof(struct node));
    size_t evict_nodes = (size_t)(cfg->evict_bytes / sizeof(struct node));
    if (target_nodes < 2) target_nodes = 2;
    if (evict_nodes < 2) evict_nodes = 2;

    struct node *target = malloc(target_nodes * sizeof(struct node));
    struct node *evict = malloc(evict_nodes * sizeof(struct node));
    if (target == NULL || evict == NULL) return 1;

    const char *pattern_name =
        (cfg->pattern == ACCESS_PATTERN_SEQUENTIAL) ? "sequential" : "random";

    if (cfg->pattern == ACCESS_PATTERN_SEQUENTIAL) {
        make_sequential_cycle(target, target_nodes);
        make_sequential_cycle(evict, evict_nodes);
    } else {
        make_random_cycle(target, target_nodes, cfg->seed);
        make_random_cycle(evict, evict_nodes, cfg->seed + 1u);
    }
    for (int w = 0; w < cfg->warmup_passes; w++) {
        chase(target, (uint64_t)target_nodes);
        chase(evict, (uint64_t)evict_nodes);
    }

    printf("target_bytes,evict_bytes,pattern,trial_index,ticks\n");

    for (uint64_t trial = 0; trial < cfg->samples; trial++) {
        /* Untimed: re-establish target residency, then displace it by
         * walking the eviction-set cycle (evict_bytes must exceed the
         * source level's real capacity -- this is the ONLY eviction
         * mechanism in this codebase; there is no clflush anywhere). */
        chase(target, (uint64_t)target_nodes);
        chase(evict, (uint64_t)evict_nodes);

        /* Timed: exactly one dependent reload of the target line. */
        uint64_t t0 = timer_start();
        struct node *reloaded = target->next;
        uint64_t t1 = timer_stop();
        (void)reloaded;

        printf("%" PRIu64 ",%" PRIu64 ",%s,%" PRIu64 ",%" PRIu64 "\n",
               cfg->target_bytes, cfg->evict_bytes, pattern_name, trial,
               (uint64_t)(t1 - t0));
    }
    fflush(stdout);
    free(evict); free(target);
    return 0;
}
\end{lstlisting}

\begin{figure}[htbp]
\centering
\fbox{\parbox[c][1.8in][c]{0.85\textwidth}{\centering
PLACEHOLDER -- methodology-logic flow chart for hit latency:\\[4pt]
fix footprint inside one confirmed level $\rightarrow$ run BOTH dependent and
independent-load batched timers $\rightarrow$ independent reads faster (MLP exposed,
confirms the dependent timer is genuinely serializing) $\rightarrow$ report the
\emph{dependent}, random-pattern median as that level's hit latency.
}}
\caption{Methodology logic for hit-latency inference. Inference rule: dependent/random
median at a confirmed single-level footprint is the reported hit latency, valid only if
paired independent-load control reads faster at the same footprint.}
\label{fig:hitlat-flow}
\end{figure}

\begin{figure}[H]
    \centering

    % Top row
    \includegraphics[width=0.48\linewidth]{sunbird_hitlat_L1.png}
    \hfill
    \includegraphics[width=0.48\linewidth]{sunbird_hitlat_L2.png}

    \par\medskip

    % Bottom row
    \includegraphics[width=0.48\linewidth]{sunbird_hitlat_LLC.png}
    \hfill
    \includegraphics[width=0.48\linewidth]{sunbird_hitlat_DRAM.png}

    \caption{Hit-latency sweep, Sunbird, footprints
    32{,}768 / 262{,}144 / 31{,}457{,}280 / 536{,}870{,}912\,B
    (L1/L2/LLC/DRAM per \texttt{CAPACITY\_RESULTS.md}).
    Units: ticks/access (TSC cycles). Interpretation: a clean,
    monotonic four-tier ladder
    (L1 $\approx 10.4$, L2 $\approx 26.8$,
    LLC $\approx 58.4$, DRAM $\approx 207$ ticks;
    dependent, random, median). The independent-load control reads
    faster than the dependent-load measurement at every level and
    pattern, confirming that MLP was exposed rather than hidden. The
    same four-panel plot for every other lab machine is in
    Section~\ref{sec:permachine}.}
    \label{fig:hitlat-plot}
\end{figure}

\subsection{Miss and Next-Level Latency}
\label{sec:misslatency}

\textbf{What is varied:} nothing within a trial -- each trial is a single timed reload;
what varies \emph{across} the sweep is which source/next-level transition is being
measured (L1$\to$L2, L2$\to$LLC, LLC$\to$DRAM), each a separate invocation.
\textbf{What is held constant:} \texttt{target\_bytes} (places the target inside the SOURCE
level) and \texttt{evict\_bytes} (an eviction-set walk sized past the source level's real
capacity but inside the next level's, per \texttt{CAPACITY\_RESULTS.md}).
\textbf{What is measured:} single-shot reload ticks per trial (not batched, since the
measurement is inherently one dependent load). The complete implementation,
\texttt{run\_miss\_latency\_experiment()}, is shown in full in Listing~\ref{lst:latency}
above (Section~\ref{sec:hitlatency}) alongside the hit-latency implementation, since both
share one source file, \texttt{main\_code/common/latency.c} (commit \texttt{e343732}); it
is not repeated here.

\begin{figure}[htbp]
\centering
\fbox{\parbox[c][1.8in][c]{0.85\textwidth}{\centering
PLACEHOLDER -- methodology-logic flow chart for miss/next-level latency:\\[4pt]
untimed re-touch of target (source-level residency restored) $\rightarrow$ untimed
eviction walk (size $>$ source capacity, $<$ next-level capacity) $\rightarrow$ ONE timed
dependent reload $\rightarrow$ record ticks (single-shot, per trial).
}}
\caption{Methodology logic for miss/next-level-latency inference. Inference rule: raw
single-shot reload latency at a transition = true reload latency + a fixed, per-machine
single-shot measurement overhead (isolated separately via a deliberately
non-colliding control eviction set, Section~\ref{sec:misslatency}).}
\label{fig:misslat-flow}
\end{figure}

\begin{figure}[H]
    \centering

    % Top-left image
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{sunbird_misslat_L1_to_L2.png}
        \caption{L1 to L2 latency}
        \label{fig:misslat-a}
    \end{subfigure}
    \hfill
    % Top-right image
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{sunbird_misslat_L2_to_LLC.png}
        \caption{L2 to LLC latency}
        \label{fig:misslat-b}
    \end{subfigure}

    \vspace{0.5cm}

        \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{sunbird_misslat_LLC_to_DRAM.png}
        \caption{LLC to DRAM latency}
        \label{fig:misslat-c}
    \end{subfigure}

    \caption{Miss/next-level latency sweep, Sunbird, three transitions
    (L1$\to$L2 / L2$\to$LLC / LLC$\to$DRAM). Units: ticks (single-shot reload).
    Interpretation: an increasing 124/284/622-tick sequence (random pattern,
    median) -- consistent ordering with the hit-latency ladder above, but see
    the single-shot overhead caveat below before treating these as clean
    incremental miss penalties. The same three-panel plot for every other lab
    machine is in Section~\ref{sec:permachine}.}
    \label{fig:misslat-plot}
\end{figure}

\textbf{Known limitation (stated directly in the source, quoted here rather than
smoothed over):} single-shot timing carries measurement overhead -- timer serialization
and post-\texttt{chase()} pipeline state cost far more when timed once than when amortized
across the $\ge$1000-access batches every other experiment uses (Section~\ref{sec:batching})
-- so a raw miss-latency median is \emph{not} directly comparable to a batched hit-latency
plateau (Section~\ref{sec:hitlatency}) without first subtracting this fixed, per-machine
overhead, which was isolated on Sunbird ($\approx$64--85 ticks) and Skylark
($\approx$65.8 ticks) via a deliberately-too-small eviction-set control but is not yet
subtracted automatically by this codebase.

\textbf{Phase-II validation:} the PMU verification pass (Section~\ref{sec:pmu}) targeted
cache boundaries and associativity, not the miss-latency overhead specifically -- a
dedicated PMU cycle-accurate cross-check that would directly resolve how much of the raw
single-shot number is fixed overhead versus real reload latency remains a concrete next
step, not performed this cycle. The hit-latency ladder itself (Section~\ref{sec:hitlatency})
was PMU-corroborated indirectly via miss-rate jumps at each capacity boundary.

\subsection{Inclusion and Exclusion Behavior}
\label{sec:inclusion}

\textbf{What is varied:} nothing within a trial; the sweep is across level \emph{pairings}
(e.g.\ L1 vs.\ L2, L1 vs.\ LLC, L2 vs.\ LLC). \textbf{What is held constant:}
\texttt{target\_bytes} (a small, densely-packed set confirmed inside the UPPER level) and
an untouched \texttt{control} line at the identical sub-page offset (0); the eviction walk
uses a page-multiple stride (\texttt{evict\_stride\_bytes}, a multiple of 4096) so every
eviction node shares one fixed sub-page offset (\texttt{evict\_offset\_bytes}, default
2048) different from the target/control's own offset -- this is what lets the walk
structurally avoid the target's own upper-level set while still spanning enough distinct
pages to pressure the bigger LOWER level. \textbf{What is measured:} per-trial single-shot
reload latency for both the target and the control channel.

\begin{lstlisting}[caption={Inclusion/exclusion behavior. Target property: inclusion policy between an upper (inner) and lower (outer) cache level. Architecture: portable common code. File: \texttt{main\_code/common/inclusion\_policy.c}, commit \texttt{7be6dac}.},label={lst:inclusion}]
int run_inclusion_policy_experiment(const struct inclusion_policy_config *cfg)
{
    size_t target_nodes = (size_t)(cfg->target_bytes / sizeof(struct node));
    size_t evict_nodes = (size_t)(cfg->evict_bytes / cfg->evict_stride_bytes);
    if (target_nodes < 2) target_nodes = 2;
    if (evict_nodes < 2) evict_nodes = 2;

    /* Target and control: densely packed, page-aligned (offset 0), two
     * independent buffers so control's residency can never be disturbed
     * by anything done to target's own memory. */
    struct node *target = NULL, *control = NULL;
    posix_memalign((void **)&target, 4096u, target_nodes * sizeof(struct node));
    posix_memalign((void **)&control, 4096u, target_nodes * sizeof(struct node));
    if (target == NULL || control == NULL) return 1;

    /* Eviction buffer: one node every evict_stride_bytes (a page multiple),
     * offset evict_offset_bytes past a fresh page-aligned allocation, so
     * every eviction node shares a fixed sub-page byte position different
     * from target/control's own (0) -- structurally avoids the target's own
     * set PROVIDED the upper level's index fits within one page (true for
     * a typical L1; not for an L2-or-bigger target). */
    uint64_t evict_buffer_bytes =
        (uint64_t)(evict_nodes - 1) * cfg->evict_stride_bytes + sizeof(struct node);
    uint64_t evict_alloc_bytes = evict_buffer_bytes + cfg->evict_offset_bytes;
    void *evict_alloc = NULL;
    posix_memalign(&evict_alloc, 4096u, (size_t)evict_alloc_bytes);
    if (evict_alloc == NULL) { free(control); free(target); return 1; }
    void *evict_base = (void *)((uint8_t *)evict_alloc + cfg->evict_offset_bytes);

    const char *pattern_name =
        (cfg->pattern == ACCESS_PATTERN_SEQUENTIAL) ? "sequential" : "random";

    if (cfg->pattern == ACCESS_PATTERN_SEQUENTIAL) {
        make_sequential_cycle(target, target_nodes);
        make_sequential_cycle(control, target_nodes);
        make_sequential_cycle_strided(evict_base, evict_nodes, (size_t)cfg->evict_stride_bytes);
    } else {
        make_random_cycle(target, target_nodes, cfg->seed);
        make_random_cycle(control, target_nodes, cfg->seed + 1u);
        make_random_cycle_strided(evict_base, evict_nodes, (size_t)cfg->evict_stride_bytes,
                                   cfg->seed + 2u);
    }
    for (int w = 0; w < cfg->warmup_passes; w++) {
        chase(target, (uint64_t)target_nodes);
        chase(control, (uint64_t)target_nodes);
        chase(evict_base, (uint64_t)evict_nodes);
    }

    printf("target_bytes,evict_bytes,evict_stride_bytes,evict_offset_bytes,pattern,channel,trial_index,ticks\n");

    for (uint64_t trial = 0; trial < cfg->samples; trial++) {
        /* Untimed: refresh target/control residency, displace via the
         * page-offset eviction walk (only eviction mechanism -- no clflush). */
        chase(target, (uint64_t)target_nodes);
        chase(control, (uint64_t)target_nodes);
        chase(evict_base, (uint64_t)evict_nodes);

        /* Timed: exactly one dependent reload of each. */
        uint64_t t0 = timer_start();
        struct node *r_target = target->next;
        uint64_t t1 = timer_stop();
        uint64_t t2 = timer_start();
        struct node *r_control = control->next;
        uint64_t t3 = timer_stop();
        (void)r_target; (void)r_control;

        printf("%" PRIu64 ",%" PRIu64 ",%" PRIu64 ",%" PRIu64 ",%s,target,%" PRIu64 ",%" PRIu64 "\n",
               cfg->target_bytes, cfg->evict_bytes, cfg->evict_stride_bytes,
               cfg->evict_offset_bytes, pattern_name, trial, (uint64_t)(t1 - t0));
        printf("%" PRIu64 ",%" PRIu64 ",%" PRIu64 ",%" PRIu64 ",%s,control,%" PRIu64 ",%" PRIu64 "\n",
               cfg->target_bytes, cfg->evict_bytes, cfg->evict_stride_bytes,
               cfg->evict_offset_bytes, pattern_name, trial, (uint64_t)(t3 - t2));
    }
    fflush(stdout);
    free(evict_alloc); free(control); free(target);
    return 0;
}
\end{lstlisting}

\begin{figure}[htbp]
\centering
\fbox{\parbox[c][2.1in][c]{0.85\textwidth}{\centering
PLACEHOLDER -- methodology-logic flow chart for inclusion/exclusion:\\[4pt]
touch target \& control (upper level) $\rightarrow$ page-offset eviction walk (lower
level, structurally avoids target's own set) $\rightarrow$ reload target \& control
$\rightarrow$ classify each vs.\ calibrated survived/invalidated latency classes
(Section~\ref{sec:hitlatency}/\ref{sec:misslatency}) $\rightarrow$ target invalidated
\& control survived $\Rightarrow$ inclusive; both survived $\Rightarrow$
exclusive/non-inclusive.
}}
\caption{Methodology logic for inclusion/exclusion inference. Inference rule: $\ge$80\%
of target trials classified invalidated-like $\Rightarrow$ inclusive; $\ge$80\%
survived-like $\Rightarrow$ exclusive/non-inclusive; otherwise uncertain. Control channel
must itself read survived-like on nearly every trial or the avoidance construction is not
holding for that pairing.}
\label{fig:inclusion-flow}
\end{figure}

\begin{figure}[htbp]
\centering
 \includegraphics[width=\textwidth]{sunbird_incl_L1_vs_L2.png}

\caption{Inclusion/exclusion, Sunbird, L1\_vs\_L2 pairing (target=L1, 32{,}768\,B;
eviction past L2, 262{,}144\,B). Units: ticks (single-shot reload). Interpretation: target
90.0\% survived-like (median 84 ticks), control 92.0\% survived-like (median 68) $\Rightarrow$
L1 is EXCLUSIVE/NON-INCLUSIVE with respect to L2, the cleanest and least caveat-exposed of
the three pairings tested on this machine. This figure illustrates the method on a single
machine; \texttt{run\_inclusion\_policy\_full.sh} has since been run for all three level
pairings on all eight lab machines (Table~\ref{tab:inclusion-crossmachine}), and the
full per-machine classifier output and plots for all three pairings are in
Section~\ref{sec:permachine} and each machine's own
\texttt{data\_processed/<machine>/FINAL\_CACHE\_TABLE.md}.}
\label{fig:inclusion-plot}
\end{figure}

\begin{table}[htbp]
\centering
\small
\caption{Inclusion/exclusion verdict by machine and level pairing, all eight lab machines
(\texttt{scripts/classify\_inclusion\_policy.py} output, one run per machine, 2026-09-13).
NI = non-inclusive/exclusive, INC = inclusive, UNC = uncertain (classifier fell short of
the 80\% threshold either way); a parenthetical lean is given for UNC verdicts where the
target/control gap still favors a direction. L2\_vs\_LLC uses an L2 target and therefore
carries the least confidence on every machine (Section~\ref{sec:inclusion}, caveat 3).}
\label{tab:inclusion-crossmachine}
\begin{tabular}{|l|l|l|l|l|}
\hline
\textbf{Machine} & \textbf{ISA/Vendor} & \textbf{L1\_vs\_L2} & \textbf{L2\_vs\_LLC} & \textbf{L1\_vs\_LLC (skip-level)} \\
\hline
Sunbird     & x86-64/Intel  & NI  & UNC (lean INC) & UNC (lean INC) \\
Artemisia   & x86-64/Intel  & UNC (footprint-confounded) & INC & INC \\
Thunderbird & AArch64/Ampere& NI  & UNC (lean INC) & NI (opposite lean from Sunbird) \\
Skylark     & x86-64/AMD    & UNC (weak lean NI) & NI  & NI \\
Upgrade     & x86-64/Intel  & NI  & NI (lean)       & UNC (lean NI) \\
Ookay       & x86-64/Intel  & NI  & UNC (lean INC) & UNC (lean INC) \\
Crux        & x86-64/Intel  & NI  & NI (lean)       & UNC (barely lean NI) \\
Charnwood   & x86-64/Intel  & NI  & UNC (lean INC) & UNC (lean INC, moderate) \\
\hline
\end{tabular}
\end{table}

\textbf{Known limitation, stated directly in the source:} the avoidance guarantee (the
eviction walk structurally cannot land on the target's own set) is proven to hold only when
the upper level's entire set-index fits within one 4\,KiB page -- true for a typical
32--64\,KiB, 8-way L1 (confirmed to fit exactly, e.g.\ Sunbird's 64 sets $\times$ 64\,B
lines = 12 index+offset bits), only \emph{approximately} true for a larger L1 with more
index bits (Thunderbird's 65{,}536\,B/4-way/256-set L1 needs 14 bits, 2 more than one page
holds), and essentially never true for an L2-or-bigger target. Pairings that use an L2
target (L2\_vs\_LLC) therefore carry the least confidence of any pairing on every machine
and are reported as leans, not firm calls. A second, independently discovered failure mode
surfaced once all eight machines were run: on Charnwood, both LLC-scale pairings' own
\emph{control} channel -- which is never touched by the eviction walk and should read
hit-speed on nearly every trial regardless of this machine's real inclusion policy --
itself showed 54.0\% and 40.0\% invalidated-like trials, direct on-machine evidence that
the large-footprint DTLB-pressure caveat (Section~\ref{sec:inclusion}'s caveat 2) can
contaminate the control channel and not just the target, something no single-machine run
had exposed. \textbf{Cross-machine summary (Table~\ref{tab:inclusion-crossmachine}):}
L1\_vs\_L2 is the one pairing that reproduces consistently -- non-inclusive/exclusive on
6 of 8 machines, with the remaining two (Artemisia, Skylark) landing UNCERTAIN rather than
disagreeing outright, one of them attributable to a known footprint-scaling confound
(Artemisia's L2 candidate is itself an unconfirmed capacity boundary). The two
LLC-involving pairings show no fleet-wide agreement at all: Artemisia reads confidently
INCLUSIVE at both, Skylark/Upgrade/Crux lean NON-INCLUSIVE at both, and
Sunbird/Thunderbird/Ookay/Charnwood land UNCERTAIN with leans that split in both
directions even between machines of the same vendor (e.g.\ Sunbird vs.\ Upgrade, both
Intel). Because the method's own structural guarantee is weakest for exactly these two
pairings, this spread is at least as consistent with the L2-target avoidance failure and
the DTLB/large-footprint confound as with genuine cross-vendor differences in LLC
inclusion policy -- this report does not claim the latter without Phase-II corroboration,
and the full per-machine reasoning (including each machine's own best-guess synthesis) is
in \texttt{data\_processed/<machine>/FINAL\_CACHE\_TABLE.md} and referenced again in
Section~\ref{sec:evolution}.

\textbf{Phase-II validation:} the perf-based PMU verification pass (Section~\ref{sec:pmu})
targeted cache boundaries/associativity rather than the eviction-based classifier itself;
a direct PMU cross-check of inclusion/exclusion behavior (e.g.\ snoop-filter or
back-invalidate event counts, where exposed) was not attempted on any machine this cycle.

\section{Per-Machine Timing-Only Results}
\label{sec:permachine}

Section~\ref{sec:phase1} defined the code, controlled sweep, and inference rule for each
target once; this section applies that identical method to all eight ECE lab machines and
reports each machine's own plots, measured values, and inferred cache geometry. Every
number below is Phase I (timing-only); Phase-II PMU agreement is summarized per machine at
the end of each subsection and detailed fleet-wide in Section~\ref{sec:pmu}. Full
per-pairing reasoning, raw numbers, and caveats for every cell live in each machine's own
\texttt{data\_processed/<machine>/FINAL\_CACHE\_TABLE.md}, which this section condenses.
Machines are presented oldest to newest (Section~\ref{sec:evolution}'s year convention).

\subsection{Sunbird (2014, Intel Haswell-EP)}
\label{sec:machine-sunbird}

2x Intel Xeon E5-2680 v3, pinned via \texttt{taskset} to one physical core on socket 0.
Sunbird has the most complete single-machine dataset on the team (Section~\ref{sec:phase1}
uses it as the worked example) and also anchors the derivation of every other machine's
generation-relative comparisons.

\subsubsection{Cache Hierarchy and Capacity}
A 1\,KiB--1\,GiB log-spaced sweep (Figure~\ref{fig:capacity-plot}, Section~\ref{sec:capacity})
shows three flat/ramp transitions, confirmed via independent repeat runs: \textbf{L1D =
32,768\,B}, \textbf{L2 = 262,144\,B}, \textbf{LLC $\approx$ 31,457,280\,B ($\approx$30\,MiB)}.
Two candidate boundaries from the automatic detector (256\,KiB and $\sim$30\,MiB as
distinct extra knees) were re-verified with a dense, high-resolution targeted re-run and
retracted as sitting inside the already-documented continuous ramps, not genuine edges.

\subsubsection{Cache Line Size}
Both the single-curve (ramp-saturation) and family-of-curves methods agree at
\textbf{64\,B} at all three levels, cross-checked with an offset sweep
(Figure~\ref{fig:linesize-plot}, Section~\ref{sec:linesize}).

\subsubsection{Cache Associativity}
\assocthreepanel{sunbird_assoc_L1.png}{sunbird_assoc_L2.png}{sunbird_assoc_L3_LLC.png}{Associativity sweeps, Sunbird. L1: clean knee at num\_ways=8$\to$9, \textbf{8-way, confirmed}. L2/LLC: both candidates -- spanning a $>$50x byte range -- break near the same num\_ways$\approx$9--10 regardless of capacity, the shared small-structure confound (Section~\ref{sec:assoc}); best guess \textbf{L2 = 8-way} (corroborated by Upgrade's derived-stride scan at an equivalent stride) and \textbf{LLC = 9-way} (most reproducible empirical wall, reported as an effective lower bound).}
S=C/(A$\times$B) cross-check at 8/8/9-way: 64 / 512 / $\approx$54,613 sets -- L1$\to$L2 scales
an exact 8x matching the capacity ratio; LLC's non-integer set count is expected since its
$\sim$30\,MiB capacity is itself a rounded estimate, not evidence the guess is wrong.

\subsubsection{Hit Latency}
\hitfourpanel{sunbird_hitlat_L1.png}{sunbird_hitlat_L2.png}{sunbird_hitlat_LLC.png}{sunbird_hitlat_DRAM.png}{Hit latency, Sunbird, dependent/random/median: L1 $\approx$10.35, L2 $\approx$26.83, LLC $\approx$58.42, DRAM $\approx$207.10 ticks -- a clean monotonic 4-tier ladder; independent-load control read faster at every level/pattern, confirming MLP was exposed.}

\subsubsection{Miss and Next-Level Latency}
\missthreepanel{sunbird_misslat_L1_to_L2.png}{sunbird_misslat_L2_to_LLC.png}{sunbird_misslat_LLC_to_DRAM.png}{Miss/next-level latency, Sunbird, random/median: L1$\to$L2 $\approx$124, L2$\to$LLC $\approx$284, LLC$\to$DRAM $\approx$622 ticks. Carries an uncorrected $\approx$64--85-tick fixed single-shot overhead (Section~\ref{sec:misslatency}); read as reload latency plus overhead, not a clean incremental number.}

\subsubsection{Inclusion and Exclusion Behavior}
\begin{figure}[H]
\centering
\includegraphics[width=0.75\textwidth]{sunbird_incl_L1_vs_L2.png}
\caption{Inclusion/exclusion, Sunbird, L1\_vs\_L2 (the cleanest of the three pairings on this machine).}
\end{figure}
\textbf{L1 vs.\ L2: NON-INCLUSIVE} (90.0\% target survived-like, confident). \textbf{L2 vs.\
LLC: UNCERTAIN} (13.5\% survived / 1.5\% invalidated among resolved trials, a 9:1 lean toward
survival but 85\% ambiguous). \textbf{L1 vs.\ LLC (skip-level): UNCERTAIN, leans INCLUSIVE}
(75.0\% invalidated-like, just short of the 80\% firm-call threshold). Best-guess synthesis:
an LLC that behaves inclusively toward L1 specifically (consistent with a cross-core
snoop-filter design) alongside a non-inclusive L2.

\subsubsection{Final Inferred Cache Table (Phase I) and PMU Agreement}
\begin{table}[H]
\centering\scriptsize
\caption{Final inferred cache table, Sunbird (Phase I best guess; PMU agreement in parentheses, Section~\ref{sec:pmu}).}
\begin{tabular}{|l|l|l|l|l|l|l|l|}
\hline
Level & Size & Line & Assoc.\ (Sets) & Hit lat.\ (ticks/ns) & Miss/next lat.\ (ticks) & Sharing & Inclusion vs.\ below\\
\hline
L1D & 32,768\,B & 64\,B & 8-way (64) -- PMU exact & 10.35 / 4.14 & 124 & Private/core -- PMU: 2 SMT threads & Non-inclusive\\
L2 & 262,144\,B & 64\,B & 8-way (512) -- PMU exact & 26.83 / 10.73 & 284 & Private/core -- PMU: confirmed & Uncertain (lean NI)\\
LLC & $\sim$31.46\,MB (PMU exact) & 64\,B & 9-way $\to$ PMU \textbf{20-way} ($\sim$54,613 $\to$ PMU 24,576 sets) & 58.42 / 23.37 & 622 & PMU: whole socket, 24 CPUs, 2.5\,MiB/core & Uncertain (lean INC)\\
DRAM & -- & -- & -- & 207.10 / 82.8 & -- & -- & --\\
\hline
\end{tabular}
\end{table}
LLC associativity is the one field Phase I got wrong here (9 vs.\ PMU's 20-way, outside even
Agner Fog's quoted 12--16-way family range for this generation, since this is a 12-core
server die); everything else -- size, line size, sharing scope, L1/L2 associativity --
matched exactly (Section~\ref{sec:pmu}).

\subsection{Charnwood (2015, Intel Skylake)}
\label{sec:machine-charnwood}

Intel Core i7-6700, pinned to physical core 3 (SMT sibling CPU 7 confirmed idle). This
machine was shared with another student's own \texttt{associativity} benchmark on a
different core during the capacity run -- flagged per boundary below.

\subsubsection{Cache Hierarchy and Capacity}
\textbf{L1D = 32,768\,B} is the one clean boundary (Figure~\ref{fig:charnwood-capacity}); the
$\sim$1.83--11.31\,MiB region shows strong bimodal contamination (12--157\% run-to-run
spread) traced to the concurrent contention, not a real second/third transition. \textbf{L2
= 262,144\,B} and \textbf{LLC = 8,388,608\,B} ($\sim$8\,MiB) are taken from
\texttt{CAPACITY\_RESULTS.md}'s cross-machine reconciliation; this machine's own sweep shows
one continuous ramp through the L2 region rather than a clean plateau. A manual 256\,MiB--
1\,GiB follow-up found the topmost region climbing $\sim$11--15\% from $\sim$12\,MiB to
$\sim$600\,MiB (plausibly TLB/page-walk growth) before a further, less reproducible
divergence above $\sim$700\,MiB (likely shared-machine memory pressure).
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{charnwood_capacity.png}
\caption{Capacity sweep, Charnwood.}
\label{fig:charnwood-capacity}
\end{figure}

\subsubsection{Cache Line Size}
\textbf{64\,B} at all three levels: 2 independent lines of evidence (coarse family-of-curves
split + step-4 offset check, one flagged-then-explained outlier).
\begin{figure}[H]
\centering
\includegraphics[width=0.70\textwidth]{charnwood_linesize.png}
\caption{Line-size sweep, Charnwood L1.}
\end{figure}

\subsubsection{Cache Associativity}
\assocthreepanel{charnwood_assoc_L1.png}{charnwood_assoc_L2.png}{charnwood_assoc_L3_LLC.png}{Associativity sweeps, Charnwood. L1: \textbf{8-way, confirmed} (0 disagreement, base+2 repeats). L2 and LLC curves are numerically indistinguishable to within $\approx$0.05 ticks/access at every num\_ways=2--23 despite a 32x byte-capacity difference -- one of the cleanest on-machine confound demonstrations on the team; best guess \textbf{L2 = LLC = 8-way} (matching L1, via the second staircase jump at num\_ways=9).}
S=C/(A$\times$B) at 8-way uniformly: 64 / 512 / 16,384 sets, both ratios (8x, 32x) matching
the capacity ratios exactly -- the cleanest internal-consistency result of any machine.

\subsubsection{Hit Latency}
\hitfourpanel{charnwood_hitlat_L1.png}{charnwood_hitlat_L2.png}{charnwood_hitlat_LLC.png}{charnwood_hitlat_DRAM.png}{Hit latency, Charnwood: L1 $\approx$7.98 (rep1+rep2 average; base run's 9.55 excluded as a documented P-state/cold-start artifact), L2 $\approx$17.27, LLC $\approx$108, DRAM $\approx$394.7 ticks.}

\subsubsection{Miss and Next-Level Latency}
\missthreepanel{charnwood_misslat_L1_to_L2.png}{charnwood_misslat_L2_to_LLC.png}{charnwood_misslat_LLC_to_DRAM.png}{Miss/next-level latency, Charnwood: L1$\to$L2 $\approx$107, L2$\to$LLC $\approx$644, LLC$\to$DRAM $\approx$852 ticks (base/rep1/rep2 average; $\approx$62-tick overhead caveat as elsewhere).}

\subsubsection{Inclusion and Exclusion Behavior}
\begin{figure}[H]
\centering
\includegraphics[width=0.75\textwidth]{charnwood_incl_L1_vs_L2.png}
\caption{Inclusion/exclusion, Charnwood, L1\_vs\_L2.}
\end{figure}
\textbf{L1 vs.\ L2: NON-INCLUSIVE} (86.0\% survived, control 100\% clean, cleanest pairing).
\textbf{L2 vs.\ LLC: leans NON-INCLUSIVE, low confidence} -- target 100\% invalidated-like,
but the \emph{control} itself reads 54.0\% invalidated-like despite never being touched by
the eviction walk, a confirmed confound (large-footprint DTLB pressure contaminating even the
control channel, not just the target -- the first machine to expose this failure mode).
\textbf{L1 vs.\ LLC (skip-level): leans INCLUSIVE, moderate confidence} (target 99.5\%
invalidated, control 40.0\% invalidated -- weaker contamination, and a $\approx$124-tick
target-control gap, more than double L2\_vs\_LLC's $\approx$54-tick gap). Best-guess
synthesis matches Sunbird's: LLC inclusive toward L1, non-inclusive L2.

\subsubsection{Final Inferred Cache Table (Phase I) and PMU Agreement}
\begin{table}[H]
\centering\scriptsize
\caption{Final inferred cache table, Charnwood.}
\begin{tabular}{|l|l|l|l|l|l|l|l|}
\hline
Level & Size & Line & Assoc.\ (Sets) & Hit lat.\ (ticks/ns) & Miss/next lat.\ (ticks) & Sharing & Inclusion vs.\ below\\
\hline
L1D & 32,768\,B & 64\,B & 8-way (64) -- PMU exact & 7.98 / 2.35 & 107 & Private/core & Non-inclusive\\
L2 & 262,144\,B & 64\,B & 8-way $\to$ PMU \textbf{4-way} (512 $\to$ PMU 1,024) & 17.27 / 5.08 & 644 & Private/core & Leans non-inclusive (low conf.)\\
LLC & 8,388,608\,B -- PMU exact & 64\,B & 8-way $\to$ PMU \textbf{16-way} (16,384, PMU) & 108 / 31.76 & 852 & Whole chip, 4c/8t, 2.0\,MiB/core & Leans inclusive (moderate conf.)\\
DRAM & -- & -- & -- & 394.7 / 116.1 & -- & -- & --\\
\hline
\end{tabular}
\end{table}

\subsection{Ookay (2017, Intel Kaby Lake)}
\label{sec:machine-ookay}

Intel Core i7-7700, core 1 for the main pipeline (core 2/3 for follow-ups). Multiple other
students' jobs were confirmed active on other cores throughout this session.

\subsubsection{Cache Hierarchy and Capacity}
Automatic detection flagged 6 candidate boundaries; the 4 between $\sim$5.3--11.9\,MiB are
very likely one continuous steep transition rather than 4 real levels. Only \textbf{L1D =
285,864\,B $\to$ taken as 32,768\,B} and the L2-region boundary look like genuine
flat/ramp transitions; \textbf{L2 = 262,144\,B}, \textbf{LLC = 8,388,608\,B} ($\sim$8\,MiB)
per \texttt{CAPACITY\_RESULTS.md}. A manual 256\,MiB--1\,GiB follow-up (run0 + 2 independent
repeats, median-of-3 combined to filter scattered single-run interference spikes) confirmed a
flat DRAM plateau ($\approx$286--297 ticks/access).
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{ookay_capacity.png}
\caption{Capacity sweep, Ookay.}
\end{figure}

\subsubsection{Cache Line Size}
\textbf{64\,B} confirmed at L1 with a clean, offset-independent elbow; L2/LLC carry only
weak/uncontradicted evidence for the same value (no competing measurement).
\begin{figure}[H]
\centering
\includegraphics[width=0.70\textwidth]{ookay_linesize.png}
\caption{Line-size sweep, Ookay L1.}
\end{figure}

\subsubsection{Cache Associativity}
\assocthreepanel{ookay_assoc_L1.png}{ookay_assoc_L2.png}{ookay_assoc_L3_LLC.png}{Associativity sweeps, Ookay. L1: \textbf{8-way, confirmed} (8/8/8 across base + 2 repeats). L2 and LLC produce numerically indistinguishable staircases at the same two breakpoints despite a 32x capacity difference; best guess \textbf{L2 = LLC = 8-way} -- with no independent technique available on this machine to separate the two (unlike Sunbird/Upgrade).}

\subsubsection{Hit Latency}
\hitfourpanel{ookay_hitlat_L1.png}{ookay_hitlat_L2.png}{ookay_hitlat_LLC.png}{ookay_hitlat_DRAM.png}{Hit latency, Ookay: L1 $\approx$7.49, L2 $\approx$18.53, LLC $\approx$74.78, DRAM $\approx$284.45 ticks.}

\subsubsection{Miss and Next-Level Latency}
\missthreepanel{ookay_misslat_L1_to_L2.png}{ookay_misslat_L2_to_LLC.png}{ookay_misslat_LLC_to_DRAM.png}{Miss/next-level latency, Ookay: L1$\to$L2 $\approx$96, L2$\to$LLC $\approx$723, LLC$\to$DRAM $\approx$693 ticks -- notably \emph{not} clearly larger than the L2$\to$LLC number, likely reflecting the two transitions' differing eviction-set margins above their respective source-level capacities rather than a true reversal of penalty size.}

\subsubsection{Inclusion and Exclusion Behavior}
\begin{figure}[H]
\centering
\includegraphics[width=0.75\textwidth]{ookay_incl_L1_vs_L2.png}
\caption{Inclusion/exclusion, Ookay, L1\_vs\_L2.}
\end{figure}
\textbf{L1 vs.\ L2: NON-INCLUSIVE} (100\% target AND 100\% control survived-like --
statistically indistinguishable from each other, the cleanest result on any machine).
\textbf{L2 vs.\ LLC: weak lean toward INVALIDATED/inclusive-like} (30.5\% invalidated vs.\
control's 2.5\%, a $\approx$12x relative lean, but 69.5\% of target trials still read
survived-like -- lowest confidence of the three, opposite lean from Sunbird's own L2\_vs\_LLC).
\textbf{L1 vs.\ LLC (skip-level): leans INVALIDATED/inclusive-like} (99.5\% paired-slower,
the single cleanest directional signal on this machine). Best-guess synthesis matches
Sunbird's and Charnwood's: an LLC inclusive of what is resident above it.

\subsubsection{Final Inferred Cache Table (Phase I) and PMU Agreement}
\begin{table}[H]
\centering\scriptsize
\caption{Final inferred cache table, Ookay.}
\begin{tabular}{|l|l|l|l|l|l|l|l|}
\hline
Level & Size & Line & Assoc.\ (Sets) & Hit lat.\ (ticks/ns) & Miss/next lat.\ (ticks) & Sharing & Inclusion vs.\ below\\
\hline
L1D & 32,768\,B & 64\,B & 8-way (64) -- PMU exact & 7.49 / 2.08 & 96 & Private/core & Non-inclusive\\
L2 & 262,144\,B & 64\,B & 8-way $\to$ PMU \textbf{4-way} (1,024 sets, PMU) & 18.53 / 5.15 & 723 & Private/core & Weak lean inclusive\\
LLC & 8,388,608\,B -- PMU exact & 64\,B & 8-way $\to$ PMU \textbf{16-way} (8,192 sets, PMU) & 74.78 / 20.77 & 693 & Whole chip, 4c/8t, 2.0\,MiB/core & Leans inclusive (moderate-strong)\\
DRAM & -- & -- & -- & 284.45 / 79.02 & -- & -- & --\\
\hline
\end{tabular}
\end{table}

\subsection{Upgrade (2017, Intel Coffee Lake)}
\label{sec:machine-upgrade}

Intel Core i7-8700, core 5 (the established convention for this machine's associativity/
software-hit-rate/PMU runs).

\subsubsection{Cache Hierarchy and Capacity}
\textbf{L1D = 32,768\,B} (PROVISIONAL from capacity data alone, later corroborated by a clean
associativity knee at that exact stride). \textbf{L2 = 262,144\,B} is a candidate,
not-independently-confirmed $\sim$1.5--4.5\,MiB shelf region taken from
\texttt{CAPACITY\_RESULTS.md}. \textbf{LLC $\approx$ 12,582,912\,B ($\sim$12\,MiB)} sits
inside a noisy, never-fully-resolved $\sim$5--22\,MiB transition on this machine's own sweep.
A 4x tail extension (64--256\,MiB) found the topmost region still climbing (+11.9\% first-to-
last-quarter), not flat at the pipeline's default ceiling.
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{upgrade_capacity.png}
\caption{Capacity sweep, Upgrade.}
\end{figure}

\subsubsection{Cache Line Size}
\textbf{64\,B} confirmed at L1 (2 methods plus 2 independent step-4 cross-alignment repeats,
all converging cleanly); L2/LLC carry the same value on weak, uncontradicted evidence (7+
repeated L2 runs never converged on any \emph{different} value).
\begin{figure}[H]
\centering
\includegraphics[width=0.70\textwidth]{upgrade_linesize.png}
\caption{Line-size sweep, Upgrade L1.}
\end{figure}

\subsubsection{Cache Associativity}
\assocthreepanel{upgrade_assoc_L1.png}{upgrade_assoc_L2.png}{upgrade_assoc_L3_LLC.png}{Associativity sweeps, Upgrade. L1: \textbf{8-way, confirmed}, independently corroborating the provisional L1 capacity estimate. L2/LLC hit the same fixed-full-capacity-stride confound as every other machine, but Upgrade is also where the \textbf{derived-stride-scan} technique (\texttt{run\_associativity\_stride\_scan.sh}) originated: a full-rigor confirm run at a shared 65,536\,B stride reveals a two-step staircase -- a marginal artifact step at way 4, then a real, sharp knee at \textbf{way 9} -- giving best guess \textbf{L2 = LLC = 8-way} with more direct support than most other machines' guesses.}
S=C/(A$\times$B) at 8-way/64\,B uniformly: 64 / 512 / 24,576 sets -- LLC's set count is an
exact integer, additional (if modest) support for 8-way.

\subsubsection{Hit Latency}
\hitfourpanel{upgrade_hitlat_L1.png}{upgrade_hitlat_L2.png}{upgrade_hitlat_LLC.png}{upgrade_hitlat_DRAM.png}{Hit latency, Upgrade: L1 $\approx$6.17, L2 $\approx$16.12, LLC $\approx$155.05, DRAM $\approx$251.56 ticks -- LLC hit latency here is unusually close to DRAM, consistent with this machine's LLC capacity/eviction-footprint uncertainty above.}

\subsubsection{Miss and Next-Level Latency}
\missthreepanel{upgrade_misslat_L1_to_L2.png}{upgrade_misslat_L2_to_LLC.png}{upgrade_misslat_LLC_to_DRAM.png}{Miss/next-level latency, Upgrade: L1$\to$L2 $\approx$67, L2$\to$LLC $\approx$510, LLC$\to$DRAM $\approx$605 ticks.}

\subsubsection{Inclusion and Exclusion Behavior}
\begin{figure}[H]
\centering
\includegraphics[width=0.75\textwidth]{upgrade_incl_L1_vs_L2.png}
\caption{Inclusion/exclusion, Upgrade, L1\_vs\_L2.}
\end{figure}
\textbf{L1 vs.\ L2: NON-INCLUSIVE} (99.0\% survived, 0\% invalidated -- cleanest result on
this machine). \textbf{L2 vs.\ LLC: leans NON-INCLUSIVE, least confidence of the three}
(95.5\% survived, 4.0\% invalidated; one repeat showed 90.4\% spread, consistent with
shared-machine interference). \textbf{L1 vs.\ LLC (skip-level): leans NON-INCLUSIVE} --
classifier itself calls this UNCERTAIN, but the target's tight interquartile reload cost sits
far closer to this machine's own LLC-hit latency than to either survived/invalidated
calibration extreme, arguing against inclusive back-invalidation. Best-guess synthesis: no
level tested here is inclusive of the level below it, the same overall shape as Skylark's
read but for a different underlying vendor/generation reason.

\subsubsection{Final Inferred Cache Table (Phase I) and PMU Agreement}
\begin{table}[H]
\centering\scriptsize
\caption{Final inferred cache table, Upgrade.}
\begin{tabular}{|l|l|l|l|l|l|l|l|}
\hline
Level & Size & Line & Assoc.\ (Sets) & Hit lat.\ (ticks/ns) & Miss/next lat.\ (ticks) & Sharing & Inclusion vs.\ below\\
\hline
L1D & 32,768\,B & 64\,B & 8-way (64) -- PMU exact & 6.17 / 1.93 & 67 & Private/core & Non-inclusive\\
L2 & 262,144\,B & 64\,B & 8-way $\to$ PMU \textbf{4-way} (1,024 sets, PMU) & 16.12 / 5.04 & 510 & Private/core & Leans non-inclusive (weak)\\
LLC & 12,582,912\,B -- PMU exact & 64\,B & 8-way $\to$ PMU \textbf{16-way} (12,288 sets, PMU) & 155.05 / 48.45 & 605 & Whole chip, 6c/12t, 2.0\,MiB/core & Uncertain (reasoned lean NI)\\
DRAM & -- & -- & -- & 251.56 / 78.61 & -- & -- & --\\
\hline
\end{tabular}
\end{table}

\subsection{Crux (2019, Intel Coffee Lake Refresh)}
\label{sec:machine-crux}

Intel Core i7-9700, 8 physical cores, no SMT, pinned to core 7.

\subsubsection{Cache Hierarchy and Capacity}
\texttt{run\_capacity\_full.sh} detected 4 boundaries: 262,144 / 9,147,840 / 11,863,280 /
16,777,216\,B. \textbf{L1D = 32,768\,B}. The $\sim$64\,KiB--4\,MiB region is a soft ramp with
no confirmed flat shelf (\textbf{L2 = 262,144\,B} taken from \texttt{CAPACITY\_RESULTS.md}
regardless); $\sim$4--64\,MiB showed up to 89\% run-to-run spread (\textbf{LLC = 8,388,608\,B}
$\sim$8\,MiB Phase I estimate, though \texttt{lscpu} reports a real 12\,MiB LLC -- noted, not
resolved, in Phase I). A manual 256\,MiB--1\,GiB follow-up (3 independent runs) found a
genuine plateau ($\approx$245--250 ticks/access, agreeing within 0.5\% at 1\,GiB).
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{crux_capacity.png}
\caption{Capacity sweep, Crux.}
\end{figure}

\subsubsection{Cache Line Size}
\textbf{64\,B} at all three levels, the most-repeated result on the team (5 independent runs
converging 1.00--1.12x spread every time; L1's own reproducibility took 3 attempts to land
exactly at 1.00x).
\begin{figure}[H]
\centering
\includegraphics[width=0.70\textwidth]{crux_linesize.png}
\caption{Line-size sweep, Crux L1.}
\end{figure}

\subsubsection{Cache Associativity}
\assocthreepanel{crux_assoc_L1.png}{crux_assoc_L2.png}{crux_assoc_L3_LLC.png}{Associativity sweeps, Crux. L1: \textbf{8-way, confirmed}. L2 (262,144\,B) and LLC (8,388,608\,B) -- a 32x capacity difference -- both independently report \textbf{raw 4-way}, with full 3/3 reproducibility each; Charnwood tested the exact same two byte values and got the exact same "4" at both, independent cross-machine corroboration this is the shared confound, not real associativity. Best guess \textbf{L2 = LLC = 8-way} (anchored to L1).}
S=C/(A$\times$B) at 8-way: 64 / 512 / 16,384 -- every count an exact integer, both ratios
(8x, 32x) matching the capacity ratios exactly, the cleanest such cross-check on the team.

\subsubsection{Hit Latency}
\hitfourpanel{crux_hitlat_L1.png}{crux_hitlat_L2.png}{crux_hitlat_LLC.png}{crux_hitlat_DRAM.png}{Hit latency, Crux: L1 $\approx$7.42, L2 $\approx$14.30, LLC $\approx$43.23, DRAM $\approx$236.80 ticks.}

\subsubsection{Miss and Next-Level Latency}
\missthreepanel{crux_misslat_L1_to_L2.png}{crux_misslat_L2_to_LLC.png}{crux_misslat_LLC_to_DRAM.png}{Miss/next-level latency, Crux: L1$\to$L2 $\approx$77, L2$\to$LLC $\approx$260 (77.8\% base-vs-repeat spread, not traced to a specific cause), LLC$\to$DRAM $\approx$437 (up to 42.6\% spread on the sequential pattern) ticks.}

\subsubsection{Inclusion and Exclusion Behavior}
\begin{figure}[H]
\centering
\includegraphics[width=0.75\textwidth]{crux_incl_L1_vs_L2.png}
\caption{Inclusion/exclusion, Crux, L1\_vs\_L2.}
\end{figure}
\textbf{L1 vs.\ L2: NON-INCLUSIVE} (97.5\% survived, cleanest pairing). \textbf{L2 vs.\ LLC:
leans NON-INCLUSIVE, moderate confidence} (90.5\% survived, 2.5\% invalidated). \textbf{L1
vs.\ LLC (skip-level): UNCERTAIN, barely leans NON-INCLUSIVE} (98\% of trials fell in the
ambiguous band, though the target reads slower than its own control in 99.5\% of trials).
Best-guess synthesis: a uniformly non-inclusive hierarchy at every tested boundary -- a
genuine point of contrast with Sunbird's own inclusive-LLC-toward-L1 reading.

\subsubsection{Final Inferred Cache Table (Phase I) and PMU Agreement}
\begin{table}[H]
\centering\scriptsize
\caption{Final inferred cache table, Crux.}
\begin{tabular}{|l|l|l|l|l|l|l|l|}
\hline
Level & Size & Line & Assoc.\ (Sets) & Hit lat.\ (ticks/ns) & Miss/next lat.\ (ticks) & Sharing & Inclusion vs.\ below\\
\hline
L1D & 32,768\,B & 64\,B & 8-way (64) -- PMU exact & 7.42 / 2.47 & 77 & Private/core & Non-inclusive\\
L2 & 262,144\,B & 64\,B & 8-way $\to$ PMU \textbf{4-way} (1,024 sets, PMU) & 14.30 / 4.77 & 260 & Private/core & Leans non-inclusive (moderate)\\
LLC & 8,388,608\,B $\to$ PMU \textbf{12,582,912\,B (1.5x)} & 64\,B & 8-way $\to$ PMU \textbf{12-way} (16,384 sets, PMU) & 43.23 / 14.41 & 437 & Whole chip, 8c, 1.5\,MiB/core & Uncertain (barely lean NI)\\
DRAM & -- & -- & -- & 236.80 / 78.93 & -- & -- & --\\
\hline
\end{tabular}
\end{table}
Crux's LLC \emph{size} was one of only 3 machines (with Skylark, Artemisia) where Phase I's
capacity estimate itself, not just associativity, was later found too small by PMU/system
report.

\subsection{Skylark (2019, AMD Zen 2)}
\label{sec:machine-skylark}

2x AMD EPYC 7532, SMT disabled machine-wide, pinned to core 10 -- the team's one AMD machine.

\subsubsection{Cache Hierarchy and Capacity}
\texttt{run\_capacity\_full.sh skylark 10 134217728} detected 3 boundaries: 16,777,216 /
18,295,680 / 21,757,352\,B. \textbf{L1D = 32,768\,B}. \textbf{L2 = 524,288\,B} (512\,KiB) --
this machine's own sweep only resolved a broad $\sim$4--16.8\,MiB candidate shelf, not a
clean edge. \textbf{LLC = 8,388,608\,B} (8\,MiB) per \texttt{CAPACITY\_RESULTS.md}, but
\textbf{flagged as a likely underestimate}: this machine's own data suggests the real
LLC$\to$DRAM knee sits closer to $\sim$16.8--21.8\,MiB. A manual follow-up extended to
2\,GiB and found a genuinely settled plateau ($\approx$280 ticks/access, $\sim$2\% rise from
512\,MiB, reproduced within $\sim$0.2\% across 2 independent runs at 2\,GiB) -- the
cleanest topmost-region result on the team.
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{skylark_capacity.png}
\caption{Capacity sweep, Skylark.}
\end{figure}

\subsubsection{Cache Line Size}
\textbf{64\,B} confirmed at L1/L2 by two independent methods, but \textbf{128\,B} at the LLC
region -- the one machine on the team with a genuine, still-unexplained per-level line-size
split, confirmed by both methods rather than a single noisy run.
\begin{figure}[H]
\centering
\includegraphics[width=0.70\textwidth]{skylark_linesize.png}
\caption{Line-size sweep, Skylark L1.}
\end{figure}

\subsubsection{Cache Associativity}
\assocthreepanel{skylark_assoc_L1.png}{skylark_assoc_L2.png}{skylark_assoc_L3_LLC.png}{Associativity sweeps, Skylark. L1: \textbf{8-way, confirmed} (single sharp knee at num\_ways=9). L2's auto-detected "9" is very plausibly an L1-aliasing artifact (524,288\,B is an exact 16x multiple of the L1 stride) rather than independent L2 signal; best guess \textbf{L2 = 8-way}. LLC's own auto-detected value disagreed across repeats (8/8/7); best guess \textbf{LLC = 8-way}, the only nearby integer giving a clean derived-set count at this machine's confirmed 128\,B LLC-region line size.}

\subsubsection{Hit Latency}
\hitfourpanel{skylark_hitlat_L1.png}{skylark_hitlat_L2.png}{skylark_hitlat_LLC.png}{skylark_hitlat_DRAM.png}{Hit latency, Skylark: L1 $\approx$6.24, L2 $\approx$16.15, LLC $\approx$27.22, DRAM $\approx$274.87 ticks.}

\subsubsection{Miss and Next-Level Latency}
\missthreepanel{skylark_misslat_L1_to_L2.png}{skylark_misslat_L2_to_LLC.png}{skylark_misslat_LLC_to_DRAM.png}{Miss/next-level latency, Skylark: L1$\to$L2 $\approx$96 (overhead-corrected $\approx$24), L2$\to$LLC $\approx$120 (corrected $\approx$38), LLC$\to$DRAM $\approx$360 (corrected $\approx$267) ticks -- using this machine's own directly-measured $\approx$65.8-tick single-shot overhead.}

\subsubsection{Inclusion and Exclusion Behavior}
\begin{figure}[H]
\centering
\includegraphics[width=0.75\textwidth]{skylark_incl_L1_vs_L2.png}
\caption{Inclusion/exclusion, Skylark, L1\_vs\_L2.}
\end{figure}
\textbf{L1 vs.\ L2: UNCERTAIN, weak lean NON-INCLUSIVE} (68.0\% survived-like -- target/control
medians came back nearly identical, a much weaker signal than most other machines'). \textbf{L2
vs.\ LLC: NON-INCLUSIVE} (99.5\% survived, confident). \textbf{L1 vs.\ LLC (skip-level):
NON-INCLUSIVE} (100\% survived, cleanest of the three, and notably \emph{different} from
Sunbird's own leaning-inclusive skip-level result). Best-guess synthesis: no level is
inclusive of the level below it -- directionally consistent with AMD Zen 2's publicly
documented non-inclusive L3 design (a post-hoc plausibility check only, not consulted while
forming this reading).

\subsubsection{Final Inferred Cache Table (Phase I) and PMU Agreement}
\begin{table}[H]
\centering\scriptsize
\caption{Final inferred cache table, Skylark.}
\begin{tabular}{|l|l|l|l|l|l|l|l|}
\hline
Level & Size & Line & Assoc.\ (Sets) & Hit lat.\ (ticks/ns) & Miss/next lat.\ (ticks) & Sharing & Inclusion vs.\ below\\
\hline
L1D & 32,768\,B & 64\,B & 8-way (64) -- PMU exact & 6.24 / 2.60 & 96 & Private/core & Uncertain (weak lean NI)\\
L2 & 524,288\,B & 64\,B & 8-way -- PMU exact, agrees! (1,024 sets, PMU) & 16.15 / 6.73 & 120 & Private/core & Non-inclusive\\
LLC & 8,388,608\,B $\to$ PMU \textbf{16,777,216\,B (2x, confirmed underestimate)} & \textbf{128\,B} (vs.\ 64\,B at L1/L2) & 8-way $\to$ PMU \textbf{16-way} (16,384 sets, PMU) & 27.22 / 11.34 & 360 & 2 cores/CCX only, 8.0\,MiB/core & Non-inclusive\\
DRAM & -- & -- & -- & 274.87 / 114.5 & -- & -- & --\\
\hline
\end{tabular}
\end{table}
Skylark's LLC sharing domain (2 cores per L3 instance, not the whole socket) is itself a
Phase-II finding that would have been invisible to timing alone -- an AMD Rome
"cache-doubled" binning choice, not a topology guess.

\subsection{Thunderbird (2020, Ampere/ARM Neoverse N1)}
\label{sec:machine-thunderbird}

Ampere Q80-30, AArch64, the team's one ARM machine, pinned to core 3/4. \texttt{CNTVCT\_EL0}
runs at only 25\,MHz, so ticks/access across the whole 1\,KiB--1\,GiB range span just
$\sim$0.08--2.4 -- the x86-calibrated automatic boundary/knee detectors' default absolute-tick
thresholds are unreachable here regardless of relative threshold, so several boundaries below
were picked by eye from the raw curve rather than the automatic detector.

\subsubsection{Cache Hierarchy and Capacity}
\textbf{L1D = 65,536\,B} (64\,KiB, larger than every x86 machine on the team) -- plateau edge
$\sim$64--75\,KiB. \textbf{L2 = 1,048,576\,B} (1\,MiB) sits at the end of a long shallow ramp
with no flat shelf (taken as given). \textbf{LLC $\approx$ 31,457,280\,B} ($\sim$30\,MiB)
inside a dominant, genuinely noisy steep transition $\sim$16--70\,MiB (20--65\% run-to-run
spread across 3 independent runs -- real shared-machine interference, confirmed via
\texttt{mpstat}/\texttt{ps}, not an artifact). A confirmed flat DRAM plateau
($\approx$2.3--2.4 ticks, $\approx$92--96\,ns) holds from $\sim$256\,MiB through 1\,GiB
($\sim$2\% run-to-run spread).
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{thunderbird_capacity.png}
\caption{Capacity sweep, Thunderbird.}
\end{figure}

\subsubsection{Cache Line Size}
\textbf{64\,B} at L1/L2 (offset-independent elbow). \textbf{128\,B at LLC} -- the one level on
this machine that does not share L1/L2's line size (majority agreement at 7/8 tested offsets,
reproduced across 2 independent seeds), mirroring Skylark's own per-level split.
\begin{figure}[H]
\centering
\includegraphics[width=0.70\textwidth]{thunderbird_linesize.png}
\caption{Line-size sweep, Thunderbird L1.}
\end{figure}

\subsubsection{Cache Associativity}
\assocthreepanel{thunderbird_assoc_L1.png}{thunderbird_assoc_L2.png}{thunderbird_assoc_L3_LLC.png}{Associativity sweeps, Thunderbird (rescaled \texttt{--min-abs-ticks}, since default x86-calibrated thresholds are unreachable at 25\,MHz). L1: \textbf{4-way, confirmed} -- reproducible across base + both repeat seeds, the team's third architecture/generation to reproduce this exact method. L2/LLC candidate strides are exact multiples of L1's own, so even their first bump is L1's own thrashing re-triggering; after raising the threshold to skip it, best guess \textbf{L2 = 12-way}, \textbf{LLC = 10-way} -- the same universal small-way-count wall (Section~\ref{sec:assoc}) reproduced on a third machine and, for the first time, a second architecture.}

\subsubsection{Hit Latency}
\hitfourpanel{thunderbird_hitlat_L1.png}{thunderbird_hitlat_L2.png}{thunderbird_hitlat_LLC.png}{thunderbird_hitlat_DRAM.png}{Hit latency, Thunderbird (ns/access is the cross-machine-comparable unit here, via the independently-read 25\,MHz \texttt{CNTFRQ\_EL0}): L1 $\approx$0.130 ticks / 5.0\,ns, L2 $\approx$0.298 ticks / 11.9\,ns, LLC $\approx$0.907 ticks / 36.3\,ns, DRAM $\approx$2.336 ticks / 93.4\,ns.}

\subsubsection{Miss and Next-Level Latency}
\missthreepanel{thunderbird_misslat_L1_to_L2.png}{thunderbird_misslat_L2_to_LLC.png}{thunderbird_misslat_LLC_to_DRAM.png}{Miss/next-level latency, Thunderbird: L1$\to$L2 $\approx$1.5 ticks (60\,ns) -- statistically indistinguishable from this machine's $\approx$1-tick single-shot overhead floor, not resolvable; L2$\to$LLC $\approx$2.0 ticks (80\,ns), a weak but reproducible signal one tick above the floor; LLC$\to$DRAM $\approx$4.0 ticks (160\,ns) is the one confidently resolvable number, overhead-corrected to $\approx$2.1 ticks ($\approx$84\,ns).}

\subsubsection{Inclusion and Exclusion Behavior}
\begin{figure}[H]
\centering
\includegraphics[width=0.75\textwidth]{thunderbird_incl_L1_vs_L2.png}
\caption{Inclusion/exclusion, Thunderbird, L1\_vs\_L2.}
\end{figure}
\textbf{L1 vs.\ L2: NON-INCLUSIVE} (84.5\% survived -- same qualitative result as Sunbird's
own cleanest pairing). \textbf{L2 vs.\ LLC: UNCERTAIN, leans INCLUSIVE} (76.5\% invalidated
among resolved trials, but 23.5\% ambiguous). \textbf{L1 vs.\ LLC (skip-level): leans
NON-INCLUSIVE} (80.5\%, just clearing the firm-call threshold -- the \emph{opposite}
directional lean from Sunbird's own skip-level result). Unlike Sunbird, Charnwood, and
Ookay, Thunderbird's two LLC-involving results do not combine into one internally consistent
story; this machine's own L1 index (65,536\,B/4-way/256 sets needs 14 address bits) does not
fit within one 4\,KiB page the way Sunbird's does, weakening the eviction construction's
avoidance guarantee even for L1-target pairings here.

\subsubsection{Final Inferred Cache Table (Phase I) and PMU Agreement}
\begin{table}[H]
\centering\scriptsize
\caption{Final inferred cache table, Thunderbird.}
\begin{tabular}{|l|l|l|l|l|l|l|l|}
\hline
Level & Size & Line & Assoc.\ (Sets) & Hit lat.\ (ticks/ns) & Miss/next lat.\ (ticks/ns) & Sharing & Inclusion vs.\ below\\
\hline
L1D & 65,536\,B & 64\,B & 4-way (256) -- PMU exact & 0.130 / 5.0 & 1.5 / 60 & Private/core & Non-inclusive\\
L2 & 1,048,576\,B & 64\,B & 12-way $\to$ PMU \textbf{8-way} (2,048 sets, PMU) & 0.298 / 11.9 & 2.0 / 80 & Private/core & Uncertain (leans inclusive)\\
LLC & $\sim$31.46\,MB (lit.\ 32\,MiB, $\approx$94\% match; no system-report entry) & \textbf{128\,B} (vs.\ 64\,B at L1/L2) & 10-way $\to$ lit.\ 16-way (24,576 sets, Phase I) & 0.907 / 36.3 & 4.0 / 160 & \textbf{ALL 80 cores} (lit.), 0.4\,MiB/core & Leans non-inclusive\\
DRAM & -- & -- & -- & 2.336 / 93.4 & -- & -- & --\\
\hline
\end{tabular}
\end{table}
Thunderbird's LLC sharing domain -- all 80 cores of the SoC, per the vendor datasheet -- is
architecturally unlike every other machine's per-socket LLC, and its L1/L2 associativity
guesses were both too high once PMU could check them (L2: 12 guessed vs.\ 8 actual).

\subsection{Artemisia (2023, Intel Sapphire Rapids)}
\label{sec:machine-artemisia}

2x Intel Xeon Gold 5420+, the newest/most architecturally distant CPU on the team's list.
Primary results from \textbf{core 20} (a 2026-09-09 redo after discovering the original core
23 shared its socket with other students' active processes); the archived core-23 run is
kept at \texttt{data\_processed/artemisia/capacity\_prior\_core23/} for its own before/after
evidentiary value (Section~\ref{sec:phasediscipline}).

\subsubsection{Cache Hierarchy and Capacity}
\textbf{L1D = 49,152\,B} (48\,KiB, the only non-32/64\,KiB L1 on the team). \textbf{L2 =
2,097,152\,B} (2\,MiB) is \textbf{not independently confirmed by this machine's own data} --
a waypoint inside one continuous $\sim$48\,KiB--90\,MiB ramp, used per
\texttt{CAPACITY\_RESULTS.md}'s cross-machine direction anyway. \textbf{LLC $\approx$
31,457,280\,B} ($\sim$30\,MiB representative); the machine's own data confirms only that
$\sim$90\,MiB is a genuine plateau \emph{onset}, not that this specific byte value is a clean
edge. Sequential-pattern's $\sim$90\,MiB+ plateau ($\approx$35.3--35.9 ticks/access) matched
within 0.4\% between the original and redo runs, confirming it is genuine hardware behavior
rather than contention.
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{artemisia_capacity.png}
\caption{Capacity sweep, Artemisia (core 20, redo).}
\end{figure}

\subsubsection{Cache Line Size}
\textbf{64\,B} confirmed at L1 (2 methods, 3 independent reproductions, offset-invariance
confirmed). L2's 64\,B is best-supported but the offset-invariance check came back only
partially mixed. LLC's line size is \textbf{unresolved} -- 11 independent attempts at the
$\sim$30\,MiB candidate found no detectable signal at all; 64\,B is carried over as the best
available default, not a machine-specific measurement at that level.
\begin{figure}[H]
\centering
\includegraphics[width=0.70\textwidth]{artemisia_linesize.png}
\caption{Line-size sweep, Artemisia L1.}
\end{figure}

\subsubsection{Cache Associativity}
\assocthreepanel{artemisia_assoc_L1.png}{artemisia_assoc_L2.png}{artemisia_assoc_L3_LLC.png}{Associativity sweeps, Artemisia. L1: best guess \textbf{12-way} (clean transition to a low-spread shelf at exactly num\_ways=13 in all 3 runs, though one repeat's raw detector output was a P-state-noise false positive) -- later PMU-confirmed exactly. L2 and LLC candidates -- a 15x byte range -- give numerically indistinguishable curves at every num\_ways=2--40; best guess \textbf{L2 = LLC = 16-way} (structural argument: L2's capacity is an exact power of two, so only a power-of-two associativity gives a clean derived-set count).}

\subsubsection{Hit Latency}
\hitfourpanel{artemisia_hitlat_L1.png}{artemisia_hitlat_L2.png}{artemisia_hitlat_LLC.png}{artemisia_hitlat_DRAM.png}{Hit latency, Artemisia: L1 $\approx$9.87, L2 $\approx$28.05, LLC $\approx$94.38, DRAM $\approx$305.58 ticks -- the highest LLC and DRAM latency in ticks of any machine, plausibly reflecting this machine's much larger multi-socket server die and slower 2.0\,GHz base clock rather than a simple generational speedup.}

\subsubsection{Miss and Next-Level Latency}
\missthreepanel{artemisia_misslat_L1_to_L2.png}{artemisia_misslat_L2_to_LLC.png}{artemisia_misslat_LLC_to_DRAM.png}{Miss/next-level latency, Artemisia: L1$\to$L2 $\approx$188, L2$\to$LLC $\approx$381, LLC$\to$DRAM $\approx$582 ticks.}

\subsubsection{Inclusion and Exclusion Behavior}
\begin{figure}[H]
\centering
\includegraphics[width=0.75\textwidth]{artemisia_incl_L1_vs_L2.png}
\caption{Inclusion/exclusion, Artemisia, L1\_vs\_L2.}
\end{figure}
\textbf{L1 vs.\ L2: UNCERTAIN} (77.5\% ambiguous, most likely because this pairing inherited
the oversized, unconfirmed L2 capacity candidate as its eviction-footprint scale -- 8x the
DTLB pressure Sunbird's equivalent, confirmed-capacity pairing carried). \textbf{L2 vs.\ LLC:
INCLUSIVE} (100\% invalidated-like, 0\% ambiguous -- cleaner than Sunbird's own equivalent
pairing). \textbf{L1 vs.\ LLC (skip-level): INCLUSIVE} (100\% invalidated-like, 0\%
ambiguous, the cleanest and most confident result of all three pairings on this machine).
Best-guess synthesis: both LLC-involving pairings decisively support an LLC that is
inclusive of both L1's and L2's resident lines -- the cleanest such reading on the team --
with L1 vs.\ L2 the one genuinely open question, plausibly a methodological artifact of the
oversized L2 eviction footprint rather than a real ambiguity in hardware behavior.

\subsubsection{Final Inferred Cache Table (Phase I) and PMU Agreement}
\begin{table}[H]
\centering\scriptsize
\caption{Final inferred cache table, Artemisia.}
\begin{tabular}{|l|l|l|l|l|l|l|l|}
\hline
Level & Size & Line & Assoc.\ (Sets) & Hit lat.\ (ticks/ns) & Miss/next lat.\ (ticks) & Sharing & Inclusion vs.\ below\\
\hline
L1D & 49,152\,B & 64\,B & 12-way (64) -- PMU exact & 9.87 / 4.94 & 188 & Private/core & Uncertain\\
L2 & 2,097,152\,B* & 64\,B & 16-way -- PMU exact, agrees! (2,048 sets, PMU) & 28.05 / 14.03 & 381 & Private/core & Inclusive\\
LLC & 31,457,280\,B $\to$ PMU \textbf{55,050,240\,B (1.75x, confirmed)} & 64\,B (unmeasured at LLC) & 16-way $\to$ PMU \textbf{15-way, close} (57,344 sets, PMU) & 94.38 / 47.19 & 582 & 1 socket, 56 CPUs, 1.875\,MiB/core & Inclusive\\
DRAM & -- & -- & -- & 305.58 / 152.79 & -- & -- & --\\
\hline
\end{tabular}
\end{table}
{\scriptsize *L2's own capacity is a \texttt{CAPACITY\_RESULTS.md} placeholder this
machine's own sweep never independently confirmed; Phase-II system-report and Intel ARK both
match it exactly regardless.} Artemisia's LLC size carries the largest Phase~I/Phase~II gap
of any machine on the team (1.75x), and its 1.875\,MiB/core figure independently matches
Chips and Cheese's own published per-core L3 slice figure for this exact SKU -- an external
cross-check outside this project's own data.

\section{Performance Counter Verification}
\label{sec:pmu}

\subsection{Verify the Reverse Engineering with Performance Counters}

\subsubsection{Exposed Cache Events}
\texttt{perf list} confirmed real cross-vendor differences. Sunbird (Intel Haswell-EP) exposes all eight target events. Skylark (AMD Zen 2) lists only five: \texttt{L1-dcache-stores}, \texttt{LLC-loads}, and \texttt{LLC-load-misses} are absent and return \texttt{<not supported>} at runtime. Thunderbird (Arm Neoverse N1) lists six: \texttt{L1-dcache-stores} is absent, and \texttt{LLC-loads}/\texttt{LLC-load-misses} are listed but unsupported, since this PMU has no LLC-scoped event to alias them to.

\subsubsection{Counter Evidence at Boundaries}
L1 miss rate confirms the L1 boundary on all three machines: Sunbird 0.61\% to 12.59\%, Skylark 0.44\% to 9.05\%, Thunderbird 0.18\% to 2.81\%, all at the L1$\to$L2 transition. The LLC boundary is confirmed by LLC-scope miss rate on Sunbird (0.08\% to 11.25\%) and by the generic cache-miss group on Skylark (11.3\% to 46.6\%, since \texttt{LLC-loads} is unsupported there). Thunderbird has no usable last-level-miss signal at all: \texttt{LLC-loads} is unsupported, and a supplementary sweep of raw L3 events showed no capacity-dependent structure.

\subsubsection{Timing Inference vs. PMU Evidence vs. Published Value}

\begin{table}[h]
\centering
\tiny
\begin{tabular}{@{}p{2.0cm}p{2.6cm}p{2.0cm}p{2.8cm}p{1.6cm}p{3.0cm}@{}}
\toprule
Machine/Level & Timing Inference & PMU Evidence & Published/System & Source & Agreement \\
\midrule
Sunbird L1D & 32,768\,B, 8w, 64\,B, 64s, 10.35t & L1 miss 0.61\% & 32\,KiB 8w 64\,B 4cyc; sys match & Fog T10.2 & Match; latency 2.6x high \\
Sunbird L2 & 262,144\,B, 8w (guess), 64\,B, 512s, 26.83t & L1 miss 12.59\% & 256\,KiB 8w 64\,B 12cyc; sys match & Fog T10.2 & Match; guess confirmed \\
Sunbird LLC & $\sim$30\,MiB, 9w (guess), 64\,B, 58.42t & LLC miss 11.25\% & 31.46\,MB exact; 20w, 24576s; Fog 2--45\,MB 12--16w 34cyc & Fog T10.2; sysfs & Size match; ways 9 vs 20, resolved to sys \\
\midrule
Skylark L1D & 32,768\,B, 8w, 64\,B, 64s, 6.24t & L1 miss 0.44\% & 32\,KiB 8w 64\,B 4cyc; sys match & Fog T22.3 & Match; latency 1.6x high \\
Skylark L2 & 524,288\,B, 8w (guess), 64\,B, 1024s, 15.38t & L1 miss 9.05\% & 512\,KiB 8w 64\,B 12cyc; sys match & Fog T22.3 & Match; guess confirmed \\
Skylark LLC & 8.39\,MB (underestimate), 8w (guess), 128\,B, 27.26t & Cache miss 46.6\% & 16.78\,MB exact; 16w, 16384s, 64\,B; Fog 4--32\,MB 16--24w 40cyc & Fog T22.3; sysfs & Size confirms underestimate; ways 8 vs 16; line 128 vs 64 unresolved; sharing 2 cores/CCX, not socket \\
\midrule
Thunderbird L1D & 65,536\,B, 4w, 64\,B, 256s, 0.13t & L1 miss 0.20\% & 64\,KiB 4w 64\,B; sys match; no cycle figure & Ampere \S2.3.1; Arm TRM & Exact match \\
Thunderbird L2 & 1.05\,MB (taken as given), 12w (guess), 64\,B, 1365s, 0.30t & L1 miss 7.72\% & 1\,MiB 8w 64\,B 2048s; sys match & Ampere \S2.4; Arm TRM & Ways 12 vs 8, resolved by 3-way convergence \\
Thunderbird LLC & $\sim$30\,MiB, 10w (guess), 128\,B, 24576s, 0.91--1.01t & \texttt{LLC-loads} unsupported; generic 5.50\% (wrong direction) & No sysfs entry; Ampere: 32\,MiB, 16w, shared across 80 cores, mostly exclusive with L2 & Ampere \S2.5 & Size $\sim$94\%; ways 10 vs 16 unresolved; sharing wider than assumed \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Eight-Machine PMU Agreement Summary}

The table above documents the full Timing/PMU/Published comparison for the three machines
where a literature reference (Agner Fog / Ampere's Arm datasheet) was available. PMU
verification (\texttt{scripts/run\_pmu\_verification.sh}, \texttt{perf stat}, plus
\texttt{lscpu --caches}/sysfs system-reported values) was subsequently run on all eight lab
machines; Table~\ref{tab:phase2-summary} consolidates the size/associativity agreement
verdict for every machine and level (full per-machine reasoning and raw PMU data are in each
machine's own \texttt{data\_processed/<machine>/PHASE2\_VALIDATION\_TABLE.md} and
Section~\ref{sec:permachine}'s final tables).

\begin{table}[H]
\centering\scriptsize
\caption{Phase I (timing) vs.\ Phase II (PMU/system-report) size and associativity agreement, all 8 lab machines. \cmark\ = exact match, \xmark\ = disagreement (Phase II value shown).}
\label{tab:phase2-summary}
\begin{tabular}{|l|l|l|l|l|l|l|}
\hline
Machine & L1D size & L1D assoc. & L2 assoc. & LLC size & LLC assoc. & Notes\\
\hline
Sunbird & \cmark & \cmark & \cmark & \cmark & \xmark\ (20w) & \\
Charnwood & \cmark & \cmark & \xmark\ (4w) & \cmark & \xmark\ (16w) & \\
Ookay & \cmark & \cmark & \xmark\ (4w) & \cmark & \xmark\ (16w) & \\
Upgrade & \cmark & \cmark & \xmark\ (4w) & \cmark & \xmark\ (16w) & \\
Crux & \cmark & \cmark & \xmark\ (4w) & \xmark\ (1.5x) & \xmark\ (12w) & \\
Skylark & \cmark & \cmark & \cmark & \xmark\ (2x) & \xmark\ (16w) & LLC line 128\,B vs.\ 64\,B unresolved\\
Thunderbird & \cmark & \cmark & \xmark\ (8w) & (no sys.\ entry; lit.\ $\approx$94\%) & (lit.\ 16w) & ARM, no sysfs cache tree exposed\\
Artemisia & \cmark & \cmark & \cmark & \xmark\ (1.75x) & \xmark\ (15w, close) & Largest LLC size gap on the team\\
\hline
\end{tabular}
\end{table}

\textbf{Headline finding: L1D is the one cache level this entire project measured
essentially perfectly via timing alone.} Size, associativity, derived sets, and line size
all matched Phase II ground truth on all 8 of 8 machines, zero exceptions -- the strongest
validation of the timing-only method anywhere in this project. L2 associativity's
confound-blocked "match L1" guess (Section~\ref{sec:assoc}) was right on only 3 of 8
machines (Sunbird, Skylark, Artemisia); LLC associativity was right on just 1 of 8
(Artemisia, and even there only approximately, 16 guessed vs.\ 15 actual). LLC \emph{size}
itself (not just associativity) was also wrong on 3 machines -- Crux (1.5x), Skylark (2x,
already flagged pre-verification as a likely underestimate), and Artemisia (1.75x) -- in
each case because that machine's own Phase-I capacity sweep never found a clean plateau at
the value ultimately used.

\subsubsection{Disagreement Analysis}
Every associativity guess Phase I made above L1 disagreed with the system or literature value once checked (Sunbird's LLC: 9-way guessed vs. 20-way actual; Skylark's LLC: 8 vs. 16; Thunderbird's L2: 12 vs. 8), consistent with a shared timing confound rather than a real per-level effect -- and, per Table~\ref{tab:phase2-summary}, this pattern holds across the full fleet, not just these three machines: 5 of 8 L2 guesses and 7 of 8 LLC guesses disagreed with ground truth once Phase II could check them. Every one of these was already flagged in Section~\ref{sec:assoc} as a low-confidence "effective bound," so the disagreement rate confirms rather than contradicts that self-assessment -- the confound was real, not a false alarm. Sharing scope also surprised in both directions: Thunderbird's SLC is shared across all 80 cores rather than per socket, while Skylark's LLC is shared by only two cores per CCX rather than the whole socket -- neither is guessable from timing alone, and both were confirmed only by Phase II's \texttt{shared\_cpu\_list}/system-report data. Thunderbird's inclusion result is the one place a vendor document directly contradicts Phase I: the datasheet calls the SLC mostly exclusive with L2, but the timing classifier leaned inclusive there, though that result was already flagged low-confidence. Skylark's LLC line size (128\,B measured vs. 64\,B published) remains unresolved, most likely an artifact of Zen 2's prefetcher rather than a real 128\,B line. LLC \emph{capacity} itself was also wrong on 3 of 8 machines (Crux, Skylark, Artemisia) -- a genuine Phase-I measurement gap (none of the three found a clean plateau at the value used), not merely an associativity-confound symptom.

\Needspace{0.6\textheight}
\section{Eight Performance Counters Across Generations}

% Requires: \usepackage{booktabs}, \usepackage[T1]{fontenc}
\begin{table}[htbp]
\centering
\small
\begin{tabular}{@{} c p{4.3cm} p{9cm} @{}}
\toprule
\textbf{\#} & \textbf{Category / event name} & \textbf{Why it reflects cache theory} \\
\midrule

1 & Cache references \newline \texttt{cache-references} (same on all 3 archs) &
Denominator for a miss rate; total traffic reaching the cache hierarchy, the baseline every ratio below is measured against. \\

2 & Cache misses \newline \texttt{cache-misses} (same on all 3 archs) &
Hardware analog of the ticks/access knees from Phase I timing; should jump exactly where working-set size crosses a real capacity boundary. \\

3 & L1 data-cache accesses/loads \newline \texttt{L1-dcache-loads} (same on all 3 archs) &
Access-count baseline for the fastest, most temporally-local tier, used to normalize \#4 into a rate. \\

4 & L1 data-cache misses \newline \texttt{L1-dcache-load-misses} (same on all 3 archs) &
Direct test of locality theory: near-zero for an L1-resident chase, rising sharply once footprint exceeds L1, the counter-based confirmation of that boundary. \\

5 & L1 data-cache stores/replacements \newline Intel/AMD: \texttt{L1-dcache-stores}; ARM: \texttt{l1d\_cache\_wb} (raw; no store alias exists) &
Store traffic and eviction/writeback counts reflect how dirty lines cycle out of L1 as the chase touches new sets, which is exactly what the ``replacements'' half of the category measures. \\

6 & LLC accesses/loads \newline Intel: \texttt{LLC-loads}; AMD: inaccessible (\texttt{perf\_event\_paranoid=2} blocks the uncore PMU); ARM: \texttt{l3d\_cache} (raw) &
Traffic reaching the last level before DRAM; theoretically should stay flat once footprint exceeds L1/L2, until it itself is exceeded. \\

7 & LLC misses \newline Intel: \texttt{LLC-load-misses}; AMD: same restriction as \#6; ARM: \texttt{l3d\_cache\_refill} (raw) &
Should jump sharply once footprint exceeds LLC capacity, the counter-based confirmation of the inferred LLC boundary, mirroring \#2/\#4's logic one level up. \\

8 & dTLB misses (memory-hierarchy event) \newline \texttt{dTLB-load-misses} (same on all 3 archs) &
Tests the standing DTLB-confound hypothesis from the associativity work. If it saturates at the same footprint where the timing method's spurious num\_ways $\approx$ 9--10 wall appeared, that is hardware corroboration, not just an inferred guess. \\

\bottomrule
\end{tabular}
\caption{Eight performance events chosen for Section 8.4.}
\label{tab:8_4_events}
\end{table}

\subsection{Standardized Cross-Machine Benchmarks}
All eight events were collected on all eight ECE machines using \texttt{scripts/run\_standardized\_benchmarks.sh}, running the three standardized benchmarks (L1-resident, LLC-sized random, beyond-LLC) at each machine's own confirmed L1/LLC footprint. Two machines have structurally reduced coverage: AMD Zen 2 (Skylark) does not expose \texttt{L1-dcache-stores} or \texttt{LLC-loads/LLC-load-misses} at all in \texttt{perf list}, and ARM Neoverse N1 (Thunderbird) lacks the same two categories under \texttt{armv8\_pmuv3}. This is documented per machine rather than backfilled with a substitute.

\subsection{Normalization}
Miss rates (cache, L1, LLC) are self-normalized ratios of misses to their own reference count, so no further scaling is applied. dTLB misses are reported per 1{,}000 accesses (raw count divided by 1000, since every run fixes samples\_achieved at 1{,}000{,}000). Latency uses perf's \texttt{ns\_per\_access\_perf} rather than raw ticks, since raw tick counts are not comparable across machines with different counter frequencies (Thunderbird's 25 MHz \texttt{CNTVCT\_EL0} versus every x86 machine's multi-GHz TSC).

\subsection{Ranked Summary}

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{eightcounters_latency_scurve.png}
\caption{Per-access latency (ns), ranked ascending within each benchmark, colored by vendor.}
\label{fig:latency_scurve}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=\textwidth]{eightcounters_dtlb_miss_scurve.png}
    \caption{dTLB misses per 1{,}000 accesses, ranked ascending within each benchmark, colored by vendor.}
    \label{fig:placeholder}
\end{figure}

\begin{table}[htbp]
\centering
\small
\begin{tabular}{@{} l r r r r @{}}
\toprule
Machine (vendor, microarch) & ns/access & L1 miss \% & LLC miss \% & dTLB/1000 \\
\midrule
Crux (Intel, Coffee Lake) & 3.31 & 1.05 & 5.76 & 1.35 \\
Ookay (Intel, Kaby Lake) & 3.71 & 0.96 & 11.04 & 1.84 \\
Upgrade (Intel, Coffee Lake) & 3.95 & 0.94 & 8.82 & 1.81 \\
Charnwood (Intel, Sky Lake) & 4.27 & 1.08 & 14.00 & 2.03 \\
Skylark (AMD, Zen 2) & 5.33 & 0.44 & n/a & 0.69 \\
Thunderbird (Ampere/ARM, N1) & 5.84 & 0.19 & n/a & 1.56 \\
Artemisia (Intel, Sapphire Rapids) & 6.27 & 0.61 & 8.69 & 1.69 \\
Sunbird (Intel, Haswell) & 10.19 & 0.63 & 2.71 & 0.51 \\
\bottomrule
\end{tabular}
\caption{L1-resident benchmark, ranked ascending by latency.}
\label{tab:l1_resident}
\end{table}

\begin{table}[htbp]
\centering
\small
\begin{tabular}{@{} l r r r r @{}}
\toprule
Machine (vendor, microarch) & ns/access & L1 miss \% & LLC miss \% & dTLB/1000 \\
\midrule
Skylark (AMD, Zen 2) & 74.04 & 6.13 & n/a & 2.91 \\
Crux (Intel, Coffee Lake) & 88.03 & 8.80 & 3.59 & 1080.28 \\
Ookay (Intel, Kaby Lake) & 136.89 & 8.79 & 16.33 & 1091.71 \\
Charnwood (Intel, Sky Lake) & 174.84 & 8.83 & 15.92 & 1096.98 \\
Upgrade (Intel, Coffee Lake) & 203.70 & 9.52 & 18.75 & 3091.93 \\
Sunbird (Intel, Haswell) & 542.14 & 7.54 & 7.25 & 0.92 \\
Artemisia (Intel, Sapphire Rapids) & 1183.75 & 8.76 & 30.68 & 11084.29 \\
Thunderbird (Ampere/ARM, N1) & 1296.89 & 5.46 & n/a & 1638.88 \\
\bottomrule
\end{tabular}
\caption{LLC-sized random benchmark, ranked ascending by latency.}
\label{tab:llc_random}
\end{table}

\begin{table}[htbp]
\centering
\small
\begin{tabular}{@{} l r r r r @{}}
\toprule
Machine (vendor, microarch) & ns/access & L1 miss \% & LLC miss \% & dTLB/1000 \\
\midrule
Crux (Intel, Coffee Lake) & 19530.32 & 13.42 & 46.75 & 254971.01 \\
Upgrade (Intel, Coffee Lake) & 19788.38 & 13.36 & 47.05 & 256121.64 \\
Ookay (Intel, Kaby Lake) & 19847.98 & 13.36 & 47.79 & 252964.81 \\
Thunderbird (Ampere/ARM, N1) & 25154.25 & 5.34 & n/a & 255722.15 \\
Charnwood (Intel, Sky Lake) & 28545.55 & 13.42 & 47.53 & 253582.41 \\
Skylark (AMD, Zen 2) & 29989.45 & 5.52 & n/a & 10.90 \\
Artemisia (Intel, Sapphire Rapids) & 35782.53 & 11.21 & 76.72 & 251331.33 \\
Sunbird (Intel, Haswell) & 38588.18 & 12.94 & 49.48 & 210666.08 \\
\bottomrule
\end{tabular}
\caption{Beyond-LLC benchmark, ranked ascending by latency.}
\label{tab:beyond_llc}
\end{table}

\subsection{Cross-Vendor and Cross-Generation Comparison}
LLC-scope counters and L1 store counts are not comparable across all three vendors: both are absent on AMD and ARM, leaving only the six Intel machines with that data. The generic \texttt{cache-references}/\texttt{cache-misses} pair is present everywhere but is not semantically equivalent across PMUs either, since it has already been shown to track L1-scope traffic almost exactly on Thunderbird's ARM PMU rather than genuine last-level activity, so raw comparisons of that pair across vendors would be misleading. dTLB-load-misses is the most portable of the eight events, with consistent semantics on all three vendors.

Skylark (AMD) shows a striking and reproducible dTLB anomaly: at LLC-sized and beyond-LLC footprints its dTLB miss rate is two to four orders of magnitude lower than every other machine at the same tier (2.91 and 10.90 per 1{,}000 accesses versus 1000+ and 250{,}000+ elsewhere), suggesting a real architectural difference in TLB reach or page management rather than measurement noise, since it is consistent across both larger benchmarks. Within Intel alone, the oldest machine (Sunbird, Haswell, 2014) shows the highest L1-resident and beyond-LLC latency of the Intel group, while the newest (Artemisia, Sapphire Rapids, 2023) is a partial outlier with the highest LLC-random latency and dTLB miss count among Intel machines, most likely reflecting its much larger multi-socket server die rather than a simple generational speedup, so raw latency does not track chip age monotonically once die size and topology differ this much.

\section{Software-Only Cache Metric}
\label{sec:hitrate}

The required deliverable is a timing-derived cache hit-rate estimator,
$\widehat{H} = N_{\text{classified hits}} / N_{\text{timed accesses}}$, that never reads a
PMU counter. \textbf{What is varied:} which workload's hit rate is being estimated
(\texttt{test\_bytes}); the calibration classes themselves are fixed, self-generated
reference distributions, not swept. \textbf{What is held constant:} the calibration
procedure runs fresh on every invocation (self-calibrating, per the Competition rule that
the estimator must generalize to a held-out machine, Section~\ref{sec:hazel}).
\textbf{What is measured:} a single scalar $\widehat{H} \in [0,1]$ with a bootstrap
confidence interval. Implementation: \texttt{main\_code/software\_hit\_rate/} (portable
common code, both x86-64 and AArch64, via the same dispatch as every other experiment,
Listing~\ref{lst:maindispatch}).

\textbf{Method, in three stages} (full docstring: \texttt{software\_hit\_rate.h}):
\begin{enumerate}
    \item \textbf{Calibration.} Build two pointer-chase cycles with the same primitives as
    every other experiment (Listing~\ref{lst:pchase},~\ref{lst:random}): a small
    \texttt{resident} buffer, warmed until fully cache-resident, and a large
    \texttt{nonresident} buffer far past any real LLC -- both timed single-shot
    (Section~\ref{sec:misslatency}'s primitive) so they carry the same fixed overhead and
    remain directly comparable.
    \item \textbf{Classification.} A single threshold $\tau$ is chosen from the merged,
    sorted calibration samples by an ROC sweep maximizing Youden's $J = \text{TPR} -
    \text{FPR}$ -- the data itself picks $\tau$, not a hand-tuned constant. The raw
    classified-hit fraction on the test stream, $p_{\text{obs}}$, is debiased via the
    Rogan--Gladen prevalence-correction estimator using the classifier's own measured
    sensitivity/specificity:
    \[ \widehat{H} = \frac{p_{\text{obs}} + \text{Sp} - 1}{\text{Se} + \text{Sp} - 1},
    \quad \text{clamped to } [0,1]. \]
    \item \textbf{Uncertainty.} A nonparametric bootstrap (resampling all three empirical
    distributions with replacement, re-deriving $\tau$/Se/Sp/$\widehat{H}$ each replicate)
    reports a [2.5, 97.5] percentile confidence interval, computed by the program itself.
\end{enumerate}

\begin{lstlisting}[caption={Software-only cache hit-rate estimator: threshold selection (Youden's J), Rogan--Gladen debiasing, and the test-workload classification/output tail. Target property: timing-derived cache hit rate $\widehat{H}$. Architecture: portable common code. File: \texttt{main\_code/software\_hit\_rate/software\_hit\_rate.c}.},label={lst:hitrate}]
static double select_threshold(const double *resident, uint64_t n_resident,
                                const double *nonresident, uint64_t n_nonresident,
                                double *out_se, double *out_sp)
{
    /* Merge+sort both calibration classes, sweep every candidate cut point,
     * keep the tau maximizing TPR(tau) - FPR(tau) (Youden's J). Ties at the
     * same tick value are folded together before evaluating TPR/FPR. Full
     * body: main_code/software_hit_rate/software_hit_rate.c lines 59-109. */
    /* returns: best_tau; *out_se = TPR at best_tau; *out_sp = 1 - FPR at best_tau */
}

/* Rogan-Gladen prevalence-correction: debiases a binary classifier's naive
 * positive rate into a calibrated rate estimate from its own Se/Sp. */
static double rogan_gladen(double p_obs, double se, double sp)
{
    double denom = se + sp - 1.0;
    if (denom < 1e-9) denom = 1e-9;
    double h = (p_obs + sp - 1.0) / denom;
    if (h < 0.0) h = 0.0;
    if (h > 1.0) h = 1.0;
    return h;
}

/* ---- excerpt from run_hit_rate_experiment(): test-workload classification,
 * Hhat computation, and result output (tau/se/sp already frozen above) ---- */
uint64_t hits = 0;
for (uint64_t i = 0; i < cfg->test_samples; i++) {
    if (test_ticks[i] <= tau) {
        hits++;
    }
}
double p_obs = (double)hits / (double)cfg->test_samples;
double hhat = rogan_gladen(p_obs, se, sp);

double boot_mean, boot_std, ci_lo, ci_hi;
bootstrap_ci(resident_ticks, calib_samples_used, nonresident_ticks, calib_samples_used,
             test_ticks, cfg->test_samples, cfg->bootstrap_reps, cfg->seed + 3u,
             &boot_mean, &boot_std, &ci_lo, &ci_hi);

printf("# result tau_ticks=%.4f sensitivity=%.4f specificity=%.4f p_obs=%.4f "
       "Hhat=%.4f ci_lower=%.4f ci_upper=%.4f bootstrap_mean=%.4f bootstrap_std=%.4f "
       "bootstrap_reps=%" PRIu64 "\n",
       tau, se, sp, p_obs, hhat, ci_lo, ci_hi, boot_mean, boot_std, cfg->bootstrap_reps);
\end{lstlisting}

\begin{figure}[htbp]
\centering
\fbox{\parbox[c][2.1in][c]{0.85\textwidth}{\centering
PLACEHOLDER-style methodology-logic flow chart for the software-only cache metric:\\[4pt]
build resident \& nonresident calibration chains $\rightarrow$ single-shot time both
$\rightarrow$ ROC sweep picks threshold $\tau$ maximizing Youden's J (records Se/Sp)
$\rightarrow$ time the TEST workload the same way $\rightarrow$ classify each test access
hit/miss at $\tau$ $\rightarrow$ debias the raw hit fraction via Rogan--Gladen using Se/Sp
$\rightarrow$ bootstrap CI $\rightarrow$ report $\widehat{H}$.
}}
\caption{Methodology logic for the software-only cache hit-rate metric. Measured quantity:
single-shot ticks/access for two calibration classes and the test workload. Expected
signature: two well-separated calibration distributions (single-digit-tick hits vs.\
hundreds-of-ticks misses); Se/Sp near 1 confirm the separation. Inference rule: the
Rogan--Gladen-debiased classified-hit fraction on the test stream is the reported
$\widehat{H}$, valid only insofar as Se/Sp (reported alongside it) are high.}
\label{fig:hitrate-flow}
\end{figure}

\subsection{Calibration Quality}
Every one of the 8 lab machines' calibration runs achieved sensitivity and specificity
above 0.99 at every tested footprint (e.g.\ Sunbird: Se~$\approx$0.991--0.993,
Sp~$\approx$0.9997--1.0000 across L1/L2/LLC/DRAM footprints) -- the two calibration classes
are cleanly separated everywhere, confirming the single-threshold design assumption
documented in \texttt{software\_hit\_rate.h} rather than merely asserting it.

\subsection{Cross-Machine Results at a Standardized Workload}
Per the assignment's chronological-plot requirement (item 14), all 8 machines were swept
over an identical set of absolute byte sizes; 262,144\,B (256\,KiB) was chosen as the
single cross-machine comparison point because it sits above every machine's L1 (32--65\,KiB
on this team) without depending on any machine-specific L2/LLC boundary.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{chrono_14_software_hit_rate.png}
\caption{Timing-derived cache hit rate $\widehat{H}$ at a standardized 256\,KiB workload,
all 8 lab machines, vs.\ processor introduction year.}
\label{fig:chrono14}
\end{figure}

\begin{table}[H]
\centering\small
\caption{$\widehat{H}$ at the standardized 256\,KiB workload, all 8 lab machines.}
\begin{tabular}{|l|l|l|}
\hline
Machine & $\widehat{H}$ @ 256\,KiB & 95\% CI\\
\hline
Sunbird & 0.7404 & 0.7363--0.7446\\
Charnwood & 1.0000 & 1.0000--1.0000\\
Ookay & 0.9613 & 0.9321--0.9631\\
Upgrade & 0.9992 & 0.9512--1.0000\\
Crux & 0.9824 & 0.8951--0.9928\\
Skylark & 0.9958 & 0.9942--0.9974\\
Thunderbird & 1.0000 & 1.0000--1.0000\\
Artemisia & 1.0000 & 0.9931--1.0000\\
\hline
\end{tabular}
\end{table}

\textbf{Sunbird is the one clear, statistically real outlier} (a tight CI that does not
overlap any other machine's) -- a genuine conflict/associativity-edge effect: a random
cyclic address stream sized near its own L1 capacity does not evenly fill every set, and
256\,KiB happens to sit close enough to Sunbird's own 32,768\,B L1 boundary (8x) to inherit
that edge effect. Every other machine reads at or within measurement noise of 1.0. This
metric does \textbf{not} show a clean chronological trend at this workload size -- the
expected, honest result given 256\,KiB is small relative to every machine's own L1+L2, not
evidence the estimator is broken. Thunderbird's CI is degenerate (exactly
1.0000--1.0000) because its 25\,MHz \texttt{CNTVCT\_EL0} quantizes the self-calibrated
threshold to $\tau=1.0000$\,ticks, a machine-specific timer-resolution artifact, not a
claim of literally zero uncertainty.

\subsection{PMU Validation and Where the Estimator Fails}
\label{sec:hitrate-pmu}

\texttt{scripts/run\_hit\_rate\_pmu\_validation.sh} runs \texttt{cache\_bench} twice per
measurement -- once plain (self-calibrating, to obtain $\tau$/Se/Sp) and once wrapped in
\texttt{perf stat} with that calibration frozen (\texttt{has\_fixed\_calibration}), so the
PMU counters are scoped to only the test workload and not the large \texttt{nonresident}
calibration buffer's own necessary DRAM traffic (Section~\ref{sec:hitrate}'s
\texttt{software\_hit\_rate.h} docstring documents why the naive whole-process comparison is
meaningless: calibration traffic alone was observed to make $H_{\text{PMU}}$ read
$\approx$0.51 at an L1-resident test footprint where the true miss rate is under 1.5\%).
$H_{\text{PMU}} = 1 - \texttt{cache-misses}/\texttt{cache-references}$ over the
perf-wrapped, test-only invocation, 3 seeded repeats per level.

\begin{table}[H]
\centering\scriptsize
\caption{Median $\widehat{H}$ vs.\ PMU-derived $H_{\text{PMU}}$, absolute error, and relative error (\%), by footprint class, 8 lab machines (3 repeats/cell).}
\begin{tabular}{|l|r|r|r|r|r|r|r|r|}
\hline
 & \multicolumn{2}{c|}{L1} & \multicolumn{2}{c|}{L2} & \multicolumn{2}{c|}{LLC} & \multicolumn{2}{c|}{DRAM}\\
Machine & $\widehat{H}$ & rel.err\% & $\widehat{H}$ & rel.err\% & $\widehat{H}$ & rel.err\% & $\widehat{H}$ & rel.err\%\\
\hline
Sunbird & 0.768 & 30.1 & 0.851 & 12.4 & 0.013 & 98.7 & 0.0001 & 99.9\\
Crux & 0.902 & 10.8 & 0.941 & 6.3 & 0.752 & 23.1 & 0.0001 & 100.0\\
Skylark & 0.676 & 48.0 & 0.999 & 0.8 & 0.878 & 57.9 & 0.0001 & 100.0\\
Upgrade & 0.901 & 11.0 & 0.954 & 4.1 & 0.407 & 58.1 & 0.0002 & 100.0\\
Charnwood & 0.859 & 16.4 & 0.880 & 13.7 & 1.000 & 3.7 & 0.0000 & 100.0\\
Ookay & 0.856 & 16.8 & 0.866 & 14.9 & 0.789 & 18.7 & 0.0002 & 100.0\\
Thunderbird & 0.999 & 0.1 & 0.976 & 6.4 & 0.362 & 61.7 & 0.0000 & 100.0\\
Artemisia & 0.783 & 27.8 & 0.716 & 20.4 & 0.006 & 99.3 & 0.0001 & 100.0\\
\hline
\end{tabular}
\end{table}

\textbf{Absolute/relative error, quantified as required (Section 8.5, item 4):} across all 8
machines, L1 median relative error is 0.1--48.0\% (mean $\approx$20\%), L2 is 0.8--20.4\%
(mean $\approx$10\%) -- the estimator's core design goal, distinguishing cache-resident from
non-resident, is met at these tiers on every machine. \textbf{The estimator systematically
fails at LLC and DRAM footprints}: LLC relative error ranges 3.7--99.3\% (median
$\approx$58\%) and DRAM relative error is 99.9--100\% on every single machine.
\textbf{Why it fails, explained rather than just reported:} the classifier uses one global
threshold $\tau$ calibrated against only two classes (L1-resident "hit" vs.\ far-DRAM
"miss"). An LLC-resident access is genuinely served by the cache (PMU: $H_{\text{PMU}}
\approx$0.90--0.98 at most machines' LLC footprint) but its latency sits well above $\tau$
(calibrated to separate L1 speed from DRAM speed), so it is classified a "miss" almost every
time -- exactly the single-threshold limitation \texttt{software\_hit\_rate.h} documents in
advance, not a bug discovered after the fact. At the DRAM footprint both $\widehat{H}$ and
$H_{\text{PMU}}$ agree the true hit rate is near zero in absolute terms, but the relative-error
metric still reads $\approx$100\% because $H_{\text{PMU}}$ itself is never exactly 0 (residual
prefetch/row-buffer/TLB-adjacent hits), making relative error a poor summary at this one
extreme and absolute error (always $<$0.001) the more honest number there. Charnwood's LLC
cell (3.7\% error, the one broad success outside L1/L2) is the exception that proves the rule:
its $\tau$ happened to sit close enough to its own LLC-resident latency that the single
threshold accidentally still classified LLC hits correctly. \textbf{Practical implication:}
this estimator, as specified, is best read as an "L1/L2-scale residency detector," not a
general any-level cache hit-rate meter -- a defensible and disclosed limitation, not a design
flaw hidden from the reader.

\section{Chronological Cache Evolution Analysis}
\label{sec:evolution}

This section combines all eight lab machines' Phase-I timing-only results
(Section~\ref{sec:permachine}) into one chronological dataset, sorted by processor
introduction year (Section~\ref{sec:platforms}'s year convention: the first public
introduction year of the specific microarchitecture/processor generation, not purchase
date). Full backing data:
\texttt{data\_processed/master/chronological\_master\_table.csv}; full reconciled table
with every column: \texttt{CHRONOLOGICAL\_MASTER\_TABLE.md}. Plots are produced by
\texttt{scripts/plot\_chronological\_master.py} from that CSV.

\subsection{Chronological Master Table}

\begin{table}[H]
\centering\tiny
\caption{Chronological master table (condensed) -- L1D row per machine, oldest to newest. Ways column shows Phase I $\to$ PMU where they disagree.}
\begin{tabular}{|l|l|l|l|l|l|l|l|l|l|}
\hline
Year & Vendor/ISA & Microarch & Machine/CPU & Node & L1D size & Ways & Sets & Line & Hit lat.\ (ticks/ns)\\
\hline
2014 & Intel x86-64 & Haswell & 2x Xeon E5-2680 v3 (Sunbird) & 22\,nm & 32,768\,B & 8 & 64 & 64\,B & 10.35 / 4.14\\
2015 & Intel x86-64 & Skylake & Core i7-6700 (Charnwood) & 14\,nm & 32,768\,B & 8 & 64 & 64\,B & 7.98 / 2.35\\
2017 & Intel x86-64 & Kaby Lake & Core i7-7700 (Ookay) & 14\,nm & 32,768\,B & 8 & 64 & 64\,B & 7.49 / 2.08\\
2017 & Intel x86-64 & Coffee Lake & Core i7-8700 (Upgrade) & 14\,nm & 32,768\,B & 8 & 64 & 64\,B & 6.17 / 1.93\\
2019 & Intel x86-64 & Coffee Lake Refresh & Core i7-9700 (Crux) & 14\,nm & 32,768\,B & 8 & 64 & 64\,B & 7.42 / 2.47\\
2019 & AMD x86-64 & Zen 2 & 2x EPYC 7532 (Skylark) & 7\,nm & 32,768\,B & 8 & 64 & 64\,B & 6.24 / 2.60\\
2020 & Ampere AArch64 & Neoverse N1 & Q80-30 (Thunderbird) & 7\,nm & 65,536\,B & 4 & 256 & 64\,B & 0.130 / 5.0\\
2023 & Intel x86-64 & Sapphire Rapids & 2x Xeon Gold 5420+ (Artemisia) & 10\,nm/Intel 7 & 49,152\,B & 12 & 64 & 64\,B & 9.87 / 4.94\\
\hline
\end{tabular}
\end{table}

L2/LLC/DRAM columns (including the many Phase I $\to$ PMU disagreements documented in
Table~\ref{tab:phase2-summary}) and the full inclusion/exclusion and software-hit-rate
columns are in \texttt{CHRONOLOGICAL\_MASTER\_TABLE.md} and each machine's own final table
(Section~\ref{sec:permachine}); repeating all $\sim$15 columns here would duplicate that
data rather than adding new information.

\subsection{Required Moore-Style Plot Set}

\begin{figure}[H]\centering\includegraphics[width=0.85\textwidth]{chrono_01_l1_capacity.png}\caption{L1D capacity vs.\ year.}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.85\textwidth]{chrono_02_l1_associativity.png}\caption{L1D associativity vs.\ year.}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.85\textwidth]{chrono_03_l1_hit_latency.png}\caption{L1D hit latency vs.\ year.}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.85\textwidth]{chrono_04_l1_miss_penalty.png}\caption{L1 miss penalty (L1$\to$L2) vs.\ year.}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.85\textwidth]{chrono_05_l2_capacity.png}\caption{L2 capacity per core vs.\ year.}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.85\textwidth]{chrono_06_l2_associativity.png}\caption{L2 associativity vs.\ year (Phase I).}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.85\textwidth]{chrono_07_l2_hit_latency.png}\caption{L2 hit latency vs.\ year.}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.85\textwidth]{chrono_08_l2_miss_penalty.png}\caption{L2 miss penalty (L2$\to$LLC) vs.\ year.}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.85\textwidth]{chrono_09a_llc_capacity_domain.png}\caption{LLC/L3 capacity (sharing-domain total) vs.\ year.}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.85\textwidth]{chrono_09b_llc_capacity_per_core.png}\caption{LLC/L3 capacity, MiB/core in sharing domain, vs.\ year.}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.85\textwidth]{chrono_10_llc_associativity.png}\caption{LLC/effective associativity vs.\ year (Phase I).}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.85\textwidth]{chrono_11_llc_latency_and_miss_penalty.png}\caption{LLC hit latency and LLC$\to$memory miss penalty vs.\ year.}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.85\textwidth]{chrono_12_line_size.png}\caption{Cache line/block size vs.\ year, all levels.}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.85\textwidth]{chrono_13_inclusion_exclusion.png}\caption{Inclusion/exclusion behavior vs.\ year (categorical/step-style summary).}\label{fig:chrono13}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.85\textwidth]{chrono_15_pmu_normalized_metrics.png}\caption{Two PMU-derived normalized metrics vs.\ year (L1 miss rate sanity check; generic cache-miss rate at the LLC footprint).}\end{figure}
Figure~\ref{fig:chrono14} (Section~\ref{sec:hitrate}) provides the 14th required plot, the
timing-derived hit-rate metric vs.\ year.

\subsection{Interpretation}

\begin{enumerate}
    \item \textbf{Approximately constant quantities.} Line size: 64\,B on 8 of 8 machines,
    zero exceptions across 2014--2023 and 3 vendors/ISAs -- the single most consistent
    measurement in the whole project. L1D capacity and associativity are constant on 6 of 8
    machines (32,768\,B / 8-way); the two deviations (Thunderbird, Artemisia) are this
    project's one Arm machine and its newest/largest-L1D Intel Xeon respectively, and do not
    themselves show a chronological trend (Thunderbird's larger L1 is \emph{older},
    2020, than Artemisia's, 2023, and the two deviate in opposite directions and magnitudes).
    \item \textbf{Growing quantities and their shape.} L2 capacity per core is the clearest
    grower: flat at 256\,KiB for 5 years (2014--2019, four machines), then three back-to-back
    doublings in four years (512\,KiB Skylark 2019 $\to$ 1\,MiB Thunderbird 2020 $\to$
    2\,MiB Artemisia 2023) -- \textbf{stepwise, not smooth or linear}, closer in spirit to
    Moore's own log$_2$-count plot than to a continuous curve. LLC capacity also grows, but
    far less cleanly: it tracks server-vs-desktop class more than generation (Sunbird and
    Artemisia, both multi-socket server parts, have the two largest LLCs on the team despite
    being the oldest and newest machines respectively; the five desktop-class machines cluster
    at 8--12\,MiB regardless of a 2015--2019 spread in year).
    \item \textbf{Latency vs.\ capacity tradeoff.} LLC hit latency does not track LLC capacity
    monotonically: Upgrade's 12\,MiB LLC (155\,ticks hit latency) is slower than Sunbird's
    2.5x-larger 30\,MiB LLC (58\,ticks) once PMU-corrected sharing-domain and core-count
    differences are accounted for (Section~\ref{sec:pmu}) -- LLC latency is dominated by
    physical distance/topology within the sharing domain (ring/mesh interconnect, core count)
    at least as much as by raw capacity. L1 miss penalty (the L1$\to$L2 transition) shows no
    clean chronological trend either (67--188 ticks across the fleet, non-monotonic in year),
    consistent with capacity being held constant at L1 (item 1) while surrounding
    microarchitecture (pipeline depth, L2 physical distance) still varies year to year.
    DRAM latency in ticks varies 2.3 (Thunderbird, 25\,MHz timer) to 395 (Charnwood) purely as
    a units artifact; in nanoseconds it is 79--152\,ns with no clean trend, reflecting memory
    technology/NUMA/server-vs-desktop platform differences more than CPU generation
    (Section~\ref{sec:evolution} treats DRAM latency explicitly as a system-level, not
    CPU-only, quantity per the assignment's own instruction).
    \item \textbf{Vendor/ISA/class differences.} L1D is remarkably uniform across all three
    vendors/ISAs (Intel, AMD, Arm) -- the one property that looks architecturally
    "solved," not vendor-specific. LLC sharing scope is the opposite: Thunderbird's SLC
    spans all 80 cores of the SoC (a distributed design unlike any x86 machine on the team),
    Skylark's is 2 cores/CCX (an AMD Rome binning choice), and every Intel machine's is
    per-socket -- none of this is guessable from timing/year alone, only from
    Phase-II \texttt{shared\_cpu\_list} data (Section~\ref{sec:pmu}). Inclusion/exclusion
    behavior (Figure~\ref{fig:chrono13}, Table~\ref{tab:inclusion-crossmachine}) is not
    cleanly vendor-determined either: Sunbird, Charnwood, and Ookay (all Intel) read as an
    inclusive-toward-L1 LLC, while Upgrade and Crux (also Intel, same era) read uniformly
    non-inclusive -- microarchitecture generation within a vendor matters at least as much as
    vendor identity here.
    \item \textbf{Which quantity most resembles Moore's own trend.} L2 capacity per core is
    the best match: a clean, if step-wise rather than smooth, doubling pattern that is
    defensible to fit (Section~\ref{sec:cachelaws}'s Law 1). No other quantity in this dataset
    shows anything close to a clean exponential -- LLC capacity is class-dominated (item 2),
    and every latency quantity is essentially flat or non-monotonic in year (item 3) --
    underscoring that Moore's observation was specific to transistor/component \emph{count},
    not a universal property of every chip-design metric, and this project's own data
    supports that distinction rather than assuming scaling is universal.
    \item \textbf{Doubling time.} L2 capacity's fitted doubling time is $\tau=3$ years
    (Section~\ref{sec:cachelaws}, Law 1), derived end-to-end from the 2014 and 2023
    endpoints (Sunbird $\to$ Artemisia, an exact 8x increase over 9 years). No other measured
    quantity in this dataset supports a defensible doubling-time fit -- L1D/line size are
    constant (item 1, doubling time undefined/infinite) and LLC capacity's growth is
    class-confounded (item 2), not a per-generation rate.
    \item \textbf{Lab-only prediction vs.\ held-out Hazel evaluation.} Deferred to
    Section~\ref{sec:hazel}, which freezes the lab-only prediction (already committed and
    tagged \emph{before} any Hazel cache experiment ran, Section~\ref{sec:cachelaws}) and
    then reveals the held-out Hazel-Haswell measurement against it.
    \item \textbf{Strength of evidence vs.\ Moore's original observation.} Moore's own plot
    (G.\ E.\ Moore, ``Cramming More Components onto Integrated Circuits,'' 1965; reproduced
    in the assignment handout) is 6 points from one process family over 6 years, later
    validated over decades. This project's L2-capacity trend is 8 points, 3
    vendors/ISAs, and both desktop and server classes, over 9 years -- broader
    cross-architecture coverage than Moore's original single-vendor sample, but a shorter
    time span and, per item 2, a curve that is visibly not smooth (5 flat years then a rapid
    burst) rather than steadily exponential throughout. This project's data supports treating
    L2 capacity as trending upward with real (if step-wise) regularity, while explicitly
    \emph{not} claiming the same strength of evidence Moore's decades-long, since-repeatedly-
    confirmed transistor-count trend eventually earned -- a claim this report does not make
    for any of its own findings.
\end{enumerate}

\section{Cache Evolution Prediction}

\subsection{Your Team's Cache Laws and Future Scaling Walls}
\label{sec:cachelaws}

Two named laws are formulated below, using both team members' surnames (Chen, Ngo) per the
assignment's naming convention. Both are fit exclusively from Phase I, timing-only measurements on
the team's 8 ECE lab machines (\texttt{CHRONOLOGICAL\_MASTER\_TABLE.md}; backing data
\texttt{data\_processed/master/chronological\_master\_table.csv}) -- no Hazel data was used to fit
either law. They concern two structurally different cache-related quantities, per the assignment's
requirement: per-core L2 capacity (a growth law) and L1D capacity/associativity/line size (an
approximate-constancy law). Both laws' parameters and their Hazel predictions were frozen together in
\texttt{PREDICTION\_FREEZE.md} at commit \texttt{b7d98c0} (git tag \texttt{predictions-frozen},
2026-09-14 18:23 UTC) \textbf{before} any Hazel cache experiment ran, satisfying this project's Phase
Discipline (\S\ref{sec:phasediscipline}).

\subsubsection*{Law 1: Chen--Ngo Cache Capacity Doubling Law}

\textbf{Statement.} Per-core L2 cache capacity on the team's lab machines grows with processor
introduction year via a step-doubling trend, doubling on average every 3 years across the 2014--2023
span sampled.
\noindent
\textbf{Quantitative rule.} $C(t) = 256\,\text{KiB} \cdot 2^{(t - 2014)/\tau}$, with $\tau = 3$ years,
fit from the dataset's two chronological endpoints (Sunbird, 2014, 256\,KiB $\to$ Artemisia, 2023,
2\,MiB -- an exact $8\times$ increase over 9 years). The underlying data is a step function, not a
smooth curve: flat at 256\,KiB for five years (2014--2019: Sunbird, Charnwood, Ookay, Upgrade, Crux),
then three back-to-back doublings in four years (512\,KiB -- Skylark, 2019; 1\,MiB -- Thunderbird,
2020; 2\,MiB -- Artemisia, 2023). The fitted $\tau$ is an end-to-end summary between two endpoints, not
a per-year validated slope (see \S\ref{sec:evolution}, items 2, 5, 6, for the full derivation).
\noindent
\textbf{Scope/domain.} 8 machines, 2014--2023, pooled across vendor/ISA (6 Intel x86-64, 1 AMD
x86-64 [Skylark], 1 Arm AArch64 [Thunderbird]) and pooled across desktop/server class. Not
vendor-specific: the two fitted endpoints are both Intel, but the two intermediate step points come
from AMD and Arm parts, so the doubling time is not read off a single vendor's roadmap alone. Does not
imply the trend holds beyond 2023, outside this 8-machine sample, or for a machine class not
represented in this fleet.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{chrono_05_l2_capacity.png}
\caption{Evidence plot for the Chen--Ngo Cache Capacity Doubling Law: per-core L2 capacity vs.\
processor introduction year, all 8 lab machines (solid points/line, log$_2$ y-axis) --
the same underlying data as Section~\ref{sec:evolution}'s required L2-capacity plot. The fitted
model $C(t) = 256\,\text{KiB}\cdot 2^{(t-2014)/3}$ is evaluated at Hazel's \texttt{haswell}
target generation ($t \approx 2014$, the model's own lower fit endpoint) rather than
extrapolated forward, per the frozen prediction below.}
\label{fig:law1-plot}
\end{figure}
\noindent
\textbf{Frozen prediction.} Evaluated at Hazel's target \texttt{haswell} generation ($t \approx 2014$,
the model's own lower fit endpoint): \textbf{256\,KiB} per core -- a same-era identity prediction, not
a forward extrapolation, so it carries no extra uncertainty band beyond the step-function caveat
above. Full frozen record: \texttt{PREDICTION\_FREEZE.md}, ``Team Cache Laws -- Frozen Predictions''
table.

\subsubsection*{Law 2: Chen--Ngo Cache Invariance Law}

\textbf{Statement.} L1D cache capacity, associativity, and line size on the team's lab machines are
approximately invariant across a decade of processor generations and three vendors/ISAs, rather than
scaling with year the way L2 capacity does.
\noindent
\textbf{Quantitative rule.} Line size: 64\,B on 8 of 8 machines (2014--2023), zero exceptions. L1D
capacity: 32,768\,B (32\,KiB) on 6 of 8 machines. L1D associativity: 8-way on 6 of 8 machines. The
same two machines account for both deviations -- Thunderbird (Arm, 2020: 64\,KiB, 4-way) and Artemisia
(Intel Sapphire Rapids, 2023: 48\,KiB, 12-way) -- and they deviate in different directions/magnitudes
from each other despite Artemisia being the newer machine, i.e.\ the deviations themselves show no
chronological trend.
\noindent
\textbf{Scope/domain.} Same 8-machine, 2014--2023, cross-vendor/ISA pool as Law 1. The invariance
claim is majority-case (6 of 8), not universal, and is stated as approximate; it is known not to hold
for the team's one Arm machine or its newest/largest-L1D Intel Xeon, and should not be assumed to hold
for machine classes outside this fleet.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{chrono_01_l1_capacity.png}
\caption{Evidence plot for the Chen--Ngo Cache Invariance Law: L1D capacity vs.\ processor
introduction year, all 8 lab machines -- 6 clustered flat at 32\,KiB, with Thunderbird
(2020, 64\,KiB) and Artemisia (2023, 48\,KiB) as the labeled outliers. Line-size and
associativity invariance (the other two legs of this law) are shown in the L1D
associativity and line-size plots in Section~\ref{sec:evolution}.}
\label{fig:law2-plot}
\end{figure}

\noindent
\textbf{Frozen prediction.} At Hazel's \texttt{haswell} target: L1D capacity \textbf{32,768\,B},
associativity \textbf{8-way}, line size \textbf{64\,B} -- chosen because \texttt{haswell} is an x86
Xeon target, the exact class where this law's 6/8 majority held with no exceptions (both deviations
are non-Haswell parts). Full frozen record: \texttt{PREDICTION\_FREEZE.md}.

\subsubsection*{Future Scaling Walls}

\textbf{Wall for Law 1 (capacity doubling).} The 3-year doubling time is fit from a fleet that was flat
for 5 years and then doubled three times in 4 years -- i.e.\ the model is extrapolating a recent burst,
not a long-observed steady rate, and two independent physical constraints could break it going
forward. First, a \emph{latency wall}: \S\ref{sec:evolution} item 3 already shows LLC hit latency
broadly growing with LLC capacity across this fleet; the same wire-delay/capacity tradeoff applies to
L2, so continuing to double L2 capacity every 3 years would eventually push L2 hit latency toward
today's LLC latency range, eroding the two-level (L2/LLC) split's purpose unless vendors insert an
additional mid-level cache -- a redesign, not a continuation of the same curve. Second, a
\emph{fabrication wall}: this project's own data cannot observe process-node effects directly (Phase I
is timing-only), but the fitted doublings land almost entirely in the fleet's more recent, smaller-node
machines (Skylark and Thunderbird at 7\,nm, Artemisia at 10\,nm/Intel~7), while SRAM cell density is
publicly known to scale much more slowly than logic density below roughly 7\,nm -- a hard limit this
project's data is consistent with but cannot independently confirm, flagged here as an external risk
to the extrapolation rather than a lab finding. Third, a \emph{core-count wall}: per-core L2 capacity
competes with core count for the same fixed die-area/power budget (already noted as a risk in
\S\ref{sec:evolution} item 7); if future generations grow core counts faster than die area, per-core L2
capacity could flatten or reverse even while total on-die SRAM keeps growing.

\medskip

\noindent\textbf{Wall for Law 2 (invariance).} Unlike Law 1, this law describes a quantity that appears to have
\emph{already} hit its scaling wall rather than one that will hit it in the future: L1D capacity and
associativity are bounded by a strict load-to-use latency budget (commonly $\leq$4 cycles in modern
designs), and growing L1D physically increases wire delay and tag-comparator fan-in in a way that would
violate that budget -- consistent with why L1D has not grown across 9 years and 3 vendors on 6 of 8
machines even as transistor budgets elsewhere on the same dies have grown by orders of magnitude. The
two deviations show the wall is soft, not absolute: a different microarchitectural latency budget
(Arm's Neoverse~N1 core design; Intel's newest server core) can push through it by roughly
50--100\,\%, but not by the multiplicative factors Law 1's L2 trend shows over the same span. The
practical implication for future generations, including any that show a materially larger L1D than
32\,KiB, is that this project's own timing-only method predicts it should also show a
correspondingly \emph{higher} L1D hit-latency in ticks (not a free capacity increase) -- a testable,
falsifiable consequence of this law that a future Hazel or successor-fleet measurement could check
directly against the frozen 32,768\,B / 8-way / 64\,B prediction above.

The held-out evaluation for both laws (measured Hazel value, error, and supported/weakened/falsified
verdict) is recorded in \texttt{PREDICTION\_FREEZE.md}'s ``Held-Out Evaluation'' table once the Hazel
cache-experiment suite has run; per that file's own rule, Hazel points are never used to refit either
law above.

Item 7's forward-looking capacity prediction (the general Section~\ref{sec:evolution} extrapolation,
distinct from the two named laws above) is derived and stated in full in Section~\ref{sec:evolution}
above, alongside the rest of the Section 9 interpretation items, since it follows directly from the
same doubling-time model (item 6). The held-out evaluation half of item 7 is in
Section~\ref{sec:hazel}, Table~\ref{tab:hazel-heldout}, now that Hazel-Haswell measurements
have been collected.

\section{Hazel HPC Validation}
\label{sec:hazel}

\subsection{Status}
Per Phase III discipline (Section~\ref{sec:phasediscipline}), the lab-only prediction table
was frozen and committed \emph{before} any Hazel cache experiment ran: commit
\texttt{b7d98c0187567ce0a1a60eb2663fc1d5351673c3}, tag \texttt{predictions-frozen},
2026-09-14 18:23 UTC (\texttt{PREDICTION\_FREEZE.md}). Of NC State's Slurm CPU-generation
constraints (Table~\ref{tab:hazel-generations}), the full timing-only reverse-engineering
suite (capacity, line size, associativity, hit/miss latency, inclusion/exclusion) has so far
been run on \textbf{one generation, \texttt{haswell}} -- chosen as the pilot because it is
the oldest/most-available Hazel constraint and the team already has a same-family lab analog
(Sunbird, a Haswell-EP Xeon E5-2680 v3). The other 8 generations
(\texttt{broadwell, cascadelake, icelake\_6326, icelake\_8358, sapphirerapids, skylake,
genoa, turin}) have only an access/build smoke test committed so far (job submission,
\texttt{cache\_bench} compiling and running cleanly under \texttt{srun}); their cache
sweeps, and Phase-II PMU verification on Hazel generally, remain outstanding work. This is
disclosed here rather than papered over: the held-out evaluation below covers
\texttt{haswell} only.

\begin{table}[H]
\centering\small
\caption{Hazel generation status.}
\label{tab:hazel-generations}
\begin{tabular}{|l|l|l|}
\hline
Slurm constraint & CPU generation & Status\\
\hline
\texttt{haswell} & Intel Xeon E5 v3 & \textbf{Full Phase-I suite complete} (below)\\
\texttt{broadwell} & Intel Xeon E5 v4 & Access/build check only\\
\texttt{cascadelake} & Intel Xeon Cascade Lake & Access/build check only\\
\texttt{icelake\_6326} & Intel Ice Lake-SP & Access/build check only\\
\texttt{icelake\_8358} & Intel Ice Lake-SP & Access/build check only\\
\texttt{sapphirerapids} & Intel Sapphire Rapids & Access/build check only\\
\texttt{skylake} & Intel Skylake-SP & Access/build check only\\
\texttt{genoa} & AMD EPYC Genoa (Zen 4) & Access/build check only\\
\texttt{turin} & AMD EPYC Turin (Zen 5), if visible & Access/build check only\\
\hline
\end{tabular}
\end{table}

\subsection{Hazel-Haswell: Machine and Method}
Node \texttt{c207n01}/\texttt{c207n02} (Hazel is dynamic; the pipeline landed on different
physical nodes across stages, all reporting the identical CPU), \texttt{Intel(R) Xeon(R) CPU
E5-2650 v3 @ 2.30GHz} -- the same Haswell-EP generation as Sunbird's own Xeon E5-2680 v3, but
a different SKU (10 cores vs.\ Sunbird's 12). Bound via \texttt{srun --cpu-bind=cores} to
logical CPU 10 (physical core 10, socket 1, NUMA node 1, confirmed via \texttt{numactl -s}).
Identical code, build flags, sample count (1,000,000/point), and pipeline scripts as every
lab machine, run under \texttt{HAZEL\_MODE=1} (which swaps \texttt{taskset} for
\texttt{srun --cpu-bind=cores} and uses an isolated per-job build directory).

\subsection{Hazel-Haswell: Timing-Only Results}
\textbf{Capacity:} L1D well-confirmed at \textbf{32,768\,B} (flat through exactly that size,
departs at the next point). L2 is \textbf{not cleanly resolved} this pass -- latency climbs
smoothly and continuously from $\sim$35\,KiB through $\sim$20\,MiB with no second, visually
distinct knee at this sweep's resolution. LLC$\to$DRAM begins between $\sim$21.8--23.7\,MiB
and does not plateau even out to the tested 256\,MiB ceiling (unlike every lab machine, which
all eventually flattened somewhere in the 256\,MiB--2\,GiB range) -- a further manual tail
extension, mirroring every lab machine's own follow-up, remains outstanding. \textbf{Line
size: 64\,B, confirmed offset-invariant at all three levels} via the Method-A step-4 check
(L1: 0\% spread across 8 offsets; L2: 1.26x spread but a real elbow at every offset; LLC:
0\% spread). \textbf{Associativity: L1 = 8-way, fully reproducible} (base + both repeats
agree exactly); \textbf{L2 unresolved} (base=3, both repeats=4, explicit pipeline
disagreement warning -- unsurprising since L2's own capacity was never confirmed this pass);
\textbf{LLC = 9-way, fully reproducible} but this is the same $\sim$9-10-way universal
small-structure confound documented across the lab fleet (Section~\ref{sec:assoc}), not a
citable real LLC associativity -- direct cross-validation that the confound is a property of
this measurement method, not something specific to the lab machines. \textbf{Hit latency}
(dependent/random/median): L1=8.46, L2=16.74, LLC=53.79, DRAM=206.18 ticks, a clean
monotonic ladder; L1's read is internally cross-checked against the capacity sweep's own
$\sim$8.3--8.5-tick median at 16--32\,KiB footprints. \textbf{Miss latency} (random/median):
L1$\to$L2=104.0, L2$\to$LLC=270.0, LLC$\to$DRAM=497.0 ticks, an increasing sequence
consistent with the hit-latency ladder, though with substantial (24--78\%) repeat spread
flagged by the pipeline itself, matching the lab fleet's own documented miss-latency
variance. \textbf{Inclusion/exclusion:} L1\_vs\_L2 UNCERTAIN (28.5\% survived / 1.0\%
invalidated, 70.5\% ambiguous -- unlike every lab machine's own clean
exclusive/non-inclusive read at this pairing, though the paired check still shows a
consistent target-slower-than-control signal); L2\_vs\_LLC EXCLUSIVE/NON-INCLUSIVE (80.5\%
survived); L1\_vs\_LLC (skip-level) EXCLUSIVE/NON-INCLUSIVE, the cleanest of the three
(97.5\% survived). Best-guess synthesis: LLC reads as non-inclusive of both L1 and L2 on
this machine, matching the pattern already seen on Skylark/Upgrade/Crux rather than
Sunbird's own inclusive-LLC-toward-L1 reading. Phase-II PMU verification has not yet been
run on Hazel (the \texttt{pmu/} section of \texttt{data\_raw/hazel\_haswell/README.md} is
explicitly left blank, gated behind the same Phase discipline as the lab machines).

\subsection{Held-Out Prediction Evaluation}
\begin{table}[H]
\centering\scriptsize
\caption{Frozen lab-only prediction vs.\ Hazel-Haswell held-out measurement. Predictions and their commit/tag were frozen \emph{before} this data existed (Section~\ref{sec:phasediscipline}); this table is filled in only now, after the freeze, per \texttt{PREDICTION\_FREEZE.md}'s own rule.}
\label{tab:hazel-heldout}
\begin{tabular}{|p{2.6cm}|p{2.6cm}|p{2.6cm}|p{4.8cm}|}
\hline
Quantity & Frozen prediction & Hazel-Haswell measured & Verdict / discussion\\
\hline
L1D capacity & 32,768\,B & 32,768\,B & \textbf{Supported, exact match}\\
L1D associativity & 8-way & 8-way, fully reproducible & \textbf{Supported, exact match}\\
L1D hit latency & $\sim$10.4 ticks (range 6.2--10.4) & 8.46 ticks & \textbf{Supported}, within the lab-fleet range\\
L1 miss penalty & $\sim$124 ticks (range 67--188) & 104.0 ticks & \textbf{Supported}, within range\\
L2 capacity/core & 256\,KiB & Not cleanly resolved (continuous ramp, no second knee) & \textbf{Untestable this pass} -- the prediction can be neither confirmed nor falsified until a dedicated dense L2 sweep resolves a boundary; flagged, not forced\\
L2 associativity & 8-way & Unresolved (base=3, repeats=4) & \textbf{Untestable} -- same reason as capacity above\\
L2 hit latency & $\sim$26.8 ticks (range 14.3--28.1) & 16.74 ticks & \textbf{Weakly supported}, within range but measured at an unconfirmed L2 footprint\\
L2 miss penalty & $\sim$284 ticks (range 120--723) & 270.0 ticks & \textbf{Supported}, within range\\
LLC capacity & $\sim$31.46\,MB & $\sim$22--24\,MB (best-guess edge, not a clean knee) & \textbf{Weakened}, $\sim$25--30\% low -- plausibly a real SKU effect (E5-2650 v3, 10 cores, vs.\ Sunbird's E5-2680 v3, 12 cores), not a methodology failure, consistent with this project's own repeated finding that LLC capacity varies by SKU within a generation\\
LLC associativity & 20-way (Phase-II-corrected) & 9-way (Phase-I only; same confound wall) & \textbf{Not a fair test} -- Hazel Phase II has not run; Hazel's own Phase-I number reproduces the identical confound documented on 6 lab machines, itself a useful cross-validation of the confound's generality\\
LLC hit latency & $\sim$58.4 ticks (range 27.2--108) & 53.79 ticks & \textbf{Supported}, close to the Sunbird same-family analog specifically\\
LLC$\to$memory miss penalty & $\sim$622 ticks (range 360--852) & 497.0 ticks & \textbf{Supported}, within range\\
Line size & 64\,B & 64\,B, confirmed offset-invariant at all 3 levels & \textbf{Supported, exact match}\\
Inclusion (L1 vs.\ L2) & NON-INCLUSIVE, confident & UNCERTAIN (mixed) & \textbf{Weakened} -- the one lab-wide-confident call did not reproduce cleanly on Hazel\\
Inclusion (L2 vs.\ LLC) & UNCERTAIN & EXCLUSIVE/NON-INCLUSIVE & \textbf{Supported} (resolved in the low-confidence prediction's own more-likely direction)\\
Inclusion (L1 vs.\ LLC, skip-level) & UNCERTAIN, leans INCLUSIVE & EXCLUSIVE/NON-INCLUSIVE, cleanest of the three & \textbf{Falsified} -- the predicted lean was wrong; Hazel's own two LLC-involving pairings agree with each other (both non-inclusive), unlike Sunbird's own split result\\
\hline
\end{tabular}
\end{table}

\textbf{Overall read:} of the 15 predicted quantities, 8 are cleanly supported (both L1D
fields, both L1 latency fields, L2/LLC miss penalty, LLC hit latency, line size), 2 are
untestable this pass (L2 capacity/associativity, since Hazel's own L2 boundary was never
resolved), 1 is not a fair comparison (LLC associativity, Phase II not yet run on Hazel), 3
inclusion/exclusion calls split evenly between supported and weakened/falsified, and LLC
capacity is measurably (if explicably) low. The quantities this project's own Chen--Ngo
Cache Invariance Law targeted (L1D capacity/associativity, line size) are exactly the ones
that held perfectly -- consistent with that law's own stated scope (an x86 Xeon target is
precisely the class where its 6/8 majority held with no exceptions,
Section~\ref{sec:cachelaws}). The Capacity Doubling Law's own prediction (L2 = 256\,KiB) was
evaluated at its same-era fit endpoint rather than extrapolated, so this pass provides no
genuine test of the law's extrapolation behavior -- only a future Hazel generation
meaningfully newer than 2014 would test that.

\textbf{Further quantitative prediction, $\sim$5 years beyond the newest measured lab
system} (Artemisia, 2023 $\to$ target year $\approx$2028), per the Capacity Doubling Law:
$C(2028) = 256\,\text{KiB}\cdot2^{(2028-2014)/3} \approx 256\,\text{KiB}\cdot 2^{4.67}
\approx 6.5\,\text{MiB}$ per core, with a wide uncertainty band (the model's own step-function
shape, Section~\ref{sec:evolution} item 2, means the true value could land anywhere from a
continued-flat $\sim$2\,MiB to well above 6.5\,MiB if another rapid-doubling burst occurs) --
stated with the same future-scaling-wall caveats as Section~\ref{sec:cachelaws}'s own
analysis (SRAM density/access-time tradeoff, core-count competition for die area). No
lab-only or Hazel machine in this dataset can directly test a 2028 prediction; it is recorded
here as the assignment's required forward-looking estimate, not a claim this project can
itself validate.

\section{Discussion}

This project's central finding is a split one: \textbf{the timing-only method is highly
reliable at the level closest to the core and progressively less reliable further away from
it.} L1D size, associativity, line size, and sharing scope matched PMU/system-report ground
truth on 8 of 8 machines with zero exceptions (Section~\ref{sec:pmu}) -- the timing-only
inference chain (Section~\ref{sec:phase1}) works exactly as designed there. The same method's
L2 associativity guess was right on only 3 of 8 machines and its LLC associativity guess on
just 1 of 8, and LLC capacity itself was wrong on 3 of 8 -- not because the method is
unsound, but because of one specific, well-evidenced confound (a small, fixed-size,
page-count-limited structure, most likely the DTLB, that saturates before a real L2/LLC set
does whenever the conflict-set stride exceeds roughly 1\,MiB, Section~\ref{sec:assoc}).
Every one of those confound-blocked guesses was already flagged as a low-confidence
"effective bound" in Phase I, before Phase II data existed -- this project's own
uncertainty reporting, not hindsight, correctly predicted where it would be wrong.

The software-only cache metric (Section~\ref{sec:hitrate}) shows the same pattern at a
different layer: a single global latency threshold cleanly separates L1/L2-scale residency
(10--20\% median relative error against PMU ground truth) but cannot distinguish an
LLC-speed hit from a DRAM-speed miss (60--100\% relative error), a limitation disclosed in
the estimator's own design docstring before PMU validation confirmed it, not discovered
after the fact.

The chronological analysis (Section~\ref{sec:evolution}) found that Moore-style scaling is
not a universal property of cache design: L2 capacity grows in a genuine, if step-wise,
doubling pattern, while L1D capacity/associativity/line-size are deliberately held nearly
constant across a decade and three vendors -- both are real, well-evidenced findings, and
neither is more "correct" than the other; they reflect different engineering constraints
(L1D's hard load-to-use latency budget vs.\ L2's more flexible area/latency tradeoff,
Section~\ref{sec:cachelaws}). The one Hazel generation tested so far (Haswell) supported
exactly the quantities the team's own Invariance Law predicted most confidently, and
weakened or falsified two of the three inclusion/exclusion calls -- a genuinely mixed,
honestly reported result rather than a curated success story.

\section{Conclusion}

Across eight ECE lab machines spanning three vendors (Intel, AMD, Ampere/Arm), three ISAs'
worth of generations (2014--2023), and one held-out Hazel HPC generation, this project built
a single portable timing-only benchmark suite, inferred each machine's L1D/L2/LLC capacity,
line size, associativity, hit latency, miss/next-level latency, and inclusion/exclusion
behavior from that suite alone, and then verified those inferences with hardware performance
counters. Cache-level discovery, capacity, and line size were reliable at every level tested;
associativity above L1 was reliable only at L1, blocked everywhere else by a real,
well-characterized, now cross-architecture-reproduced measurement confound rather than by
sloppy methodology -- a negative result documented with the same rigor as the positive ones.
The team's two named empirical laws (per-core L2 capacity doubling roughly every 3 years;
approximate invariance of L1D geometry) were fit exclusively from the eight lab observations,
frozen in Git before any Hazel measurement, and then genuinely tested against a held-out
Hazel generation -- the Invariance Law held cleanly where its own stated scope predicted it
would, and the broader inclusion/exclusion picture showed real, disclosed disagreement rather
than a forced match. The software-only cache-hit-rate estimator meets its design goal at
L1/L2 scale and is explicit about failing at LLC/DRAM scale, a limitation predicted by its
own design before it was measured. Taken together, the strongest and most broadly
reproducible finding in this entire dataset is the simplest one: L1D cache geometry is,
across a decade of process nodes and three competing vendors, the one part of the memory
hierarchy that has not needed to change.

\newpage

% ============================================================
% MANDATORY TEAM CONTRIBUTION APPENDIX
% ============================================================

% Force the next section to be Section 14 (13 content sections precede this
% appendix after the per-machine/software-metric/evolution restructuring).
% Do NOT use \appendix because that would make the section
% number change from 14 to A.
\setcounter{section}{13}
\renewcommand{\thesection}{\arabic{section}}

% Make subsections use letters A, B, C, ..., I.
\setcounter{subsection}{0}
\renewcommand{\thesubsection}{\Alph{subsection}}

\section{Mandatory Team Contribution Appendix}

\subsection{Team Members and GitHub Accounts}

\begin{table}[htbp]
\centering
\caption{Team-member identification and GitHub accounts for the mandatory contribution appendix.}
\label{tab:team-members}

\begin{tabular}{|p{4cm}|p{4cm}|p{5cm}|}
\hline
\textbf{Member Name} &
\textbf{Unity ID} &
\textbf{GitHub Username} \\
\hline

Nick Ngo (Member 1) &
nsngo &
NicholasNgo4 \\
\hline

Kevin Chen (Member 2) &
krchen &
krchen1 \\
\hline

\end{tabular}

\end{table}


\subsection{Result / Data-Point Attribution}

For every result shown in the paper---including each final table entry, plotted machine/generation point, prediction point, PMU comparison, and major derived quantity---identify who produced the underlying measurement and who checked/processed it.

Mechanically repeated points from one identical sweep may be grouped as one row. For example, ``L1 capacity sweep on Sunbird, all working-set sizes.'' However, no reported result should be unattributed.

\begin{longtable}{|p{1.2cm}|p{4cm}|p{2.5cm}|p{2.5cm}|p{3cm}|}
\caption{Result/data-point attribution for every reported result.}
\label{tab:result-attribution} \\

\hline
\textbf{Result / Data ID} &
\textbf{Result, figure, table, machine, or data series} &
\textbf{Member 1 contribution} &
\textbf{Member 2 contribution} &
\textbf{Raw-data / script / commit reference} \\
\hline
\endfirsthead

\hline
\textbf{Result / Data ID} &
\textbf{Result, figure, table, machine, or data series} &
\textbf{Member 1 contribution} &
\textbf{Member 2 contribution} &
\textbf{Raw-data / script / commit reference} \\
\hline
\endhead

R1 & Capacity sweeps, all 8 machines (all working-set sizes/boundaries, Section~\ref{sec:permachine}) & Ran all sweeps, wrote \texttt{capacity.c}/\texttt{run\_capacity\_full.sh}, boundary analysis & Reviewed boundaries against \texttt{CAPACITY\_RESULTS.md} reconciliation & \texttt{data\_raw/<machine>/capacity/}, \texttt{main\_code/common/capacity.c}\\
\hline
R2 & Line-size sweeps, all 8 machines, both methods & Reviewed cross-machine agreement & Ran all sweeps, wrote \texttt{line\_size.c}/\texttt{run\_line\_size\_full.sh}, both methods & \texttt{data\_raw/<machine>/line\_size/}, \texttt{main\_code/common/line\_size.c}\\
\hline
R3 & Associativity sweeps, all 8 machines, incl.\ the cross-machine confound investigation & Ran all sweeps, wrote \texttt{associativity.c}, derived-stride-scan technique, confound analysis & Reviewed L1 results against line-size/capacity corroboration & \texttt{data\_raw/<machine>/associativity/}, \texttt{main\_code/common/associativity.c}\\
\hline
R4 & Hit/miss latency sweeps, all 8 machines & Ran all sweeps, wrote \texttt{latency.c}/\texttt{run\_\{hit,miss\}\_latency\_full.sh} & Reviewed overhead-caveat discussion & \texttt{data\_raw/<machine>/latency/}, \texttt{main\_code/common/latency.c}\\
\hline
R5 & Inclusion/exclusion sweeps, all 8 machines, 3 pairings each & Ran all sweeps, wrote \texttt{inclusion\_policy.c}, classifier design & Reviewed cross-machine table (Table~\ref{tab:inclusion-crossmachine}) & \texttt{data\_raw/<machine>/inclusion\_policy/}, \texttt{scripts/classify\_inclusion\_policy.py}\\
\hline
R6 & Software-only hit-rate estimator + sweep, all 8 machines (Section~\ref{sec:hitrate}) & Reviewed PMU-validation failure-mode analysis & Wrote \texttt{software\_hit\_rate.c/h}, ran all sweeps + PMU validation harness & \texttt{data\_raw/<machine>/software\_hit\_rate/}, \texttt{main\_code/software\_hit\_rate/}\\
\hline
R7 & Phase-II PMU verification, Crux/Skylark/Upgrade/Charnwood/Ookay/Artemisia (6 of 8 machines) & Ran verification, wrote per-machine \texttt{PHASE2\_VALIDATION\_TABLE.md} & -- & \texttt{data\_raw/<machine>/pmu/}, \texttt{scripts/run\_pmu\_verification.sh}\\
\hline
R8 & Phase-II PMU verification, Sunbird and Thunderbird (2 of 8 machines) & Reviewed against literature (Agner Fog / Ampere datasheet) & Ran verification, timer/units caveats for ARM & \texttt{data\_raw/\{sunbird,thunderbird\}/pmu/}\\
\hline
R9 & Eight-counter cross-generation study (Section~\ref{sec:pmu} eight-counters section), all 8 machines & Reviewed ranked-summary interpretation & Wrote \texttt{run\_standardized\_benchmarks.sh}/\texttt{summarize\_eight\_counters.py}, ran all machines & \texttt{data\_processed/master/eight\_counters/}\\
\hline
R10 & Hazel-Haswell full Phase-I suite (Section~\ref{sec:hazel}) & Reviewed held-out evaluation table against frozen predictions & Ran entire Hazel pipeline (9 Slurm jobs, all 5 experiment types) & \texttt{data\_raw/hazel\_haswell/}, \texttt{hpc\_slurm/}\\
\hline
R11 & Chronological master table, Moore-style plots, two named cache laws (Sections~\ref{sec:evolution}--\ref{sec:cachelaws}) & Wrote Section 9 interpretation/prediction answers, cache-law fitting & Wrote \texttt{plot\_chronological\_master.py}, consolidated \texttt{CHRONOLOGICAL\_MASTER\_TABLE.md} & \texttt{data\_processed/master/chronological\_master\_table.csv}, \texttt{PREDICTION\_FREEZE.md}\\
\hline
R12 & Portable common code (pointer chase, timer, batching, access pattern) and main driver (Section~\ref{sec:build}) & Wrote \texttt{main.c}, \texttt{benchmark.c}, \texttt{pointer\_chase.c}, \texttt{random.c} & Wrote \texttt{timer.h}/\texttt{timer\_x86.h}/\texttt{timer\_arm.h} & \texttt{main\_code/common/}\\
\hline
R13 & Report body text, Sections 1--8 and 10--14 & Drafted Section 9 answers and consolidation edits & Drafted the bulk of the report prose and initial section scaffolding & \texttt{report/src/report.tex}\\
\hline

\end{longtable}

Repeated points from one identical sweep are grouped by machine/experiment-type per the
instructions above (e.g.\ ``capacity sweeps, all 8 machines'' is one row, not 8); per-machine
detail for every row is traceable via each machine's own \texttt{data\_raw/<machine>/README.md}
and \texttt{data\_processed/<machine>/FINAL\_CACHE\_TABLE.md}, both committed alongside the raw
data cited above. Attribution is derived from git commit-author hostnames (e.g.\
\texttt{nsngo@<machine>.ece.ncsu.edu} vs.\ \texttt{krchen@<machine>.ece.ncsu.edu}), which
reliably indicate who was physically logged into a given machine for a given commit, cross-checked
against \texttt{git shortlog}/\texttt{git log --numstat} on the relevant files.


\subsection{Team Meeting Log}

Record project meetings or substantial synchronous work sessions, if any. Include who attended and a concise summary of decisions, work completed, unresolved issues, and next steps.

\begin{longtable}{|p{2.5cm}|p{3cm}|p{2cm}|p{2cm}|p{5cm}|}
\caption{Team meeting and synchronous-work log.}
\label{tab:meeting-log} \\

\hline
\textbf{Date / Time} &
\textbf{Purpose} &
\textbf{Member 1 attended?} &
\textbf{Member 2 attended?} &
\textbf{Summary / decisions / next steps} \\
\hline
\endfirsthead

\hline
\textbf{Date / Time} &
\textbf{Purpose} &
\textbf{Member 1 attended?} &
\textbf{Member 2 attended?} &
\textbf{Summary / decisions / next steps} \\
\hline
\endhead

29Aug2026, 3pm &
Meet Team Members, start on project &
Yes &
Yes &
Github + overleaf report created, tested access to machines, filled out Table 1, begin building micro benchmark\\
\hline

& & & & \\
\hline

& & & & \\
\hline

& & & & \\
\hline

\end{longtable}

Add rows as needed. If the team had no formal meetings, state that explicitly and describe how coordination occurred.


\subsection{Code Contribution}

List the code components, scripts, benchmark modules, build files, and infrastructure each member created, modified, reviewed, or debugged. Include Git commit hashes or pull-request references where useful.

\begin{longtable}{|p{4cm}|p{3.5cm}|p{3.5cm}|p{3cm}|}
\caption{Code, script, build, and infrastructure contributions.}
\label{tab:code-contributions} \\

\hline
\textbf{Code component / file / module} &
\textbf{Member 1 contribution} &
\textbf{Member 2 contribution} &
\textbf{Git reference} \\
\hline
\endfirsthead

\hline
\textbf{Code component / file / module} &
\textbf{Member 1 contribution} &
\textbf{Member 2 contribution} &
\textbf{Git reference} \\
\hline
\endhead

\texttt{main\_code/common/\{main,benchmark,pointer\_chase,random,capacity\}.\{c,h\}} & Primary author: capacity engine, benchmark/pointer-chase primitives, main CLI driver and dispatch & Co-edited: benchmark.c/random.c shared review & \texttt{8fb1467}, \texttt{e343732}, \texttt{7be6dac}\\
\hline

\texttt{main\_code/common/\{associativity,latency,inclusion\_policy\}.\{c,h\}} & Primary author: associativity engine (incl.\ derived-stride-scan, huge-page DTLB control), hit/miss latency, inclusion/exclusion classifier design & Reviewed for cross-experiment consistency with line\_size.c's conventions & \texttt{22104d6}, \texttt{e343732}, \texttt{7be6dac}\\
\hline

\texttt{main\_code/common/line\_size.\{c,h\}}, \texttt{access\_pattern.h} & Reviewed against capacity.c's sweep-loop conventions & Primary author: both line-size methods (single-curve + family-of-curves), offset cross-check & \texttt{1689b22}\\
\hline

\texttt{main\_code/x86\_64/timer\_x86.h}, \texttt{main\_code/aarch64/timer\_arm.h}, \texttt{main\_code/common/timer.h} & Reviewed x86/ARM timer-overhead characterization & Primary author: portable timer dispatch, RDTSC/RDTSCP and CNTVCT\_EL0 serialization sequences & \texttt{e0f2538}\\
\hline

\texttt{main\_code/software\_hit\_rate/software\_hit\_rate.\{c,h\}} & Reviewed PMU-validation-mode rationale & Primary author: ROC/Youden's-J threshold selection, Rogan--Gladen estimator, bootstrap CI, PMU-validation decoupled-calibration mode & (2026-09-14 commits)\\
\hline

\texttt{scripts/run\_\{capacity,associativity,hit\_latency,miss\_latency,inclusion\_policy\}\_full.sh}, \texttt{run\_associativity\_stride\_scan.sh}, \texttt{run\_capacity\_\{sweep,validate\}.sh}, \texttt{plot\_\{capacity,associativity,hit\_latency,miss\_latency,inclusion\_policy,chronological\_master,eight\_counters\_master\}.py}, \texttt{detect\_associativity.py}, \texttt{detect\_cache\_hierarchy.py}, \texttt{classify\_inclusion\_policy.py} & Primary author & Reviewed for cross-script CLI-convention consistency & \texttt{scripts/}\\
\hline

\texttt{scripts/run\_line\_size\{,\_full,\_sweep\}.sh}, \texttt{run\_software\_hit\_rate\_sweep.sh}, \texttt{run\_\{pmu\_verification,hit\_rate\_pmu\_validation,standardized\_benchmarks\}.sh}, \texttt{detect\_line\_size.py}, \texttt{plot\_line\_size\{,\_family,\_offset\}.py}, \texttt{plot\_software\_hit\_rate.py}, \texttt{compare\_hit\_rate\_pmu.py}, \texttt{summarize\_\{pmu,eight\_counters,software\_hit\_rate,raw\}.py} & Reviewed for cross-script CLI-convention consistency & Primary author & \texttt{scripts/}\\
\hline

\texttt{Makefile}, \texttt{cache\_bench.c} main dispatch, GitHub repository scaffolding/folder structure & Reviewed & Primary author (build config, initial repo scaffolding) & \texttt{7be6dac}\\
\hline

\texttt{hpc\_slurm/} (all Hazel Slurm job scripts, all 9 generations) & Reviewed job-script conventions against lab-machine pipeline & Primary author (sole author of the entire Hazel HPC run, 9 partitions) & \texttt{hpc\_slurm/}\\
\hline

\end{longtable}


\subsubsection*{Required GitHub Contribution-Plot Screenshot}

Immediately after the code-contribution table, insert a screenshot of the GitHub repository's contributor/contribution plot.

\begin{figure}[htbp]
\centering

% Uncomment after adding the screenshot:
% \includegraphics[width=\textwidth]{github_contributions.png}

\fbox{
\parbox[c][3in][c]{0.85\textwidth}{
\centering
Insert GitHub contributor/contribution plot screenshot here.

Show both student accounts; caption with full name and GitHub username mapping.
}
}

\caption{GitHub contributor/contribution evidence.}
\label{fig:github-contributions}

\end{figure}


\subsection{Experimental-Running Contribution}

Document who actually ran each major experiment. Include the machine/generation and enough information to connect the work to the raw-data files.

\begin{longtable}{|p{4.5cm}|p{3cm}|p{4cm}|p{4cm}|}
\caption{Experimental-running contribution by machine/generation.}
\label{tab:experimental-contributions} \\

\hline
\textbf{Experiment / machine / generation} &
\textbf{Run date(s)} &
\textbf{Member 1 contribution} &
\textbf{Member 2 contribution} \\
\hline
\endfirsthead

\hline
\textbf{Experiment / machine / generation} &
\textbf{Run date(s)} &
\textbf{Member 1 contribution} &
\textbf{Member 2 contribution} \\
\hline
\endhead

Capacity, all 8 lab machines & 2026-09-09 to 2026-09-12 & Ran all sweeps, dense follow-ups, and tail extensions & -- \\
\hline

Associativity, all 8 lab machines (incl.\ confound investigation, derived-stride-scan on Upgrade) & 2026-09-11 to 2026-09-13 & Ran all sweeps and the cross-machine confound analysis & -- \\
\hline

Hit/miss latency, inclusion/exclusion, all 8 lab machines & 2026-09-13 & Ran all sweeps & -- \\
\hline

Line size, all 8 lab machines (both methods) & 2026-09-11 to 2026-09-13 & -- & Ran all sweeps, both methods \\
\hline

Software-only hit-rate estimator + PMU validation, all 8 lab machines & 2026-09-14 & -- & Ran all sweeps and the PMU-validation harness \\
\hline

Phase-II PMU verification: Crux, Skylark, Upgrade, Charnwood, Ookay, Artemisia (6 machines) & 2026-09-13 & Ran verification & -- \\
\hline

Phase-II PMU verification: Sunbird, Thunderbird (2 machines) & 2026-09-13 & -- & Ran verification \\
\hline

Eight-counter cross-generation study, all 8 lab machines & 2026-09-13 to 2026-09-14 & -- & Ran standardized benchmarks and collected counters \\
\hline

Hazel HPC, \texttt{haswell} generation (full Phase-I suite: capacity/line\_size/associativity/latency/inclusion\_policy) & 2026-09-14 & -- & Submitted and monitored all 9 Slurm jobs \\
\hline

Hazel HPC, remaining 8 generations (access/build check only) & 2026-09-14 & -- & Submitted access-check jobs \\
\hline

\end{longtable}


\subsection{Plotting and Writing Contribution}

List each major figure/table and each major paper section. State who generated/checked the plots, who drafted the text, and who revised it.

\begin{longtable}{|p{5cm}|p{4.5cm}|p{4.5cm}|}
\caption{Plotting, table, and report-writing contributions.}
\label{tab:plotting-writing-contributions} \\

\hline
\textbf{Figure / table / report section} &
\textbf{Member 1 contribution} &
\textbf{Member 2 contribution} \\
\hline
\endfirsthead

\hline
\textbf{Figure / table / report section} &
\textbf{Member 1 contribution} &
\textbf{Member 2 contribution} \\
\hline
\endhead

Capacity/associativity/hit/miss-latency/inclusion-policy plots, all 8 machines (Section~\ref{sec:permachine}) & Generated and checked all plots (own scripts) & Reviewed plots against reported numbers \\
\hline

Line-size and software-hit-rate plots, all 8 machines & Reviewed plots against reported numbers & Generated and checked all plots (own scripts) \\
\hline

Section~\ref{sec:build}--\ref{sec:phasediscipline} (Experimental Methodology) & Reviewed & Drafted \\
\hline

Section~\ref{sec:phase1} (Timing-Only method) and Section~\ref{sec:permachine} (Per-Machine Results) & Drafted per-machine capacity/associativity/latency/inclusion prose; verified code listings against committed source & Reviewed \\
\hline

Section~\ref{sec:pmu} (PMU Verification) & Drafted 6-machine verification prose & Drafted Sunbird/Thunderbird verification prose \\
\hline

Section~8.4 (Eight Performance Counters) & Reviewed & Drafted \\
\hline

Section~\ref{sec:hitrate} (Software-Only Cache Metric) & Reviewed & Drafted (own implementation) \\
\hline

Section~\ref{sec:evolution}--\ref{sec:cachelaws} (Chronological Evolution, Cache Laws) & Drafted interpretation items and both laws' quantitative fits & Generated master table/plots; reviewed \\
\hline

Section~\ref{sec:hazel} (Hazel Validation), Discussion, Conclusion & Drafted held-out evaluation and synthesis & Ran the underlying Hazel experiments; reviewed \\
\hline

Mandatory Contribution Appendix & Reviewed & Drafted (from git history) \\
\hline

\end{longtable}


\subsection{Analysis Contribution}

Document the reasoning work separately from running experiments and making plots.

\begin{longtable}{|p{5cm}|p{4.5cm}|p{4.5cm}|}
\caption{Analysis and interpretation contributions.}
\label{tab:analysis-contributions} \\

\hline
\textbf{Analysis question / conclusion} &
\textbf{Member 1 contribution} &
\textbf{Member 2 contribution} \\
\hline
\endfirsthead

\hline
\textbf{Analysis question / conclusion} &
\textbf{Member 1 contribution} &
\textbf{Member 2 contribution} \\
\hline
\endhead

Identifying and retracting spurious capacity boundaries (frequency-ramp artifacts, session-contention spikes, multi-boundary detector false positives) & Primary: identified the frequency-ramp-start artifact and the associativity DTLB-scale confound hypothesis; designed the derived-stride-scan cross-check & Reviewed and cross-checked against line-size data \\
\hline

Deciding how L2/LLC associativity best-guess values were derived when direct measurement was confound-blocked (Section~\ref{sec:assoc}, per-machine final tables) & Primary: S=C/(A$\times$B) internal-consistency reasoning, per-machine best-guess synthesis & Reviewed \\
\hline

Interpreting inclusion/exclusion cross-machine disagreement (Table~\ref{tab:inclusion-crossmachine}) as a genuine vendor/generation split vs.\ a methodology artifact & Primary & Reviewed \\
\hline

Explaining why the software-only hit-rate estimator fails at LLC/DRAM scale (Section~\ref{sec:hitrate-pmu}) & Reviewed & Primary (designed and diagnosed own estimator) \\
\hline

Validating PMU evidence against Phase-I inference and deciding the Agreement verdict per cell (Section~\ref{sec:pmu}) & Primary for 6 machines & Primary for 2 machines (Sunbird, Thunderbird) \\
\hline

Fitting the two named cache laws' quantitative rules and identifying their future scaling walls (Section~\ref{sec:cachelaws}) & Primary & Reviewed \\
\hline

Constructing and freezing the lab-only Hazel prediction table before any Hazel data existed (\texttt{PREDICTION\_FREEZE.md}) & Primary & Reviewed \\
\hline

Evaluating the held-out Hazel-Haswell measurements against the frozen prediction and assigning support/weaken/falsify verdicts (Table~\ref{tab:hazel-heldout}) & Primary & Reviewed against own raw Hazel data \\
\hline

\end{longtable}


\subsection{AI / LLM Assistance Disclosure}

Complete this table if any AI/LLM tool was used. Add rows as needed. If no AI was used, write ``None.''

\begin{longtable}{|p{3cm}|p{3.5cm}|p{4cm}|p{4.5cm}|}
\caption{AI/LLM assistance disclosure.}
\label{tab:ai-disclosure} \\

\hline
\textbf{Tool} &
\textbf{Task / artifact} &
\textbf{Type of help received} &
\textbf{What the team verified and can explain} \\
\hline
\endfirsthead

\hline
\textbf{Tool} &
\textbf{Task / artifact} &
\textbf{Type of help received} &
\textbf{What the team verified and can explain} \\
\hline
\endhead

Claude &
GitHub Structure and Necessary Folders &
Content Creation - Created Github Infrastructure containing required folders/files &
The content creation is purely administrative. The populated folders and files did not contain any technical content and were created based off what the project specifications required. The team verified the repository by examining the contents inside it.\\
\hline

ChatGPT &
Overleaf LaTeX Report
& Content Creation - Populated report section headers and fixed syntax issues
& The team verified by examining the LaTeX file, ensuring the file compiles, and content matches the project requirements.\\
\hline

Claude &
Report Sections 3--4 (Experimental Methodology; Timing-Only Cache Reverse Engineering) &
Content Creation -- drafted methodology prose, inserted verified source-code listings copied verbatim from \texttt{main\_code/common/} with file/commit citations, and added methodology-logic/plot figure placeholders &
The team verified every code listing against the actual repository source at the cited commit, confirmed the described method matches each header's own docstring and each machine's \texttt{data\_raw/<machine>/README.md}, and can explain the pointer-chase/timing/batching/access-pattern design and every stated caveat (e.g.\ the unresolved L2/LLC associativity confound, the single-shot miss-latency overhead) independently of the AI-drafted text.\\
\hline

\end{longtable}

\noindent
\textbf{AI accountability statement:} Both students are responsible for understanding every submitted artifact, including AI-assisted code and analysis.


\subsection{Final Contribution Confirmation}

Both members must review the completed appendix before submission and complete Table 14.


\begin{table}[H]
\centering
\caption{Final contribution-appendix confirmation by both team members.}
\label{tab:final-confirmation}

\begin{tabular}{|p{6cm}|p{8cm}|}
\hline
\textbf{Member Name} &
\textbf{Confirmation / Date} \\
\hline

Member 1 & \\
\hline

Member 2 & \\
\hline

\end{tabular}

\end{table}

\end{document}
