# charnwood — Reproduction Manifest

> Fill in every field before freezing results. This README must let a grader go from
> this folder to the exact command that generated the data without guessing.

## Machine Identification
- Hostname: charnwood.ece.ncsu.edu
- CPU model (from `lscpu`/`/proc/cpuinfo`): Intel(R) Core(TM) i7-6700 CPU @ 3.40GHz (1 socket, 4 cores / 8 threads)
- ISA / architecture: x86-64
- Vendor / microarchitecture / codename (researched, NOT from cache tables): Intel Skylake (desktop), per `MACHINE_RESEARCH.md` Table 1
- Introduction year (per the team's stated year convention): 2015 (per `MACHINE_RESEARCH.md`)
- Process node (if reliably documented): 14 nm (per `MACHINE_RESEARCH.md`)
- Kernel version: Linux 6.8.0-101-generic (see `build/kernel_version.txt`)
- Page size: 4096 bytes (see `build/page_size.txt`)
- SMT siblings idle during runs? Yes for the pinned core: this session had `Cpus_allowed_list` 0-7 (all logical CPUs on this 4c/8t chip). Topology (`lscpu -e=CPU,CORE,SOCKET,NODE`): physical core 3 = logical CPUs {3,7}. Before and throughout every run, another logged-in user's process (`clclark7`, command name `associativity`, PID 982794) was pinned at ~100% CPU on logical CPU 5 (physical core 1, the sibling of CPU 1) — confirmed via repeated `mpstat -P ALL` sampling before the run and `ps`/`taskset` checks during it. Runs were pinned to CPU 3 (physical core 3, socket 0, NUMA node 0), a different physical core from the one in use by the other user; its SMT sibling CPU 7 was left idle. `who` showed no interactive login sessions at run time (15 users counted by `uptime` are stale/background sessions), but `ps`/`mpstat` showed the machine was not otherwise quiet — see "Known interference" below.

## Environment
- Compiler + version: GCC 13.3.0 (Ubuntu 13.3.0-6ubuntu2~24.04.1) (see `build/compiler_version.txt`)
- Compiler flags: `-O0 -g -std=c11 -Wall -Wextra -fno-omit-frame-pointer` (Makefile CFLAGS)
- Timer method used (RDTSC/RDTSCP+LFENCE, CNTVCT_EL0, etc.): x86 RDTSC (LFENCE-fenced start) / RDTSCP (LFENCE-fenced stop), batched: N=1000 dependent pointer-chase steps timed per start/stop pair, ticks/access = (t1-t0)/N. See `main_code/x86_64/timer_x86.h`, `main_code/common/benchmark.c`. Disassembly excerpt confirming the loop/timer placement survived -O0 unmodified: `build/cache_bench.source.dis` (functions `x86_tsc_start`, `x86_tsc_stop`, `measure_dependency_chain_batched`).
- Affinity/binding command used: `taskset -c 3 ./cache_bench ...` (see `scripts/run_capacity_full.sh` / `scripts/run_capacity_sweep.sh`)
- NUMA/locality method: no explicit NUMA pinning beyond CPU affinity; single-socket machine (1 NUMA node), so no cross-node concern. Not verified with `numastat` (Phase-I-safe; contains no cache-topology info, TODO if we want to confirm).
- Git commit hash of the code used for these results: 881fbfc00f6bf576dfbc35cf5d97ce2a1aa09ad9 (see `build/git_commit_at_run.txt`, recorded at build+run time)

## Per-Experiment Reproduction

### capacity/
- Source file(s): `main_code/common/{main.c,capacity.c,capacity.h,benchmark.c,benchmark.h,pointer_chase.c,pointer_chase.h,random.c,random.h}`, `main_code/x86_64/timer_x86.h`, `main_code/common/timer.h`
- Build command: `make` (from repo root; see `Makefile`)
- Run command + arguments:
  - **Automated pipeline** (coarse sweep, 1 KiB-64 MiB -> auto boundary detection -> dense +-3 octave sweep per boundary -> 2 repeats on deepest boundary -> 4x tail-extension sweep 64-256 MiB): `./scripts/run_capacity_full.sh charnwood 3 67108864`. Full transcript: `data_raw/charnwood/capacity/run_capacity_full_20260908T221801Z.log`.
  - **Manual follow-up** (the 64-256 MiB tail-extension was still climbing, not flat -- see below): one additional dense sweep 256 MiB-1 GiB plus 2 independent repeats, same flags as the pipeline's dense sweeps (`--points-per-octave 48 --samples 1000000 --batch-size 1000 --warmup-passes 3 --seed 12345 --min-bytes 268435456 --max-bytes 1073741824`, both patterns, `taskset -c 3`), run ad hoc (not via a script) at timestamp `20260908T230824Z`.
  - All runs pinned with `taskset -c 3`
- Sample count: 1,000,000 timed accesses per (size, pattern) point; warm-up (3 full untimed chase passes) excluded from that count
- Random seed(s): 12345 (xorshift32, `make_random_cycle`); sequential-pattern runs use `make_sequential_cycle` (no seed/randomness)
- Raw output filename(s): `data_raw/charnwood/capacity/capacity_coarse_{random,sequential}_20260908T221801Z.csv.gz`, `capacity_coarse_ext_{random,sequential}_20260908T221801Z.csv.gz` (64-256 MiB tail), `capacity_dense{0..5}_{random,sequential}_20260908T221801Z.csv.gz` (one dense window per auto-detected boundary), `capacity_dense5_rep{1,2}_{random,sequential}_20260908T221801Z.csv.gz` (repeats on deepest boundary), `capacity_denseTail_{random,sequential}_20260908T221801Z.csv.gz` (dense sweep past the deepest boundary, 64-256 MiB), `capacity_denseTail2{,_rep1,_rep2}_{random,sequential}_20260908T230824Z.csv.gz` (manual 256 MiB-1 GiB extension, main + 2 repeats). All gzipped (~9x smaller) before committing, per `.gitignore`; summarization was run on the uncompressed originals before compression, so summary CSVs in `data_processed/` are unaffected.
- Processing script -> data_processed path: `python3 scripts/summarize_raw.py <raw.csv> -o data_processed/charnwood/capacity/<name>_summary.csv` for each raw file above (run before gzipping), then `python3 scripts/detect_cache_hierarchy.py data_processed/charnwood/capacity/coarse_combined_random_summary.csv` for the first-pass boundary hypothesis, then `python3 scripts/plot_capacity.py data_processed/charnwood/capacity/*_summary.csv -o data_processed/charnwood/capacity/plots --machine charnwood --boundary 311744 --boundary 1923096 --boundary 2286960 --boundary 5931640 --boundary 7692384 --boundary 11863280` for the final curve and box-plot figures (re-run after the manual 1 GiB extension so the plots include it)
- Excluded runs (if any) and reason: none
- **Auto-detected boundaries** (from `detect_cache_hierarchy.py`, timing-only, on the coarse 1 KiB-256 MiB random-pattern data): 311,744 B (~304 KiB), 1,923,096 B (~1.83 MiB), 2,286,960 B (~2.18 MiB), 5,931,640 B (~5.66 MiB), 7,692,384 B (~7.34 MiB), 11,863,280 B (~11.31 MiB). The detector's naive per-boundary "L1..L6, Memory" labeling should **not** be taken at face value: this chip physically has only L1/L2/L3 (per later literature check in Phase II), so the 4 boundaries between ~1.83 MiB and ~11.31 MiB are very likely partly or wholly artifacts of the interference documented below, not 4 genuine additional cache levels. Only the ~304 KiB (L1) boundary showed low run-to-run spread (5%, 2 runs) in the box-plot check; every boundary from ~1.83 MiB through ~11.31 MiB showed 12-157% run-to-run spread with hundreds of outlier batches per point (see `plots/capacity_boxplots.png` annotations) -- these should be treated as unresolved/contaminated pending a re-run when the machine is quiet, not as confirmed cache-level transitions.

- **CORRECTION (2026-09-10, from already-committed coarse data -- no new runs
  needed): the "~304 KiB (L1)" label above is wrong.** Re-examining the full coarse
  random-pattern curve from 1 KiB through ~680 KiB (not just the narrow dense window
  immediately around 311,744 B) shows there is **no flat shelf anywhere between
  32,768 B and at least 679,912 B** -- it's one continuous, monotonic ramp (7.80
  ticks at 32,768 B up to 33.1 ticks at 679,912 B, noisier than the other machines
  in this exact range consistent with the interference documented below, but still
  clearly one climbing trend, not a step). 311,744 B is just an ordinary point on
  that ramp. The low run-to-run spread originally cited for it (5%, 2 runs) shows
  it was measured precisely, not that it sits at a real knee -- a smooth ramp can be
  measured precisely at any single point without that point being a boundary.
  What actually *is* a genuine flat plateau, missed by the original auto-detector
  run (same failure mode documented on Thunderbird -- no adjacent coarse point
  clears the default `--min-abs-ticks 3.0` even though the region is flat before and
  ramping after): **1,024-30,048 bytes, 26 points, 7.60-7.87 ticks/access** (noisier
  than Sunbird/Skylark/Crux's equivalent region -- consistent with this machine's
  already-documented shared-machine interference -- but still clearly flat, not
  trending), with the ramp clearly underway by 35,728 B (8.33). Sequential-pattern
  control at 32,768 vs. 35,728 B stays flat (7.882 vs 7.890 ticks), confirming this
  is a real random-access cache effect. **Corrected L1 estimate: 32,768 bytes
  (32 KiB)** -- same value independently found the same way on Sunbird
  (hand-confirmed via associativity), Crux, Skylark, Upgrade, and Ookay (see each
  machine's own README) -- consistent across 6 of this team's 7 x86 machines
  checked so far. **PROVISIONAL** (clean in the data; not yet cross-checked by an
  independent test). The already-documented contamination in the ~1.83-11.31 MiB
  region is unaffected by this correction and still needs the planned quiet re-run
  to resolve L2/L3 here -- **do not use 311,744 bytes as an L1 associativity
  stride on this machine; use 32,768 instead if/when associativity is run here.**
- **Known interference (confirmed, not just suspected):** another logged-in user's process (`clclark7`, PID 982794, command `associativity`) ran continuously at ~100% CPU on logical CPU 5 (a different physical core from the one used here) for the entire duration of both the automated pipeline and the manual follow-up. Despite correct core isolation (disjoint physical cores), this produced strong bimodal contamination in the random-pattern medians from ~1.8 MiB through ~20 MiB: many overlapping-window points show two well-separated median clusters (e.g. one cluster ~40-50 ticks, another ~200-250 ticks, at the *same* working-set size across different sweep passes) rather than a single noisy value, consistent with shared-L3/memory-bandwidth contention from the other user's benchmark rather than scheduling jitter alone (see `plot_capacity.py`'s per-point spread notes in the run log). This is the same *category* of multi-tenant interference documented on Sunbird, but here it is a real-time, sustained, identifiable contending process rather than transient OS scheduling noise, so no exact analogue of Sunbird's "different spike locations across independent repeats" argument was needed to demonstrate it -- the contending process was directly observed via `ps`/`mpstat` throughout.
- **Topmost-region plateau check:** the pipeline's default 64-256 MiB tail-extension sweep did *not* flatten -- median latency rose steadily from ~289 ticks (64-80 MiB) to ~321 ticks (203-256 MiB), an ~11% climb with no sign of leveling off, confirmed monotonic across 6 size buckets (see analysis notes below). Per project convention (mirroring the Sunbird follow-up), one additional manual dense sweep (256 MiB-1 GiB) plus 2 independent repeats was run to resolve this. Result: **the region from ~256-600 MiB does look like a genuine, reproducible plateau** (~320-335 ticks/access; run-to-run spread <5% across the 3 independent runs at most points in that band). However, **the climb does not fully stop by 1 GiB** -- mean per-bucket medians continue rising gently in the main run and rep1 (up to ~335-347 ticks by 800 MiB-1 GiB), and one of the two repeats (`rep2`) diverges sharply above ~700 MiB (up to 412 ticks by the 812-1024 MiB bucket, vs. ~330-337 for the main run and rep1 in the same range; run-to-run spread reaches 17-43% in that band). This late-onset, single-repeat divergence is most plausibly additional shared-machine memory-pressure interference (system swap was already 2.6-2.8 GiB in use out of 4 GiB total at run time, from other users' processes) rather than a cache-hierarchy effect, but it was not run down further -- per the pipeline's own non-adaptive design philosophy, this is flagged as **still open** rather than chased with further extensions. **Bottom line: no hard capacity plateau was reached within the swept range (up to 1 GiB); the gentle ~11-15% rise from ~12 MiB to ~600 MiB is reproducible and likely reflects real TLB/page-walk-driven latency growth as the working set exceeds TLB reach (to be confirmed in Phase II), while the instability above ~700 MiB is most likely shared-machine noise and needs a re-run on a quiet machine to resolve.**

### line_size/
- Source file(s): `main_code/common/{main.c,line_size.c,line_size.h,benchmark.c,benchmark.h,pointer_chase.c,pointer_chase.h,random.c,random.h}`, `main_code/x86_64/timer_x86.h`, `main_code/common/timer.h`
- Build command: `make` (from repo root; see `Makefile`)
- Run command + arguments: `./scripts/run_line_size.sh charnwood 3 32768,262144,8388608` (default coarse strides `8,16,32,64,128,256`; no `candidate_overrides_csv` given, so Method-A step 4 -- bracket+offset refinement -- was skipped for every level per the script's own no-auto-detection policy; see per-level results below for the candidates to use on a follow-up run). Boundaries are this machine's own established capacity-experiment values from the `capacity/` section and `CAPACITY_RESULTS.md`'s consolidated table: L1=32,768 B (PROVISIONAL, corrected estimate), L2=262,144 B (team judgment call inside a smooth, boundary-free ramp -- **not** a confirmed knee), L3=8,388,608 B (rounded to a clean power of two from the team's "~8 MiB" call inside the documented noisy/contaminated ~4-13 MiB transition -- **not** a confirmed knee either). Pinned `taskset -c 3` (physical core 3, CPUs {3,7} -- same core as the capacity run; confirmed idle via `mpstat -P ALL` and `mpstat -P 3,7` across 3 sampling windows immediately before this run, and via `ps --sort=-pcpu` showing no other user's process above ~2% CPU at run time -- a quieter session than the capacity run's, which shared the machine with another student's `associativity` benchmark). Full transcript: `data_raw/charnwood/line_size/run_line_size_20260912T214225Z.log`.
- Sample count: 1,000,000 timed accesses per (stride, footprint) point for Method A (family of curves) and per (stride) point for Method B (single curve); warm-up (2 full untimed chase passes for A, 3 for B) excluded from that count
- Random seed: 12345 (xorshift32, `make_random_cycle`); sequential-pattern runs (Method A's prefetcher-sensitive control curve only) use the corresponding sequential cycle constructor, no seed/randomness
- **Results, per level (Method A steps 1-3 -- family of curves -- always ran; step 4 skipped everywhere, see above; Method B ran alongside per the script's design but found no usable transition at any level):**
  - **Level boundary=32,768 B (L1) -- clean, textbook family-of-curves split. Candidate line size: 64 B.** `plots/line_size_family_curve.png`: strides 64/128/256 B jump straight to a flat ~13.8-14.0 ticks/access plateau right at the 32,768 B boundary and stay flat through the rest of the window (2^15-2^17 B), while strides 8/16/32 B show a much slower, still-climbing ramp over the same range, only approaching the same latency near the window's right edge (2^17 B) -- the same "two visually distinct clusters" signature documented on Sunbird/Crux/Skylark/Upgrade/Ookay for their own L1 levels, all of which independently landed on 64 B too. The boxplot at the diagnostic elbow (`plots/line_size_family_boxplots.png`) confirms this sharply: at ~35.875 KiB, 64 B and 128 B already read ~13.8-13.9 ticks (tight boxes, no overlap with the other cluster) while 16 B and 32 B still read ~8.8 ticks. `plot_line_size_family.py`'s own diagnostic (not auto-applied) independently reports 64 B here too. Method B found no transition in its own coarse sweep at this level's footprint (65,536 B, 2.0x the boundary) -- not a contradiction, matches the same "no separation at this footprint" pattern seen on other machines' non-L1 levels. **Recommended next step: re-run with `candidate_overrides_csv` step-4 candidate `64` for this level to get the bracket+offset confirmation (Sunbird's authoritative methodology).**
  - **Level boundary=262,144 B -- no clean split; one continuous, stride-ordered ramp, consistent with this NOT being a genuine cache-level boundary here.** `plots/line_size_family_curve.png` shows all 6 stride curves climbing together from ~14 to ~40-43 ticks across the whole 2^15-2^20 B window with no flat elbow for any stride -- curves separate by stride magnitude (larger stride = higher latency, the expected background effect of a random chase touching more distinct lines) but never plateau. This matches the capacity/ section's finding that the ~180-370 KiB region here is "a smooth, low-noise monotonic ramp with no plateau at 256 KiB" -- i.e. 262,144 B is a team judgment call, not a hardware-confirmed edge, and the line-size family-of-curves method (which needs a real capacity transition to anchor against) correctly shows no usable signal here either. `plot_line_size_family.py`'s own diagnostic reports 64 B by its fixed-grid elbow-agreement heuristic, but this should **not** be trusted given the visibly non-textbook (no-plateau) curve shape -- treat as "no result" for this level. Method B also found no transition in its coarse sweep.
  - **Level boundary=8,388,608 B (~8 MiB) -- no clean split; noisy, consistent with the documented capacity contamination in this region.** `plots/line_size_family_curve.png` shows all 6 strides overlapping almost exactly through ~2^22 B, then rising together through a single steep, non-monotonic transition (~2^22.5-2^25 B) with strides crossing each other repeatedly (e.g. 64 B briefly highest, then 128 B overtakes) rather than settling into two clean clusters -- the same shared-machine-interference signature already documented for this exact byte range in the capacity/ section (12-157% run-to-run spread, bimodal contamination from another user's concurrent process during that earlier run). `plot_line_size_family.py`'s diagnostic elbow values are themselves non-monotonic across strides (e.g. 64 B's elbow is *lower* than 32 B's and 128 B's), the tell-tale sign this heuristic is not finding a real signal here. Method B found no transition either. **No line-size candidate should be cited from this level** until the underlying ~4-13 MiB capacity contamination is resolved with a quiet re-run (see capacity/ section's outstanding TODO).
- **Cross-level takeaway:** only the L1 level (32,768 B, the one boundary this machine has independently confirmed via a clean flat-then-ramp signature) produced a textbook family-of-curves result, and it agrees with every other x86 machine on this team so far (**64 B**). The two deeper levels tested here are not yet confirmed capacity boundaries on this machine (per capacity/ section), and the family-of-curves method correctly failed to find a clean split at either -- this is expected behavior, not a tool failure, and is itself indirect evidence that 262,144 B and 8,388,608 B are not real hardware transitions on this machine. Re-running line_size against a genuinely confirmed L2/L3 boundary (once the capacity/ follow-up quiet re-run happens) is the natural next step, not retrying more strides at these same two footprints.
- Notes on alignment/candidate strides tested: coarse strides 8/16/32/64/128/256 B (project default), 6 points/octave, `--align-bytes 4096`, `--offset-bytes 0` (step-4 offset sweep not run, see above). No non-default candidates tested this session.

- **Follow-up run (same session): L1 step-4 confirmation attempt + independent re-run of the L2/L3 family-of-curves sweeps.** `./scripts/run_line_size.sh charnwood 3 32768,262144,8388608 8,16,32,64,128,256 64,,` -- candidate `64` supplied only for the L1 level (L2/L3 fields left blank, so their step 4 was skipped again; their Method-A steps 1-3 re-ran from scratch with fresh random samples as a reproducibility check). Core re-confirmed idle immediately before this run (`mpstat -P 3,7` across 3 windows, `ps --sort=-pcpu` showing no other user above ~2% CPU). Timestamp `20260912T215548Z`, full transcript `data_raw/charnwood/line_size/run_line_size_20260912T215548Z.log`.
  - **L1 (32,768 B) step 4 -- mostly clean, one flagged anomaly; net verdict: 64 B remains the best-supported candidate but is not yet citable as fully clean.** Bracketed strides 40/48/56/64/72/80/88 B at 8 node-0 offsets (0-56 B, 8 B steps), pattern=random. `plots/line_size_offset_elbow.png`: 7 of the 8 offsets (0,16,24,32,40,48,56 B) trace an essentially identical, symmetric V bottoming exactly at the 64 B candidate. **Offset=8 B is the one outlier**, reading anomalously high detected-elbow footprints at strides 48 B and 64 B (65,520-65,536 B, vs. ~41,000-52,000 B for every other offset at those same strides) -- this single offset's deviation is what drove the script's own auto-verdict ("elbow varies 1.78x across offsets ... do not cite without further investigation"). Critically, this is an artifact of the *elbow-location heuristic* on a noisy single point, not a disagreement in the underlying latency: `plots/line_size_offset_boxplots.png` (raw latency at the fixed 35.875 KiB footprint, stride=64 B, across all 8 offsets) shows every offset -- **including offset=8 B** -- reading a tight, overlapping ~13.6-14.0 ticks/access box, no offset standing apart from the rest. So the plateau latency itself is offset-insensitive at 64 B across all 8 offsets tested; only one offset's *elbow-footprint estimate* (a secondary, noisier derived quantity from a small 7-point bracket window) came out anomalous. **Net read: 64 B is well-supported** (clean coarse-pass split + 7/8 clean offset elbows + offset-insensitive plateau latency across all 8), matching every other x86 machine on this team, but per this project's Phase-I discipline (see Sunbird's own similarly-noisy single-run step-4 caveat) it is being recorded as *supported, not independently re-confirmed clean* -- a repeat of just this step-4 bracket (different seed) would settle whether offset=8B's anomaly reproduces or was a one-off.
  - **L2 (262,144 B) and L3 (8,388,608 B) re-run: reproduced, not resolved.** Both `plots/line_size_family_curve.png` plots from this independent re-run (fresh random samples, same seed/core/sample-count) are visually near-identical in shape and value to the first run's -- L2 still one smooth stride-ordered ramp with no plateau anywhere in [32,768, 1,048,576] B; L3 still a single steep, non-monotonic, stride-crossing transition in roughly [2^22.5, 2^25] B with the same lack of two-cluster separation. This reproducibility is itself informative: it rules out "the first run's noisy result was a one-off sampling fluke" -- the absence of a clean line-size split at these two footprints is a *stable, repeatable* finding on this machine, consistent with the capacity/ section's conclusion that neither 262,144 B nor 8,388,608 B is a confirmed real cache-level boundary here. No line-size candidate is cited for either level.
- **Updated cross-level takeaway:** L1 = **64 B**, well-supported by two independent lines of evidence (coarse family-of-curves split + step-4 offset check, one offset flagged but explained above) and consistent with every other x86 machine on the team. L2/L3 line-size candidates remain unobtainable at these footprints, now confirmed reproducible across two independent runs -- next step for those two levels is resolving the underlying capacity contamination (quiet re-run per the capacity/ section TODO), not further line_size attempts at the same footprints.

- **Third run (same session, at user request): forced step-4 candidate=64 for L2 and L3 too, to get the same bracket+offset confirmation check L1 got.** `./scripts/run_line_size.sh charnwood 3 32768,262144,8388608 8,16,32,64,128,256 64,64,64`. Core re-confirmed idle beforehand. Timestamp `20260912T220221Z`, transcript `data_raw/charnwood/line_size/run_line_size_20260912T220221Z.log`. L1's own step 4 reproduced cleanly again this run (7/8 offsets exact match at 36,736 B, 1/8 offset with no detectable elbow at all -- consistent with, if anything cleaner than, the second run's result). **L2 and L3 results below -- read in full before citing 64 B at either level; the automated step-4 checker's own "8/8 agree" verdict for L2 is misleading here, not a genuine confirmation:**
  - **L2 (262,144 B): the checker reported "elbow stable across 8/8 offsets (524,288 B, 1.00x spread) -- consistent with a genuine, alignment-independent transition," but this does NOT hold up as real line-size evidence once the OTHER bracket strides are checked.** `plots/line_size_offset_elbow.png` shows the *same* 524,288 B (2^19) elbow value recurring not just for stride=64 B but also for stride=40, 48, 56, 72, and 80 B, across most offsets -- a genuine, stride-specific line-size transition should behave *differently* below vs. at/above the true line size (that's the entire premise of the family-of-curves method), not land on the identical footprint value regardless of which of 7 different strides was used. The boxplot at that point (`plots/line_size_offset_boxplots.png`, "candidate stride 64 B at footprint 512 KiB") shows a consistent ~34-35 ticks/access across all 8 offsets, but 512 KiB is just an ordinary mid-ramp point on the same smooth, boundary-free climb already documented in the coarse family curve and in the capacity/ section (33.1 ticks was already logged at a nearby size in the capacity sweep) -- there is no flat plateau here for any stride, so "8/8 offsets agree" is agreeing on a point along a shared ramp, not on a shared plateau edge. This is the exact failure mode Sunbird's own line_size/ writeup warned about: a step-4 offset check can look perfectly clean while still not indicating a real line-size knee. **Verdict: 64 B is NOT confirmed at the L2 footprint; the clean-looking step-4 result here is a false positive, most likely explained by every stride's own coarse elbow-detector locking onto the same isolated bump/measurement artifact in this level's shared, un-plateaued ramp.**
  - **L3 (8,388,608 B): step 4 remains unresolved/noisy, consistent with every prior run at this level.** `plots/line_size_offset_elbow.png` shows wide, inconsistent scatter -- most offsets cluster loosely around ~15 MiB for strides 64-88 B, but two offsets (8 B, 48 B) spike to ~30 MiB (near the very top of this level's swept window) at stride 72 B and 64 B respectively. The script's own verdict: "elbow varies 2.00x across offsets (14,946,752-29,893,568 bytes) -- do not cite this candidate as a clean line-size result." This matches every earlier finding for this level (contaminated ~4-13 MiB capacity region, no plateau in the coarse family curve, non-monotonic per-stride elbows) -- **64 B is not supported here either, and no candidate is citable at this level from any run so far.**
- **Final cross-level takeaway (all three runs considered): only L1 = 64 B is a real, citable Phase-I line-size result on this machine.** L2 and L3 both sit on footprints charnwood's own capacity data has never confirmed as genuine cache-level boundaries, and forcing a step-4 check at either one produces results that look superficially clean (L2) or genuinely inconclusive (L3) but do not survive closer inspection (L2's "agreement" recurs across unrelated strides; L3's does not agree at all). Resolving the capacity/ section's outstanding quiet-machine re-run for the ~4-13 MiB region remains the prerequisite for a trustworthy L2/L3 line-size attempt here, not additional step-4 candidates at these two footprints.

- **Fourth run (same session, at user request): independent reproducibility check of L2/L3 step 4, fresh timing samples, same candidate=64.** `./scripts/run_line_size.sh charnwood 3 262144,8388608 8,16,32,64,128,256 64,64` (L1 not re-tested this time). Core re-confirmed idle beforehand. Timestamp `20260912T225441Z`, transcript `data_raw/charnwood/line_size/run_line_size_20260912T225441Z.log`.
  - **L2: the "8/8 offsets agree at 524,288 B" result reproduced EXACTLY on independent samples** (524,288-524,288 B, 1.00x spread, all 8 offsets again) -- this rules out one-run sampling luck as the explanation. But the full per-offset elbow table (`run_line_size_20260912T225441Z.log` lines 226-233) shows this is because **every other bracket stride tested (40, 48, 56, 72, 80, 88 B) also lands in the same tight 467,016-588,480 B range**, not just 64 B -- e.g. offset=0B alone gives elbows of 524280/524256/524272/**524288**/524232/524240/588456 for strides 40/48/56/**64**/72/80/88 respectively, a <13% spread across a 2.2x range of stride values. **This reproducibility makes the finding stronger, not weaker, in the opposite direction from what a citable line-size result would need**: it confirms the ~512-590 KiB elbow is a stable, real, alignment-independent feature of this level's data -- but one shared almost identically by every stride tested, which is inconsistent with it being a stride-dependent line-size knee (the entire premise of the family-of-curves method is that strides below vs. at/above the true line size behave *differently*). The far more likely explanation, consistent with the coarse family-of-curves plot showing one continuous ramp with no plateau for any stride: this is the coarse elbow-detector locking onto the same shared point of maximum curvature in the ramp's shape (all 6+ stride curves in this window have visually similar shape, just vertically offset), which is a capacity-region artifact common to the whole family, not a per-stride effect. **Still not citable as a line-size result**, now for a stronger reason than before (confirmed real+reproducible, but proven stride-independent rather than merely suspected so).
  - **L3: step 4 remained noisy and did NOT reproduce the same values as the third run** (this run: "elbow varies 1.78x across offsets, 9,415,872-16,777,216 bytes"; third run: "elbow varies 2.00x, 14,946,752-29,893,568 bytes") -- different exact numbers each time, unlike L2's exact reproduction, which is itself further confirmation that this level's step-4 signal is genuine sampling noise (consistent with the documented ~4-13 MiB capacity contamination) rather than a stable-but-misleading artifact like L2's. **Not citable**, as before.
- **Conclusion after 4 runs: 64 B stands only for L1. Do not record 64 B as an L2 or L3 result** -- L2's step-4 check is real and repeatable but demonstrably not stride-specific (fails the method's own core assumption), and L3's is simple run-to-run noise. If this needs revisiting, the prerequisite is the same as before: resolve the capacity/ section's ~4-13 MiB contamination on a quiet machine first, rather than repeating step 4 at these same two footprints again.

- **Fifth run (same session, at user request): plain family-of-curves re-run for L2/L3 (Method A steps 1-3 + Method B, no step-4 override), a third independent sample.** `./scripts/run_line_size.sh charnwood 3 262144,8388608`. Core re-confirmed idle. Timestamp `20260913T135833Z`, transcript `data_raw/charnwood/line_size/run_line_size_20260913T135833Z.log`.
  - **L2 Method A (family of curves): reproduced a third time, unchanged conclusion.** `plots/line_size_family_curve.png` is visually indistinguishable in shape from the two prior runs -- one continuous ramp, no plateau for any of the 6 strides. No line-size candidate from this method.
  - **L2 Method B (single-curve): this run is the first of five total runs where it actually detected a transition ("64") and ran the dense+repeat stages** -- previous runs at this exact footprint (524,288 B) all reported "none." On inspection this is NOT the textbook rising-then-flat saturation signature `detect_line_size.py` is designed to find: the coarse curve (`plots/line_size_curve.png`) climbs from ~32 to ~38 ticks over strides 8-130 B, then *declines* from ~38 down to ~13 ticks as stride keeps increasing to 1024 B -- a falling trend, not a plateau. This is the exact "capacity-driven fall-off" artifact already documented and explicitly warned about in this project's own methodology (see Sunbird's line_size/ writeup: at a fixed footprint, growing the stride shrinks the number of distinct nodes actually chased, so very large strides can make the working set collapse back toward a smaller cache level, producing a falling tail that is NOT the line size). The dense-window boxplot at the detected "64" (`plots/line_size_boxplots.png`) confirms this reading: 63 B/64 B/65 B medians are 35.5/34.4/36.0 ticks -- non-monotonic (a dip, not a rising-then-flat step) and already well below the ~38-tick peak seen at ~130 B, i.e. squarely inside the declining tail, not at a genuine saturation edge. Run-to-run spread is tiny (0-1% across the repeats) only because the tail's local shape is itself reproducible, not because it's a real signal. **This "64" from Method B is a spurious detection inside a known-and-documented artifact region, not new evidence for a real L2 line size.**
  - **L3 Method A (family of curves): reproduced a third time, with striking numerical consistency in the per-stride diagnostic elbow values.** `plots/line_size_family_curve.png` is again visually indistinguishable from both prior runs. The auto-diagnostic's per-stride elbow footprints this run ({8B: 21,137,968; 16B: 21,137,968; 32B: 18,831,776; 64B: 14,946,752; 128B: 18,831,744; 256B: 18,831,616}) are nearly byte-identical to the second run's ({..., 128B: 21,137,920, otherwise identical}) -- i.e. this non-monotonic, stride-independent elbow pattern (64 B's own elbow sits *lower* than both 32 B's and 128 B's, both times) is itself a stable, repeatable feature of this level's data, not noise -- further reinforcing the "shared capacity-region artifact, not a per-stride line-size signal" reading already established for this level. L3 Method B again found no transition.
- **Updated final conclusion (5 runs total): 64 B remains citable for L1 only.** Both deeper levels have now been independently re-sampled three times each (Method A) with visually and numerically consistent "no real plateau" results, plus two step-4 attempts (run 3-4) and one Method-B false-positive (run 5) that each looked momentarily promising but did not survive inspection once compared across strides or against the known capacity-driven-fall-off failure mode. This is about as thorough a null result as Phase I timing alone can produce at these two footprints -- the path forward is still the capacity/ section's outstanding quiet-machine re-run of the ~4-13 MiB region, not further line_size attempts here.

- **Sixth run (same session, at user request): third independent step-4 attempt for L2/L3, candidate=64 for both.** `./scripts/run_line_size.sh charnwood 3 262144,8388608 8,16,32,64,128,256 64,64`. Core re-confirmed idle beforehand. Timestamp `20260913T141559Z`, transcript `data_raw/charnwood/line_size/run_line_size_20260913T141559Z.log`.
  - **L2: the cross-stride-collapse pattern reproduced a third time, unchanged conclusion.** `run_line_size_20260913T141559Z.log` lines 226-233: every bracket stride (40-88 B) again lands in the same narrow ~467,016-660,528 B band regardless of which stride was used, the same signature documented on the two prior L2 step-4 attempts. Still not citable -- reproducible, but not stride-specific.
  - **L3: this attempt is qualitatively different from its own two predecessors, and worth flagging even though it isn't being called a confirmed result.** Unlike the third and fourth runs (2.00x and 1.78x scattered spread, no agreement), this run's `plots/line_size_offset_elbow.png` shows a textbook step shape -- strides 40/48 B elbow at ~21.9 MiB, 56 B at ~16.8 MiB, and strides 64/72/80/88 B all settle to an extremely tight ~14.9469-14.9470 MiB band -- **and all 8 node-0 offsets are byte-for-byte identical at every stride** (`run_line_size_20260913T141559Z.log` lines 226-233 read as one repeated row). This is structurally different from L2's failure mode: here, strides *below* the candidate genuinely disagree with strides *at/above* it, rather than every stride collapsing to the same value -- the actual premise the family-of-curves method depends on. The latency boxplot at this point (`plots/line_size_offset_boxplots.png`, footprint 14.25 MiB) shows consistent ~294-300 tick medians across all 8 offsets, though with wide whiskers (~265-335, reflecting this level's already-documented capacity-region noise). **Read with real caution despite the clean shape: this is the ONE clean result out of THREE independent step-4 attempts at this exact candidate/level** (third and fourth runs both scattered); a single clean run against two noisy ones is not yet a reproducible pattern by this project's own standard (contrast L1's and L2's results, both of which reproduced their respective verdicts on *every* attempt). The perfect offset-to-offset byte-identical agreement is itself slightly suspicious rather than purely reassuring -- real independent timing samples this project has collected elsewhere always show at least sub-percent jitter (see L1's and L2's own offset tables); identical values to the byte across 8 separate benchmark invocations is more consistent with all 8 landing on the same underlying log-spaced grid point than with 8 independently-resolved measurements. **Not citable yet** -- needs at least one more independent repeat to see whether this clean shape reproduces or was this run's turn to get lucky, the same bar L1 already cleared twice.
- **Updated final conclusion (6 runs total): 64 B remains citable for L1 only.** L2 is now a settled null result (three consistent step-4 attempts, all showing the same non-stride-specific collapse). L3 is unresolved rather than settled -- two clean misses and one promising-but-unverified hit at the same candidate -- and needs one more independent step-4 repeat before this run's clean shape can be trusted over its own two predecessors' noise.

- **Seventh run (same session, at user request): fourth independent step-4 attempt for L2/L3, candidate=64 for both -- the requested tie-breaker for L3.** `./scripts/run_line_size.sh charnwood 3 262144,8388608 8,16,32,64,128,256 64,64`. Core re-confirmed idle beforehand. Timestamp `20260913T142351Z`, transcript `data_raw/charnwood/line_size/run_line_size_20260913T142351Z.log`.
  - **L2: reproduced the cross-stride collapse a fourth consecutive time** (524,288 B for stride 64, same ~467K-660K band for every other bracket stride, same as all three prior attempts). Four-for-four, still not stride-specific -- settled null result, no further attempts needed here.
  - **L3: re-examining all FOUR step-4 attempts together (not just this run in isolation) changes the read from the prior entry -- this now looks like a real, reproducible signal, not a coin-flip.** The auto-printed "spread" verdict for each run (2.00x / 1.78x / 1.00x / 1.12x this run) is misleading in isolation because it's just `max/min` over 8 offsets, so a single scattered outlier cell inflates it even when the other 7 agree. Tabulating the actual stride=64 elbow value across all 8 offsets in each of the 4 independent runs:
    - Run 3 (20260912T220221Z): 7/8 offsets = 14,946,752 B (1 outlier: offset=48B -> 29,893,568)
    - Run 4 (20260912T225441Z): 6/8 offsets = 14,946,752 B (2 outliers: offset=16B -> 9,415,872; offset=32B -> 16,777,216)
    - Run 6 (20260913T141559Z): 8/8 offsets = 14,946,752 B (0 outliers)
    - Run 7 (20260913T142351Z, this run): 6/8 offsets = 14,946,752 B (2 outliers: offset=24B, 48B -> 13,316,032)
    **27 of 32 offset-trials across 4 independent runs (84%) agree on the exact same elbow value, 14,946,752 B (~14.25 MiB), at the candidate stride.** The below-candidate strides are similarly consistent run-to-run: stride 40 B reads ~16.8-18.8 MiB and stride 56 B reads ~16.8 MiB in every single run, both clearly and consistently above the 64+ B cluster. The scattered outlier cells land at a *different* offset each run (48B, then 16B/32B, then none, then 24B/48B) -- exactly the "single-point interference spike, different location per run" signature already well-documented elsewhere in this project (Sunbird/Crux/Ookay/Thunderbird capacity follow-ups all needed a median-of-repeats step for the same reason), not evidence against the underlying elbow. **Correction to the prior entry's verdict: this is not "one clean run against two noisy ones" -- it's a consistent majority signal in all four runs, obscured by trusting the auto-printed min/max spread statistic instead of looking at the full per-offset distribution.**
  - **Revised L3 read: candidate 64 B is reasonably well-supported by the step-4 check after 4 independent repeats** (84% direct offset agreement on one exact value, a consistent and much higher elbow for strides below it in every run) -- though still with real caveats before calling it a clean Sunbird-grade result: (1) the per-run interference-outlier rate (12-25% of offsets per run) is itself a symptom of this level's documented ~4-13 MiB capacity contamination and hasn't gone away; (2) unlike L1's result, this has not been cross-checked against a Method-B (single-curve) confirmation -- Method B has never found a transition at this footprint (16,777,216 B) in any of the 4 runs it was tried.
- **Updated final conclusion (7 runs total): L1 = 64 B remains the cleanest, most-confirmed result. L2 = settled null (4/4 consistent non-stride-specific collapse, do not cite). L3 = 64 B is now reasonably supported (84% direct agreement across 4 independent step-4 repeats, consistent step shape in every run) but should be reported as "supported by repeated step-4 evidence, not yet independently corroborated by Method B or a second method the way L1 was" -- a meaningfully stronger position than the "not citable" verdict recorded after runs 3-6, on the same underlying data, once analyzed per-offset rather than via the summary spread statistic alone.**

- **Eighth run (same session, at user request): fifth independent step-4 attempt for L2/L3, candidate=64 for both.** `./scripts/run_line_size.sh charnwood 3 262144,8388608 8,16,32,64,128,256 64,64`. Core re-confirmed idle beforehand. Timestamp `20260913T144034Z`, transcript `data_raw/charnwood/line_size/run_line_size_20260913T144034Z.log`.
  - **L2: reproduced the cross-stride collapse a fifth consecutive time.** Five-for-five now; this level is a fully settled null result, no further step-4 attempts planned.
  - **L3: this run is the noisiest of the five step-4 attempts, and the noise this time is NOT confined to the candidate stride.** Only 4/8 offsets hit the mode value 14,946,752 B at stride=64 (offsets 0, 16, 48, 56 all read something else this run) -- but critically, the *below-candidate reference strides also got noisier this run* (e.g. offset=48B: stride 40 -> 11,863,280, far below its usual ~18.8 MiB; offset=56B: stride 40 -> 23,726,560, well above its usual range) -- unlike runs 3/4/6/7, where the below-candidate strides stayed tight even when stride=64 had an outlier. This reads as this run having simply been noisier overall (consistent with the level's well-documented capacity-region contamination flaring up session-to-session) rather than the stride=64 elbow specifically becoming unreliable relative to its neighbors -- but it does pull down the aggregate agreement rate and is reported honestly rather than dropped. **Updated tally across all 5 runs: 31/40 offset-trials (77.5%) agree on 14,946,752 B at stride=64** (was 84% after 4 runs) -- still a clear majority and still the same exact value every time it does agree, but the margin is not growing with more repeats the way L1's or L2's results did, which is itself informative (a truly clean signal like L1's tends to get cleaner or stay flat with more repeats, not erode).
- **Updated final conclusion (8 runs total): L1 = 64 B (strongest, most-repeated result). L2 = settled null across 5/5 step-4 attempts, do not cite. L3 = still majority-favors-64B (77.5% of 40 offset-trials across 5 independent runs) but the margin is eroding rather than firming up with more data, and it remains uncorroborated by Method B at any of the 5 attempts -- report as "leaning toward 64 B, not a settled Phase-I result" rather than either "confirmed" or "ruled out."**

- **Ninth run (same session, at user request): sixth independent step-4 attempt, L3 only (L2 not re-tested -- already settled at 5/5).** `./scripts/run_line_size.sh charnwood 3 8388608 8,16,32,64,128,256 64`. Core re-confirmed idle beforehand. Timestamp `20260913T144937Z`, transcript `data_raw/charnwood/line_size/run_line_size_20260913T144937Z.log`.
  - **Stride=64 column this run: 5/8 offsets at the mode 14,946,752 B** (`run_line_size_20260913T144937Z.log` lines 226-233); 3 outliers, including one offset (8B) where no elbow was found at all within the window and the detector defaulted to the window ceiling (33,554,432 B, this level's `--max-bytes`) -- a "no signal" result reported as a number, not a genuine competing elbow. **Notably, this run's below-candidate strides (40/48/56 B) and above-candidate strides (72/80/88 B) both stayed tight** (72: 7/8 at mode; 80: 7/8; 88: 8/8 -- perfect) -- unlike run 8, where the noise spread into the reference strides too, this run's extra scatter is concentrated specifically at stride=64 itself, which is a different and slightly more concerning failure mode (it suggests the candidate stride itself, not just general session noise, is what's hardest to pin down cleanly here).
  - **Updated running tally across all 6 step-4 attempts: 36/48 offset-trials (75%) agree on 14,946,752 B at stride=64.** Trend across runs: 7/8, 6/8, 8/8, 6/8, 4/8, 5/8 -- oscillating in a 4-8 range with no clear upward or downward trend, i.e. this is best read as a noisy-but-real majority signal that has now plateaued around ~75-85% agreement rather than converging toward either "always agrees" (L1's pattern) or "never agrees" (L2's pattern) with more repeats.
- **Updated final conclusion (9 runs total, unchanged in substance from run 8): L1 = 64 B, strongest result. L2 = settled null, 5/5. L3 = 64 B remains the majority/leading candidate (75% agreement, 6 independent repeats) but should still be reported as "leaning, not settled" -- six repeats have now stabilized the agreement rate around three-quarters rather than pushing it toward either extreme, and Method B has still never corroborated it at this footprint. Further repeats of the same step-4 check are unlikely to resolve this further on their own; the same underlying blocker as before applies -- this level's ~4-13 MiB capacity contamination needs a quiet-machine re-run before line_size evidence here can be fully trusted either way.**

### associativity/
- Source file(s): `main_code/common/{main.c,associativity.c,associativity.h,benchmark.c,benchmark.h,pointer_chase.c,pointer_chase.h,random.c,random.h}`, `main_code/x86_64/timer_x86.h`, `main_code/common/timer.h`; pipeline: `scripts/{run_associativity_full.sh,summarize_raw.py,detect_associativity.py,plot_associativity.py}`
- Build command: `make` (from repo root; see `Makefile`); git commit at run time: `881fbfc00f6bf576dfbc35cf5d97ce2a1aa09ad9`
- Run command + arguments: `./scripts/run_associativity_full.sh charnwood 3 32768,262144,8388608` (core 3, same physical core as the capacity run). `cache_bytes` per level taken as an explicit hand-picked override from `CAPACITY_RESULTS.md`'s consolidated table (L1=32,768 B, L2=262,144 B, LLC rounded to the nearest power of two from that table's "~8 MiB" LLC entry = 8,388,608 B) — **not** re-derived from this machine's own capacity data. This is a deliberate departure from the pipeline's stated preference for a hand-*confirmed* per-machine value: Charnwood's own capacity README (see `capacity/` section above) explicitly documents that only the ~304 KiB/32,768 B L1 boundary is clean on this machine — the L2/LLC region (~1.83-11.31 MiB) is flagged unresolved/contaminated pending a quiet re-run, and that section says outright "use 32,768 [for L1]... if/when associativity is run here" without endorsing an L2/LLC number. The L2/262,144 B and LLC/~8 MiB values used here come only from the cross-machine consolidated table, on the basis that 6-of-8 machines converge on those same round numbers — see "Known limitations / open questions" below for why the results obtained this way should not be over-read regardless.
- Machine was confirmed quiet before this run (see Machine Identification section above for the standing multi-tenant-interference finding from the capacity run): `who`/`mpstat -P ALL`/`ps --sort=-pcpu` all showed logical CPUs 3 and 7 (physical core 3) idle and no other user process consuming CPU, unlike the capacity run's documented contention from another student's `associativity` process (PID 982794, since exited).
- Sample count: 1,000,000 timed accesses per (num_ways, pattern) point (`--samples 1000000 --batch-size 1000`), 3 untimed warmup passes; num_ways swept 2-40 (`--min-ways 2 --max-ways 40 --way-step 1`), both `random` and `sequential` patterns, base run (seed 12345) + 2 reproducibility repeats (seeds 12346, 12347 — see `associativity.h`/pipeline header comment on why repeats vary the seed rather than reusing one)
- Conflict-set construction method: node-to-node stride fixed at `cache_bytes` (the candidate level's own capacity), which forces every probed node into the same cache set with a distinct tag regardless of unknown line size/way count (see `main_code/common/associativity.h`'s docstring for the argument); num_ways probed nodes are chased in one dependent cyclic pointer chase, sweeping num_ways from 2 to 40 to find the hit -> thrashing knee.
- Raw output filenames: `data_raw/charnwood/associativity/{L1,L2,L3_LLC}/associativity_{base,rep1,rep2}_{random,sequential}_20260912T225012Z.csv.gz`; full run transcript: `data_raw/charnwood/associativity/run_associativity_full_20260912T225012Z.log`
- Processed summaries / plots: `data_processed/charnwood/associativity/{L1,L2,L3_LLC}/{base,rep1,rep2}_{random,sequential}_summary_20260912T225012Z.csv`, `data_processed/charnwood/associativity/{L1,L2,L3_LLC}/plots/associativity_{curve,boxplots}.{png,pdf}`

**Results:**
| Level | cache_bytes | Base estimate | Repeat 1 | Repeat 2 |
|---|---|---|---|---|
| L1 | 32,768 B (32 KiB) | 8 | 8 | 8 |
| L2 | 262,144 B (256 KiB) | 4 | 4 | 4 |
| L3/LLC | 8,388,608 B (8 MiB) | 4 | 4 | 4 |

- **L1 = 8-way: clean, matches the team-wide result.** Single sharp knee (flat ~7.6 ticks/access through num_ways=8, jump to ~19-23 at 9+), 0 disagreement across base + 2 repeats, both patterns track together. Same shape and same estimate as Sunbird's and Upgrade's independently hand-confirmed L1 = 8-way results (see `CLAUDE.md`'s associativity section). This is the one level on this machine where the result should be trusted at face value.
- **L2 and LLC did NOT produce trustworthy independent numbers — new, unusually direct evidence for the team's already-documented DTLB/small-structure confound.** Both curves are multi-step staircases, not single knees: a first jump at num_ways=4 (7.56 -> 15.6 ticks), a second, sharper jump at num_ways=9 (15.6 -> 21.9 ticks) — the same num_ways=9 anomaly CLAUDE.md documents recurring across every Sunbird large-stride candidate tried — then a gradual, noisy climb from ~num_ways=24 onward (25-45% run-to-run spread per the pipeline's own overlap warnings, consistent with genuine measurement noise at that depth rather than a third real knee). `detect_associativity.py` only reports the first knee, hence "4" for both levels.
  - **The striking new fact: L2 (256 KiB stride) and LLC (8 MiB stride) — a 32x difference in the candidate capacity — produce median latencies that agree to within ~0.05 ticks/access at every num_ways from 2 through 23** (e.g. 21.892 vs. 21.892 at num_ways=9, 21.898 vs. 21.872 at num_ways=20; see the two levels' `base_random_summary_20260912T225012Z.csv` files). Two genuinely different cache levels, being probed at their own real distinct capacities, have no reason to produce numerically indistinguishable curves — this is direct, on-this-machine evidence that both "L2" and "LLC" runs are measuring the same small, capacity-independent structure (most likely the DTLB, per CLAUDE.md's leading hypothesis: `cache_bytes` is always a multiple of the 4096-byte page size, so this experiment always exercises the DTLB too, not just whichever data-cache level it's aimed at), not the real L2 or LLC associativity.
  - **Conclusion: treat "L2 associativity = 4" and "LLC associativity = 4" for Charnwood as NOT resolved** — they are reproducible (0 spread across repeats) but reproducibly wrong in the sense of not reflecting the cache level nominally under test. This mirrors, and adds a cleaner same-machine A/B comparison to, the dead end CLAUDE.md already documents from Sunbird (10 of 13 large-stride candidates across a 27x byte range breaking at the same num_ways=10) and Upgrade (a two-step staircase with the same "~9" second step). Per that writeup, simply trying more candidate byte values is very unlikely to help; the "derived-stride scan" technique in `scripts/run_associativity_stride_scan.sh` (validated on Upgrade's L1, inconclusive on Upgrade's L2/LLC) is the only currently-proposed way forward, and hasn't been tried on this machine.
- **Known limitations / open questions specific to this run:**
  1. The L2 (262,144 B) and LLC (8,388,608 B) `cache_bytes` values were taken from `CAPACITY_RESULTS.md`'s cross-machine table, not from a hand-confirmed boundary in Charnwood's own capacity data — that data explicitly flags the ~1.83-11.31 MiB region as contaminated/unresolved (see `capacity/` section above). Even setting aside the confound finding above, this run cannot be treated as validating those specific byte values as Charnwood's real L2/LLC capacities.
  2. Because of finding above (L2 and LLC curves being numerically indistinguishable through num_ways=23), this run cannot currently distinguish whether Charnwood's real L2 capacity is actually 262,144 B, whether its real LLC capacity is actually ~8 MiB, or either — the method is bottlenecked on the shared confound before capacity choice even matters, consistent with CLAUDE.md's Sunbird conclusion ("no candidate byte value fixes it... something with ~9 slots saturates long before any real, larger cache set would").
  3. Per Phase I discipline, no hardware topology lookup (`cpuid`, `/proc/cpuinfo` cache fields, full `lscpu`) was used to investigate this further — see CLAUDE.md's "Phase I discipline caveat" for why that specific line of investigation was already tried and reverted on Sunbird.

### latency/
- Source file(s): 
- Run command + arguments: 
- Dependent-chain batch size N used: 
- Regular vs. randomized control included? 

### inclusion_policy/
- Source file(s): 
- Run command + arguments: 
- Eviction/reload construction: 

### pmu/ (Phase II only — leave blank until Phase I is frozen)
- `perf list` output filename: 
- Events collected + exact semantics on this CPU: 
- Run command + arguments: 

## Reservation Log (if applicable)
- Reserved core/package: 
- Time window: 
